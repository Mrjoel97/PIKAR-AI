---
phase: 12.1-agent-knowledge-base
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260323100000_admin_knowledge_base.sql
  - app/services/knowledge_service.py
  - tests/unit/admin/test_knowledge_service.py
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03, KNOW-04, KNOW-06]

must_haves:
  truths:
    - "PDF text is extracted and chunked into embeddings with scope=system metadata"
    - "DOCX/TXT/MD text is extracted and chunked into embeddings with scope=system metadata"
    - "Images are stored in Supabase Storage and their Gemini-generated descriptions are embedded"
    - "Video uploads are enqueued as ai_jobs with status=processing (not processed inline)"
    - "Knowledge can be scoped to a specific agent or made globally available (agent_scope=null)"
    - "System-scoped embeddings can be searched without returning user-scoped embeddings"
  artifacts:
    - path: "supabase/migrations/20260323100000_admin_knowledge_base.sql"
      provides: "admin_knowledge_entries table, match_system_knowledge RPC, admin-knowledge bucket, permission seeds"
      contains: "CREATE TABLE admin_knowledge_entries"
    - path: "app/services/knowledge_service.py"
      provides: "process_document, process_image, process_video, search_system_knowledge, get_knowledge_stats"
      exports: ["process_document", "process_image", "process_video", "search_system_knowledge", "get_knowledge_stats"]
    - path: "tests/unit/admin/test_knowledge_service.py"
      provides: "Unit tests for knowledge service"
      min_lines: 100
  key_links:
    - from: "app/services/knowledge_service.py"
      to: "app/rag/ingestion_service.py"
      via: "ingest_document() for chunking + embedding"
      pattern: "from app\\.rag\\.ingestion_service import"
    - from: "app/services/knowledge_service.py"
      to: "app/rag/embedding_service.py"
      via: "generate_embeddings_batch for image description embedding"
      pattern: "from app\\.rag\\.embedding_service import"
    - from: "supabase/migrations/20260323100000_admin_knowledge_base.sql"
      to: "embeddings table"
      via: "match_system_knowledge RPC queries existing embeddings with scope filter"
      pattern: "CREATE.*FUNCTION.*match_system_knowledge"
---

<objective>
Create the database schema and backend service layer for the agent knowledge base -- the admin_knowledge_entries tracking table, Supabase Storage bucket, match_system_knowledge RPC, and a Python knowledge_service.py that orchestrates file extraction, chunking, embedding, and system-scope search.

Purpose: This is the data foundation. All subsequent plans (tools, API, UI) call into knowledge_service.py. Without it, nothing can be uploaded, processed, or searched.

Output: SQL migration, knowledge_service.py with 5 exported functions, unit tests
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-agent-knowledge-base/12.1-RESEARCH.md

<interfaces>
<!-- Existing RAG pipeline interfaces the executor needs -->

From app/rag/ingestion_service.py:
```python
def chunk_text(text: str, chunk_size: int = 500, chunk_overlap: int = 50) -> list[str]:
    """Split text into overlapping chunks. Returns list of text chunks."""

async def ingest_document(
    supabase_client: Any,
    content: str,
    source_type: str,
    source_id: str | None = None,
    metadata: dict | None = None,
    user_id: str | None = None,
    agent_id: str | None = None,
    chunk_size: int = 500,
    chunk_overlap: int = 50,
) -> list[str]:
    """Chunks document, generates embeddings, inserts into embeddings table. Returns list of embedding IDs."""
```

From app/rag/embedding_service.py:
```python
EMBEDDING_MODEL = "text-embedding-004"
EMBEDDING_DIMENSION = 768

def generate_embeddings_batch(texts: list[str], batch_size: int = 5) -> list[list[float]]:
    """Generate embeddings for a batch of texts. Returns list of 768-dim float vectors."""
```

From app/services/supabase.py:
```python
def get_service_client():
    """Return Supabase service-role client (bypasses RLS)."""
```

From app/services/supabase_async.py:
```python
async def execute_async(query_builder) -> Any:
    """Execute a Supabase query builder in asyncio.to_thread. Returns result.data."""
```

From tests/unit/admin/conftest.py:
```python
@pytest.fixture
def mock_supabase_client():
    """Mock Supabase service client with rpc mock."""

@pytest.fixture
def admin_user_dict():
    """Returns {"id": "user-admin-uuid", "email": "admin@test.com", ...}"""
```

Existing embeddings table schema (from 0001_initial_schema.sql):
- id UUID PK, user_id UUID (nullable), agent_id UUID (nullable, FK agents.id)
- source_type TEXT, source_id TEXT, content TEXT, embedding vector(768)
- metadata JSONB, created_at TIMESTAMPTZ
- ivfflat index on embedding column

Existing ai_jobs table (background job pattern):
- id UUID PK, user_id UUID, job_type TEXT, status TEXT (pending/processing/completed/failed)
- input_data JSONB, output_data JSONB, created_at/updated_at TIMESTAMPTZ
- claim_next_ai_job RPC exists for worker pickup
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration — admin_knowledge_entries table, match_system_knowledge RPC, storage bucket, permission seeds</name>
  <files>supabase/migrations/20260323100000_admin_knowledge_base.sql</files>
  <behavior>
    - admin_knowledge_entries table exists with columns: id UUID PK, filename TEXT NOT NULL, file_type TEXT NOT NULL (document/image/video), mime_type TEXT, file_path TEXT, agent_scope TEXT (nullable, null=global), uploaded_by TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'processing', chunk_count INT DEFAULT 0, embedding_ids TEXT[] DEFAULT '{}', file_size_bytes BIGINT, error_message TEXT, created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now()
    - RLS enabled on admin_knowledge_entries (service-role bypass, no anon access)
    - match_system_knowledge(query_embedding vector(768), match_threshold float, match_count int, filter_agent_scope text DEFAULT NULL) RPC returns rows from embeddings where metadata->>'scope'='system' AND (filter_agent_scope IS NULL OR metadata->>'agent_scope' IS NULL OR metadata->>'agent_scope'=filter_agent_scope), ordered by cosine similarity
    - admin-knowledge storage bucket created (public=false, 100MB file size limit)
    - 8 permission seed rows in admin_agent_permissions for knowledge tools: upload_knowledge (confirm), list_knowledge_entries (auto), search_knowledge (auto), delete_knowledge_entry (confirm), get_knowledge_stats (auto), check_knowledge_duplicate (auto), validate_knowledge_relevance (auto), recommend_chunking_strategy (auto)
  </behavior>
  <action>
    Create SQL migration file `supabase/migrations/20260323100000_admin_knowledge_base.sql`:

    1. CREATE TABLE admin_knowledge_entries with all columns listed in behavior. Add index on agent_scope and status. RLS enabled with service-role-only policy (matching pattern from 20260323000000_agent_config_feature_flags.sql).

    2. CREATE FUNCTION match_system_knowledge that:
       - Accepts query_embedding vector(768), match_threshold float DEFAULT 0.5, match_count int DEFAULT 5, filter_agent_scope text DEFAULT NULL
       - Queries the EXISTING `embeddings` table filtering on metadata->>'scope' = 'system'
       - When filter_agent_scope IS NOT NULL: also filters (metadata->>'agent_scope' IS NULL OR metadata->>'agent_scope' = filter_agent_scope) — this returns both global AND agent-specific entries
       - When filter_agent_scope IS NULL: returns all system-scoped entries regardless of agent_scope
       - Orders by 1 - (embedding <=> query_embedding) DESC (cosine similarity)
       - Returns id, content, metadata, similarity score
       - IMPORTANT: Do NOT modify the existing match_embeddings function (per RESEARCH.md anti-pattern)

    3. INSERT INTO storage.buckets for 'admin-knowledge' bucket (public=false, file_size_limit=104857600)

    4. INSERT 8 rows into admin_agent_permissions for knowledge tools (action_category='knowledge'):
       - upload_knowledge: confirm tier, high risk
       - delete_knowledge_entry: confirm tier, medium risk
       - list_knowledge_entries, search_knowledge, get_knowledge_stats, check_knowledge_duplicate, validate_knowledge_relevance, recommend_chunking_strategy: auto tier, low risk

    Follow the exact pattern from 20260323000000_agent_config_feature_flags.sql for RLS policies and permission seeding.
  </action>
  <verify>
    <automated>uv run ruff check supabase/migrations/20260323100000_admin_knowledge_base.sql 2>/dev/null; echo "SQL file exists:" && test -f supabase/migrations/20260323100000_admin_knowledge_base.sql && echo "YES" || echo "NO"</automated>
  </verify>
  <done>Migration file exists with admin_knowledge_entries table, match_system_knowledge RPC, admin-knowledge bucket, and 8 permission seeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Knowledge service + unit tests — process_document, process_image, process_video, search_system_knowledge, get_knowledge_stats</name>
  <files>app/services/knowledge_service.py, tests/unit/admin/test_knowledge_service.py</files>
  <behavior>
    - test_process_pdf: process_document with PDF bytes calls pypdf PdfReader, chunks text, calls ingest_document with metadata scope=system and agent_scope, inserts admin_knowledge_entries row with status=completed and chunk_count
    - test_process_docx: process_document with DOCX bytes calls python-docx Document, same embedding flow
    - test_process_txt: process_document with TXT bytes reads raw text, same embedding flow
    - test_process_image: process_image calls Gemini vision to generate description, embeds description text, stores file in Supabase Storage, inserts admin_knowledge_entries row
    - test_process_video_enqueues: process_video stores raw file in Storage, inserts admin_knowledge_entries with status=processing, enqueues ai_jobs row with job_type=admin_knowledge_video
    - test_search_system_scope: search_system_knowledge calls match_system_knowledge RPC with query embedding and optional agent_scope filter
    - test_get_knowledge_stats: get_knowledge_stats returns per-agent counts, total entries, total embeddings, storage usage
    - test_process_document_empty: process_document with empty content returns error dict
  </behavior>
  <action>
    Create `app/services/knowledge_service.py` with 5 exported async functions:

    **1. process_document(file_bytes, filename, mime_type, agent_scope, uploaded_by) -> dict**
    - Detect file type from mime_type: application/pdf -> pypdf PdfReader, application/vnd.openxmlformats-officedocument.wordprocessingml.document -> python-docx Document, text/* -> raw decode
    - Extract text content. If empty, return {"error": "No text content extracted"}
    - Upload raw file to Supabase Storage bucket 'admin-knowledge' at path `{entry_id}/{filename}`
    - Call `ingest_document(client, content, source_type='admin_training', metadata={'scope': 'system', 'agent_scope': agent_scope, 'filename': filename})` — set agent_id=None, user_id=None (per RESEARCH.md Pitfall 1: do NOT use agent_id UUID FK)
    - Insert admin_knowledge_entries row with status='completed', chunk_count, embedding_ids from ingest_document return
    - Return {"entry_id": ..., "chunk_count": ..., "status": "completed"}

    **2. process_image(file_bytes, filename, mime_type, agent_scope, uploaded_by) -> dict**
    - Upload image to Storage at `{entry_id}/{filename}`
    - Call Gemini vision (gemini-2.5-flash) with the image bytes + prompt "Describe this image in detail for a business knowledge base. Include all visible text, objects, and context." — use try/except with google.genai import guard matching embedding_service.py pattern
    - Embed the text description via generate_embeddings_batch
    - Insert single embedding row into embeddings table with metadata scope=system, agent_scope, source_type=admin_training_image
    - Insert admin_knowledge_entries row with chunk_count=1, status='completed'
    - Return {"entry_id": ..., "description": first_200_chars, "status": "completed"}

    **3. process_video(file_bytes, filename, mime_type, agent_scope, uploaded_by) -> dict**
    - Upload video to Storage at `{entry_id}/{filename}`
    - Insert admin_knowledge_entries row with status='processing'
    - Insert ai_jobs row with job_type='admin_knowledge_video', input_data={'entry_id': ..., 'file_path': ..., 'agent_scope': agent_scope, 'mime_type': mime_type}
    - Return {"entry_id": ..., "status": "processing", "message": "Video queued for background transcription"}
    - Do NOT attempt inline transcription (per RESEARCH.md Pitfall 2)

    **4. search_system_knowledge(query, agent_name=None, top_k=5) -> list[dict]**
    - Generate embedding for query via generate_embeddings_batch([query])
    - Call supabase.rpc("match_system_knowledge", {query_embedding, match_threshold=0.5, match_count=top_k, filter_agent_scope=agent_name})
    - Return list of {content, similarity, metadata} dicts

    **5. get_knowledge_stats() -> dict**
    - Query admin_knowledge_entries grouped by agent_scope for per-agent counts
    - Count total entries, total embeddings (sum of chunk_count), distinct agent_scopes
    - Query Supabase Storage for admin-knowledge bucket usage (or compute from file_size_bytes sum)
    - Return {"total_entries": N, "total_embeddings": N, "by_agent": {...}, "storage_bytes": N}

    Use `get_service_client()` and `execute_async()` for all DB operations, following the exact patterns from `app/services/agent_config_service.py`.

    Create `tests/unit/admin/test_knowledge_service.py` with 8 tests matching behavior section. Mock `get_service_client`, `execute_async`, `ingest_document`, `generate_embeddings_batch`, and the google.genai client. Follow the mock patterns from `tests/unit/admin/test_config_service.py` (use both _SERVICE_CLIENT_PATCH and _EXECUTE_ASYNC_PATCH).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/admin/test_knowledge_service.py -x -v</automated>
  </verify>
  <done>knowledge_service.py exports 5 functions, all 8+ tests pass, PDF/DOCX/TXT extraction works, image description embedding works, video enqueues background job, system-scope search calls correct RPC, stats aggregation returns per-agent counts</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/admin/test_knowledge_service.py -x -v
uv run ruff check app/services/knowledge_service.py
test -f supabase/migrations/20260323100000_admin_knowledge_base.sql
</verification>

<success_criteria>
- SQL migration defines admin_knowledge_entries table, match_system_knowledge RPC, admin-knowledge bucket, 8 permission seeds
- knowledge_service.py exports process_document, process_image, process_video, search_system_knowledge, get_knowledge_stats
- process_document handles PDF, DOCX, TXT, MD file types
- process_image generates Gemini vision description and embeds it
- process_video returns 202-style processing response with ai_jobs enqueue
- System knowledge search filters on metadata scope=system without touching user embeddings
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-agent-knowledge-base/12.1-01-SUMMARY.md`
</output>
