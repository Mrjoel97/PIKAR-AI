---
phase: 07-foundation
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/app/(admin)/layout.tsx
  - frontend/src/app/(admin)/page.tsx
  - frontend/src/app/(admin)/audit-log/page.tsx
  - frontend/src/components/admin/AdminSidebar.tsx
  - frontend/src/components/admin/AdminChatPanel.tsx
  - frontend/src/components/admin/ConfirmationCard.tsx
  - frontend/src/components/admin/adminNav.ts
  - frontend/src/hooks/useAdminChat.ts
autonomous: true
requirements: [ASST-01, ASST-04, ASST-06, AUDT-03, AUTH-05]

must_haves:
  truths:
    - "Non-admin user navigating to /admin is redirected to /dashboard server-side (no admin UI flash)"
    - "Admin user sees the admin layout with sidebar navigation and collapsible chat panel"
    - "Admin can type a message and receive streaming SSE responses from the AdminAgent"
    - "When agent returns requires_confirmation, a ConfirmationCard renders with Confirm/Cancel buttons"
    - "Confirm button sends confirmation_token back to POST /admin/chat and is disabled after first click"
    - "Chat history reloads from admin_chat_messages on page refresh (session persistence)"
    - "Admin can browse audit trail entries at /admin/audit-log with filters"
    - "Sidebar shows navigation for all admin sections (Overview, Users, Monitor, Analytics, Approvals, Config, Billing, Integrations, Settings)"
  artifacts:
    - path: "frontend/src/app/(admin)/layout.tsx"
      provides: "Admin layout with server-side access check, sidebar, and chat panel"
      min_lines: 30
    - path: "frontend/src/hooks/useAdminChat.ts"
      provides: "SSE chat hook with confirmation state management"
      exports: ["useAdminChat"]
    - path: "frontend/src/components/admin/AdminChatPanel.tsx"
      provides: "Collapsible chat panel docked to bottom"
    - path: "frontend/src/components/admin/ConfirmationCard.tsx"
      provides: "Action confirmation card with Confirm/Cancel buttons"
    - path: "frontend/src/components/admin/AdminSidebar.tsx"
      provides: "Admin navigation sidebar"
    - path: "frontend/src/app/(admin)/audit-log/page.tsx"
      provides: "Audit log viewer with table and filters"
  key_links:
    - from: "frontend/src/app/(admin)/layout.tsx"
      to: "/admin/check-access"
      via: "Server-side fetch in layout component"
      pattern: "fetch.*admin/check-access"
    - from: "frontend/src/hooks/useAdminChat.ts"
      to: "/admin/chat"
      via: "fetchEventSource SSE connection"
      pattern: "fetchEventSource.*admin/chat"
    - from: "frontend/src/hooks/useAdminChat.ts"
      to: "admin_chat_messages"
      via: "Supabase query for history on mount"
      pattern: "admin_chat_messages.*select"
    - from: "frontend/src/components/admin/ConfirmationCard.tsx"
      to: "frontend/src/hooks/useAdminChat.ts"
      via: "confirmAction callback with token"
      pattern: "confirmAction|confirmation_token"
---

<objective>
Build the entire admin frontend: server-side AdminGuard layout, admin sidebar, collapsible AI chat panel with SSE streaming, confirmation card component, useAdminChat hook, and audit log viewer page.

Purpose: This plan creates everything the admin sees and interacts with. The layout's server-side access check (via Plan 01's check-access endpoint) prevents non-admins from ever seeing admin UI. The chat panel connects to Plan 03's SSE endpoint. The confirmation card implements the confirm-tier UX. The audit log viewer shows the trail from Plan 02's audit service.

Output: Complete admin frontend shell with working chat, confirmation flow, and audit trail viewing.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation/07-CONTEXT.md
@.planning/phases/07-foundation/07-RESEARCH.md
@.planning/phases/07-foundation/07-01-SUMMARY.md

@frontend/src/hooks/useAgentChat.ts (template for useAdminChat)
@frontend/src/components/layout/Sidebar.tsx (template for AdminSidebar)
@frontend/src/components/layout/sidebarNav.ts (template for adminNav)
@frontend/src/lib/supabase/server.ts (server-side Supabase client for layout)
@frontend/src/lib/supabase/client.ts (client-side Supabase for chat history)
@frontend/src/app/(personas)/layout.tsx (route group pattern)

<interfaces>
<!-- Contracts the executor needs from existing code and Plan 01 -->

From frontend/src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>;
// Server-side client using cookies — use in server components
```

From frontend/src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient;
// Client-side Supabase client — use in hooks/client components
```

From frontend/src/hooks/useAgentChat.ts (key patterns to mirror):
```typescript
import { fetchEventSource } from '@microsoft/fetch-event-source';
export type Message = { id?: string; role: 'user' | 'agent' | 'system'; text?: string; ... };
export function useAgentChat(options?: UseAgentChatOptions): {
  messages: Message[]; sendMessage: (text: string) => Promise<void>;
  isStreaming: boolean; error: string | null; ...
};
```

Backend endpoints (from Plans 01 + 03):
- GET /admin/check-access -> {access: true, email, admin_source} or 403
- POST /admin/chat (SSE) -> body: {session_id?, message, confirmation_token?}
- GET /admin/chat/sessions -> [{id, title, created_at, updated_at}]
- GET /admin/chat/history/{session_id} -> [{id, role, content, metadata, created_at}]
- GET /admin/audit-log -> audit entries with query param filters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin layout + AdminGuard + sidebar + navigation</name>
  <files>
    frontend/src/app/(admin)/layout.tsx
    frontend/src/app/(admin)/page.tsx
    frontend/src/components/admin/AdminSidebar.tsx
    frontend/src/components/admin/adminNav.ts
  </files>
  <action>
    1. Create `frontend/src/components/admin/adminNav.ts`:
       - Export `ADMIN_NAV_ITEMS` array with: Overview (/admin), Users (/admin/users), Monitor (/admin/monitoring), Analytics (/admin/analytics), Approvals (/admin/approvals), Config (/admin/config), Billing (/admin/billing), Integrations (/admin/integrations), Settings (/admin/settings), Audit Log (/admin/audit-log)
       - Each item: { label, href, icon } using Lucide icons (LayoutDashboard, Users, Activity, BarChart3, CheckCircle, Settings, CreditCard, Plug, Shield, FileText)
       - Follow the pattern from `frontend/src/components/layout/sidebarNav.ts`

    2. Create `frontend/src/components/admin/AdminSidebar.tsx`:
       - 'use client' component
       - Dark theme sidebar (bg-gray-900 text-gray-100) — consistent with existing dark dashboard
       - Header: "Pikar Admin" branding
       - Navigation: map ADMIN_NAV_ITEMS with active state highlighting (usePathname)
       - Bottom: admin email display + back to dashboard link
       - Follow the Sidebar.tsx pattern but with dark theme and admin-specific nav

    3. Create `frontend/src/app/(admin)/layout.tsx`:
       - Server component (NO 'use client')
       - Import `createClient` from `@/lib/supabase/server`
       - Get session from server-side Supabase client: `const { data: { session } } = await supabase.auth.getSession()`
       - If no session, redirect to `/auth/login`
       - Extract access token from session
       - Fetch `GET {API_URL}/admin/check-access` with Bearer token
       - If response not OK (403), redirect to `/dashboard` — non-admin never sees admin UI
       - On success, render: flex layout with AdminSidebar + main content area + AdminChatPanel (placeholder div for now — Task 2 will create the real component)
       - Layout: `<div className="flex h-screen bg-gray-950 text-gray-100">` wrapping sidebar + main + chat

    4. Create `frontend/src/app/(admin)/page.tsx`:
       - Simple admin overview/welcome page
       - "Welcome to Pikar Admin" heading
       - Placeholder cards for system status (will be populated in later phases)
       - This is what admins see at /admin

    5. Verify by running frontend lint.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai/frontend && npx next lint --dir src/app/\(admin\) src/components/admin 2>/dev/null || echo "Lint check completed"</automated>
  </verify>
  <done>
    - /admin route group exists with server-side AdminGuard in layout.tsx
    - Non-admin redirect happens server-side (no UI flash)
    - AdminSidebar renders with 10 navigation items in dark theme
    - /admin page shows welcome/overview
    - Layout structured for sidebar + main + chat panel
  </done>
</task>

<task type="auto">
  <name>Task 2: useAdminChat hook + AdminChatPanel + ConfirmationCard + Audit log page</name>
  <files>
    frontend/src/hooks/useAdminChat.ts
    frontend/src/components/admin/AdminChatPanel.tsx
    frontend/src/components/admin/ConfirmationCard.tsx
    frontend/src/app/(admin)/audit-log/page.tsx
    frontend/src/app/(admin)/layout.tsx
  </files>
  <action>
    1. Create `frontend/src/hooks/useAdminChat.ts`:
       - Mirror `useAgentChat.ts` structure but simpler (no widget display, no agent modes)
       - State: messages (Message[]), isStreaming, error, currentSessionId, pendingConfirmation
       - `Message` type: { id?: string, role: 'user' | 'agent' | 'system', text?: string, confirmation?: ConfirmationData }
       - `ConfirmationData` type: { token: string, action_details: { action: string, risk_level: string, description: string } }
       - `loadSessions()`: fetch GET /admin/chat/sessions using Supabase client auth token
       - `loadHistory(sessionId)`: fetch GET /admin/chat/history/{sessionId}, populate messages
       - `sendMessage(text)`: POST to /admin/chat via fetchEventSource with { session_id, message }, parse SSE chunks, append to messages
       - During SSE parsing: detect `requires_confirmation: true` in streamed data, set pendingConfirmation state
       - `confirmAction(token)`: POST to /admin/chat with { session_id, confirmation_token: token, message: "" }, clear pendingConfirmation on success
       - `rejectAction()`: clear pendingConfirmation, add system message "Action cancelled"
       - On mount: if initialSessionId provided, loadHistory. Otherwise load sessions list.
       - Auth: get Supabase session token for Authorization header on all API calls
       - Return: { messages, isStreaming, error, sendMessage, confirmAction, rejectAction, pendingConfirmation, sessions, loadHistory, currentSessionId }

    2. Create `frontend/src/components/admin/ConfirmationCard.tsx`:
       - 'use client' component
       - Props: { confirmation: ConfirmationData, onConfirm: (token: string) => void, onReject: () => void, isProcessing: boolean }
       - Render: card with yellow/amber border (warning style), action name, risk level badge, description text
       - [Cancel] button (gray, always enabled)
       - [Confirm] button (red for high risk, amber for medium, green for low) — disabled when isProcessing (double-click protection)
       - On Confirm click: call onConfirm(token) and button becomes disabled with spinner

    3. Create `frontend/src/components/admin/AdminChatPanel.tsx`:
       - 'use client' component
       - Collapsible panel docked to bottom-right of the admin layout
       - Collapsed state: single input bar with expand button, shows "Ask me anything about the platform..." placeholder
       - Expanded state: full chat panel with message list, input field, send button
       - Use `useAdminChat` hook for all state management
       - Message rendering: user messages right-aligned (blue), agent messages left-aligned (gray)
       - When pendingConfirmation is set, render ConfirmationCard inline in the message list
       - Auto-scroll to bottom on new messages
       - Session switcher: dropdown or tabs for switching between sessions
       - Greeting on first mount: fetch health status and show "All systems healthy" or error count

    4. Create `frontend/src/app/(admin)/audit-log/page.tsx`:
       - 'use client' component
       - Fetch audit entries from GET /admin/audit-log with auth token
       - Table display: timestamp, admin email, action, target, source tag (with color-coded badge), details (expandable)
       - Filter bar: source filter (all/manual/ai_agent/impersonation/monitoring_loop), date range
       - Pagination: simple prev/next with limit/offset query params
       - Empty state: "No audit entries yet"
       - Error state: "Failed to load audit log" with retry button

    5. Update `frontend/src/app/(admin)/layout.tsx`:
       - Replace placeholder chat panel div with actual `<AdminChatPanel />` import
       - AdminChatPanel is a client component, layout is server component — import is fine (Next.js handles this)

    6. Verify frontend builds without errors.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai/frontend && npx tsc --noEmit 2>&1 | head -30 || echo "Type check completed"</automated>
  </verify>
  <done>
    - useAdminChat hook provides SSE streaming, session persistence, confirmation state
    - AdminChatPanel renders collapsible chat with message list and input
    - ConfirmationCard renders with Confirm/Cancel buttons and double-click protection
    - Audit log page shows table with filters and pagination
    - Layout imports and renders AdminChatPanel
    - Frontend compiles without TypeScript errors
  </done>
</task>

</tasks>

<verification>
- Frontend TypeScript compilation: `cd frontend && npx tsc --noEmit`
- Admin route group exists: `ls frontend/src/app/(admin)/`
- Key components exist: AdminSidebar, AdminChatPanel, ConfirmationCard, useAdminChat
- Audit log page at /admin/audit-log
</verification>

<success_criteria>
- Non-admin users redirected server-side from /admin to /dashboard (AUTH-05)
- Admin sees sidebar with 10 nav items + collapsible chat panel (ASST-01)
- Chat streams AdminAgent responses via SSE (ASST-01)
- Confirm-tier responses render ConfirmationCard with Confirm/Cancel (ASST-04)
- Confirm button disabled after click (double-click protection) (ASST-05 frontend)
- Chat history reloads on refresh via admin_chat_messages query (ASST-06)
- Audit log viewer at /admin/audit-log with source filters (AUDT-03)
- Frontend compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-04-SUMMARY.md`
</output>
