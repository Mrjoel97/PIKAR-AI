---
phase: 07-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agents/admin/__init__.py
  - app/agents/admin/agent.py
  - app/agents/admin/tools/__init__.py
  - app/agents/admin/tools/health.py
  - app/services/admin_audit.py
  - app/services/confirmation_tokens.py
  - tests/unit/admin/test_admin_agent.py
  - tests/unit/admin/test_autonomy.py
  - tests/unit/admin/test_confirmation.py
  - tests/unit/admin/test_audit.py
autonomous: true
requirements: [ASST-03, ASST-05, AUDT-01]

must_haves:
  truths:
    - "AdminAgent is a valid ADK Agent that can be instantiated and run"
    - "check_system_health tool checks autonomy permissions before executing"
    - "An auto-tier tool executes immediately and returns results"
    - "A confirm-tier tool returns requires_confirmation=True with a UUID token"
    - "A blocked-tier tool returns an error explanation without executing"
    - "Confirmation tokens are stored in Redis with 5-minute TTL"
    - "Confirmation tokens are consumed atomically (GETDEL) — second consumption returns None"
    - "log_admin_action writes to admin_audit_log with correct source tag"
  artifacts:
    - path: "app/agents/admin/agent.py"
      provides: "AdminAgent singleton + create_admin_agent factory"
      exports: ["admin_agent", "create_admin_agent"]
    - path: "app/agents/admin/tools/health.py"
      provides: "check_system_health tool with autonomy enforcement"
      exports: ["check_system_health"]
    - path: "app/services/admin_audit.py"
      provides: "Audit logging service"
      exports: ["log_admin_action"]
    - path: "app/services/confirmation_tokens.py"
      provides: "Confirmation token store/consume via Redis"
      exports: ["store_confirmation_token", "consume_confirmation_token"]
  key_links:
    - from: "app/agents/admin/tools/health.py"
      to: "app/services/supabase.py"
      via: "admin_agent_permissions table query"
      pattern: "admin_agent_permissions.*autonomy_level"
    - from: "app/services/confirmation_tokens.py"
      to: "app/services/cache.py"
      via: "get_cache_service for Redis client"
      pattern: "get_cache_service"
    - from: "app/services/admin_audit.py"
      to: "app/services/supabase.py"
      via: "admin_audit_log table insert"
      pattern: "admin_audit_log.*insert"
---

<objective>
Build the AdminAgent (ADK agent with factory pattern), the autonomy enforcement layer, confirmation token management, and audit logging service.

Purpose: This plan creates the AI brain and trust enforcement that powers the admin chat. The AdminAgent needs to exist before any SSE endpoint can stream its output, and the autonomy/confirmation/audit services must be in place before any tool can safely execute. These are pure backend services with zero file overlap with Plan 01 (which handles auth/DB/encryption), so they run in parallel.

Note: ASST-02 ("AdminAgent has 30+ tools across 7 domains") is a cross-phase requirement that will be incrementally fulfilled as domain tools are added in Phases 8-15. Phase 7 builds the foundation (1 tool: check_system_health) and the autonomy enforcement infrastructure that all future tools will use. ASST-02 is NOT claimed by this plan.

Output: AdminAgent with check_system_health tool, autonomy tier enforcement in tool code, Redis-backed confirmation tokens with atomic consumption, and audit log service.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation/07-CONTEXT.md
@.planning/phases/07-foundation/07-RESEARCH.md

@app/agents/financial/agent.py (agent factory pattern template)
@app/agents/base_agent.py (PikarAgent base class)
@app/agents/shared.py (get_model, get_fallback_model, FAST_AGENT_CONFIG)
@app/services/cache.py (CacheService + get_cache_service for Redis access)
@app/services/supabase.py (get_service_client)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/agents/base_agent.py:
```python
from google.adk.agents import Agent as _BaseAgent
class PikarAgent(_BaseAgent):
    # Extends base ADK Agent — use as drop-in replacement
```

From app/agents/shared.py:
```python
def get_model() -> str: ...       # Returns primary model name (Gemini 2.5 Pro)
def get_fallback_model() -> str: ...  # Returns fallback model name (Gemini 2.5 Flash)
FAST_AGENT_CONFIG = ...           # GenerateContentConfig for fast responses
DEEP_AGENT_CONFIG = ...           # GenerateContentConfig for detailed responses
```

From app/services/cache.py:
```python
class CacheService:
    _redis: redis.asyncio.Redis | None
    async def _get_redis(self) -> redis.asyncio.Redis | None: ...

def get_cache_service() -> CacheService: ...  # Singleton accessor
```

From app/services/supabase.py:
```python
def get_service_client() -> Client: ...  # Service-role client, bypasses RLS
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminAgent + check_system_health tool with autonomy enforcement</name>
  <files>
    app/agents/admin/__init__.py
    app/agents/admin/agent.py
    app/agents/admin/tools/__init__.py
    app/agents/admin/tools/health.py
    tests/unit/admin/test_admin_agent.py
    tests/unit/admin/test_autonomy.py
  </files>
  <behavior>
    - test_admin_agent_instantiation: admin_agent is an Agent instance with name="AdminAgent" and check_system_health in tools
    - test_create_admin_agent_factory: create_admin_agent() returns a new Agent instance, create_admin_agent("_test") has name="AdminAgent_test"
    - test_health_tool_auto_tier: Mock admin_agent_permissions query returning autonomy_level="auto" -> check_system_health executes and returns overall_status
    - test_health_tool_confirm_tier: Mock permissions returning "confirm" -> returns {requires_confirmation: True, confirmation_token: <uuid>, action_details: {...}}
    - test_health_tool_blocked_tier: Mock permissions returning "blocked" -> returns {error: "...blocked..."}
    - test_health_tool_permission_db_error: Mock permissions query raises exception -> defaults to auto, executes normally
    - test_health_tool_returns_service_status: When auto tier, tool returns dict with overall_status and services keys
  </behavior>
  <action>
    1. Create test files first:
       - `tests/unit/admin/test_admin_agent.py` — tests for agent instantiation and factory
       - `tests/unit/admin/test_autonomy.py` — tests for autonomy tier enforcement in check_system_health

    2. Create `app/agents/admin/__init__.py`:
       - Export admin_agent, create_admin_agent from agent module

    3. Create `app/agents/admin/tools/__init__.py`:
       - Export check_system_health from health module

    4. Create `app/agents/admin/tools/health.py`:
       - `async def check_system_health() -> dict` — the Phase 7 health tool
       - FIRST: query admin_agent_permissions table for action_name="check_system_health"
       - If autonomy_level == "blocked": return error dict
       - If autonomy_level == "confirm": generate UUID token, return {requires_confirmation: True, confirmation_token, action_details}
       - If autonomy_level == "auto" (or DB error fallback): execute health check
       - For health check execution: import and call existing health endpoint functions directly (NOT via HTTP). Import `get_liveness` from `app.fast_api_app`. Wrap in try/except for each endpoint.
       - Return {overall_status: "healthy"|"degraded"|"unhealthy", services: {...}, summary: "..."}
       - Use `get_service_client()` for DB queries, NOT the user-scoped client

    5. Create `app/agents/admin/agent.py`:
       - Import `PikarAgent as Agent` from `app.agents.base_agent`
       - Import `get_model`, `FAST_AGENT_CONFIG` from `app.agents.shared`
       - Import `check_system_health` from tools
       - Define ADMIN_AGENT_INSTRUCTION constant per 07-RESEARCH.md Pattern 2
       - Create `admin_agent` singleton: Agent(name="AdminAgent", model=get_model(), tools=[check_system_health], ...)
       - Create `create_admin_agent(name_suffix="") -> Agent` factory function

    6. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/admin/test_admin_agent.py tests/unit/admin/test_autonomy.py -x -v</automated>
  </verify>
  <done>
    - AdminAgent instantiates with check_system_health tool
    - create_admin_agent factory produces fresh instances
    - check_system_health enforces autonomy tiers from admin_agent_permissions DB table
    - Auto tier executes, confirm tier returns token, blocked tier returns error
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmation token service + audit logging service</name>
  <files>
    app/services/confirmation_tokens.py
    app/services/admin_audit.py
    tests/unit/admin/test_confirmation.py
    tests/unit/admin/test_audit.py
  </files>
  <behavior>
    - test_store_token: store_confirmation_token("tok", {action: "test"}, "user1") stores in Redis with key "admin:confirm:tok" and 300s TTL
    - test_consume_token_success: After storing, consume_confirmation_token("tok") returns the stored payload
    - test_consume_token_double: After consuming once, second consume returns None (atomic single-use)
    - test_consume_token_expired: Token not in Redis -> returns None
    - test_store_token_no_redis: Redis unavailable -> returns False gracefully
    - test_consume_token_no_redis: Redis unavailable -> returns None gracefully
    - test_log_admin_action: log_admin_action(admin_user_id, action, target_type, target_id, details, source) inserts row into admin_audit_log
    - test_log_admin_action_monitoring: log_admin_action with source="monitoring_loop" and admin_user_id=None succeeds
    - test_log_admin_action_all_sources: Each source tag ('manual', 'ai_agent', 'impersonation', 'monitoring_loop') is accepted
  </behavior>
  <action>
    1. Create test files first:
       - `tests/unit/admin/test_confirmation.py` — tests for token store/consume (mock Redis client)
       - `tests/unit/admin/test_audit.py` — tests for log_admin_action (mock Supabase client)

    2. Create `app/services/confirmation_tokens.py`:
       - `async def store_confirmation_token(token: str, action_details: dict, admin_user_id: str) -> bool`
         - Get Redis client via `get_cache_service()._get_redis()` (or add a `get_redis_client()` accessor)
         - If Redis unavailable, return False
         - Store JSON payload at key `admin:confirm:{token}` with `ex=300` (5-min TTL)
         - Payload: `{"action_details": action_details, "admin_user_id": admin_user_id}`
       - `async def consume_confirmation_token(token: str) -> dict | None`
         - Get Redis client
         - If Redis unavailable, return None
         - Use `await redis_client.getdel(f"admin:confirm:{token}")` for atomic consume
         - If None (expired or already consumed), return None
         - Parse and return JSON payload
       - Follow exact pattern from 07-RESEARCH.md Pattern 4

    3. Create `app/services/admin_audit.py`:
       - `async def log_admin_action(admin_user_id: str | None, action: str, target_type: str | None, target_id: str | None, details: dict | None, source: str) -> None`
         - Validate source is one of ('manual', 'ai_agent', 'impersonation', 'monitoring_loop')
         - Use `get_service_client()` (service role to bypass RLS)
         - Insert into admin_audit_log table
         - Log errors but do not raise (audit should not break the action it's logging)

    4. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/admin/test_confirmation.py tests/unit/admin/test_audit.py -x -v</automated>
  </verify>
  <done>
    - store_confirmation_token stores in Redis with 5-min TTL, returns True/False
    - consume_confirmation_token atomically consumes via GETDEL, second call returns None
    - Both functions degrade gracefully when Redis is unavailable
    - log_admin_action inserts into admin_audit_log with correct source tag
    - admin_user_id can be NULL for monitoring_loop source
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/admin/test_admin_agent.py tests/unit/admin/test_autonomy.py tests/unit/admin/test_confirmation.py tests/unit/admin/test_audit.py -x -v` — all tests pass
- `uv run ruff check app/agents/admin/ app/services/confirmation_tokens.py app/services/admin_audit.py --fix` — no lint errors
- `uv run ruff format app/agents/admin/ app/services/confirmation_tokens.py app/services/admin_audit.py` — formatted
</verification>

<success_criteria>
- AdminAgent is a valid ADK Agent following the create_financial_agent() factory pattern
- check_system_health tool queries admin_agent_permissions table BEFORE executing
- Autonomy enforcement: auto executes, confirm returns token, blocked returns error
- Confirmation tokens stored in Redis with GETDEL atomic consumption
- Audit logging writes to admin_audit_log with all 4 source tags supported
- All services degrade gracefully when Redis/DB is unavailable
- All tests green, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-02-SUMMARY.md`
</output>