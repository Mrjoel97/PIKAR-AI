---
phase: 07-foundation
plan: 05
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/routers/admin/audit.py
  - app/routers/admin/__init__.py
autonomous: false
requirements: [AUDT-03, ASST-04, ASST-05, ASST-06, AUDT-01, AUTH-04]

must_haves:
  truths:
    - "GET /admin/audit-log returns paginated, filterable audit entries"
    - "Audit log entries include correct source tags (manual, ai_agent, monitoring_loop)"
    - "The admin panel end-to-end flow works: login -> chat -> confirm action -> audit entry appears"
    - "Non-admin redirect works in a real browser session"
    - "Chat persists across browser refresh"
    - "ADMIN_EMAILS env var is not visible in frontend client bundle"
  artifacts:
    - path: "app/routers/admin/audit.py"
      provides: "GET /admin/audit-log endpoint with filters and pagination"
      exports: ["router"]
  key_links:
    - from: "app/routers/admin/audit.py"
      to: "app/services/supabase.py"
      via: "admin_audit_log table query"
      pattern: "admin_audit_log.*select"
    - from: "app/routers/admin/audit.py"
      to: "app/middleware/admin_auth.py"
      via: "Depends(require_admin)"
      pattern: "Depends\\(require_admin\\)"
---

<objective>
Create the audit log API endpoint and verify the complete admin panel end-to-end through a human checkpoint.

Purpose: The audit log API is the last backend piece — it feeds the audit log viewer created in Plan 04. The checkpoint verifies that all Phase 7 requirements work together in a real browser session: auth gate, chat streaming, confirmation flow, session persistence, and audit trail.

Note: AUDT-04 ("Impersonation actions tagged with impersonation_session_id in audit log") is deferred to Phase 13 when impersonation functionality is built. The admin_audit_log table has a nullable impersonation_session_id column (schema-ready from Plan 01 migration), but no impersonation data flows through Phase 7.

Output: GET /admin/audit-log endpoint + verified end-to-end admin panel functionality.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation/07-CONTEXT.md
@.planning/phases/07-foundation/07-RESEARCH.md
@.planning/phases/07-foundation/07-01-SUMMARY.md
@.planning/phases/07-foundation/07-02-SUMMARY.md
@.planning/phases/07-foundation/07-03-SUMMARY.md
@.planning/phases/07-foundation/07-04-SUMMARY.md

<interfaces>
<!-- Contracts from prior plans -->

From app/middleware/admin_auth.py (Plan 01):
```python
async def require_admin(credentials = Depends(security)) -> dict:
    """Returns dict with id, email, admin_source."""
```

From app/services/admin_audit.py (Plan 02):
```python
async def log_admin_action(admin_user_id, action, target_type, target_id, details, source) -> None: ...
```

Database table (Plan 01 migration):
```sql
-- admin_audit_log columns: id, admin_user_id, action, target_type, target_id, details, source, impersonation_session_id (nullable, unused in Phase 7), created_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit log API endpoint</name>
  <files>
    app/routers/admin/audit.py
    app/routers/admin/__init__.py
  </files>
  <action>
    1. Create `app/routers/admin/audit.py`:
       - `router = APIRouter()`
       - `GET /audit-log` endpoint (will be mounted as GET /admin/audit-log via prefix)
       - Depends on require_admin
       - Rate limit: `@limiter.limit("120/minute")`
       - Query params: source (optional filter — 'manual'|'ai_agent'|'impersonation'|'monitoring_loop'), limit (int, default 50, max 100), offset (int, default 0), start_date (optional ISO date), end_date (optional ISO date)
       - Query admin_audit_log via get_service_client() (service role, bypasses RLS)
       - Apply filters: `.eq("source", source)` if provided, `.gte("created_at", start_date)` and `.lte("created_at", end_date)` if provided
       - Order by created_at desc
       - Apply `.range(offset, offset + limit - 1)` for pagination
       - Return: `{"entries": [...], "total": count, "limit": limit, "offset": offset}`
       - Error handling: try/except, return 500 with error message on DB failure

    2. Update `app/routers/admin/__init__.py`:
       - Import audit router: `from app.routers.admin.audit import router as audit_router`
       - Include it: `admin_router.include_router(audit_router)`

    3. Lint and verify.
  </action>
  <verify>
    <automated>uv run ruff check app/routers/admin/audit.py --fix && uv run ruff format app/routers/admin/audit.py && uv run pytest tests/unit/admin/ -x -q</automated>
  </verify>
  <done>
    - GET /admin/audit-log returns paginated audit entries
    - Supports source, date range, limit/offset filters
    - Rate limited at 120/minute, requires admin auth
    - Lint clean, existing tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end admin panel verification</name>
  <files>N/A</files>
  <action>
    Human verification of complete admin panel foundation. Phase 7 requirements verified (13 of 15 — ASST-02 and AUDT-04 are cross-phase, deferred to Phases 8-15 and Phase 13 respectively):
    - Server-side auth gate (AUTH-01..05)
    - AdminAgent with SSE chat (ASST-01, ASST-03..06)
    - Autonomy tier enforcement and confirmation flow
    - Session persistence across refresh
    - Audit trail logging and viewing (AUDT-01..03)
    - MultiFernet encryption (AUDT-02)

    Prerequisites: Backend running (`make local-backend`), frontend running (`cd frontend && npm run dev`), Supabase running with migration applied (`supabase db push --local`), ADMIN_EMAILS env var set to your email.

    Verification steps:

    1. Auth gate (AUTH-01..05):
       - Login as your admin email account
       - Navigate to /admin — verify admin panel with sidebar and chat panel
       - DevTools > Sources > search for "ADMIN_EMAILS" — must NOT appear in client bundle (AUTH-04)
       - Incognito window: login as non-admin, navigate to /admin — verify redirect to /dashboard (AUTH-05)

    2. AdminAgent chat (ASST-01, ASST-06):
       - Expand chat panel, type "What is the current system health?" and send
       - Verify SSE streaming response from AdminAgent (ASST-01)
       - Refresh browser (F5) — verify chat history reloads (ASST-06)

    3. Confirmation flow (ASST-03, ASST-04, ASST-05):
       - In DB: change check_system_health autonomy_level to 'confirm'
       - Ask "Check system health" — verify confirmation card with Confirm/Cancel (ASST-04)
       - Click Confirm — verify action executes (ASST-05)
       - Reset autonomy_level to 'auto'

    4. Audit trail (AUDT-01, AUDT-03):
       - Navigate to /admin/audit-log — verify entries with source="ai_agent" (AUDT-01)
       - Use source filter — verify filtering works (AUDT-03)
  </action>
  <verify>Human verification — type "approved" or describe issues</verify>
  <done>
    - 13 Phase 7 requirements verified working in browser (ASST-02 deferred to Phases 8-15, AUDT-04 deferred to Phase 13)
    - Auth gate blocks non-admins, allows admins
    - Chat streams and persists across refresh
    - Confirmation flow works with double-click protection
    - Audit log shows entries with correct source tags
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/admin/ -x -v` — all unit tests pass
- `uv run ruff check app/routers/admin/ app/middleware/ app/services/encryption.py app/services/confirmation_tokens.py app/services/admin_audit.py app/agents/admin/ --fix` — all lint clean
- Manual browser verification of Phase 7 success criteria from ROADMAP
</verification>

<success_criteria>
- Audit log API returns paginated, filterable entries (AUDT-03)
- End-to-end flow verified in browser: auth -> chat -> confirm -> audit
- 13 Phase 7 requirements confirmed working (ASST-02, AUDT-04 deferred — see traceability notes)
- All tests pass, all code lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-05-SUMMARY.md`
</output>