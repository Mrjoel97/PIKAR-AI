---
phase: 55-integration-quality-load-testing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/integration_manager.py
  - app/services/google_workspace_auth_service.py
  - app/routers/configuration.py
  - frontend/src/app/dashboard/configuration/page.tsx
  - tests/unit/test_integration_manager.py
  - tests/unit/app/test_google_workspace_auth_service.py
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "Disconnecting an OAuth provider clears stale lifecycle state rather than leaving behind broken sync/error residue"
    - "Google Workspace has a backend-owned disconnect lifecycle that does not expose tokens in the browser"
    - "A reconnect starts from a truthful disconnected/reconnectable state rather than inheriting stale status"
    - "Regression tests cover the disconnect truth contract for both generic integrations and Google Workspace"
  artifacts:
    - path: "app/services/integration_manager.py"
      provides: "OAuth credential disconnect lifecycle cleanup"
      contains: "delete_credentials"
    - path: "app/services/google_workspace_auth_service.py"
      provides: "Canonical Google Workspace disconnect lifecycle"
      contains: "disconnect"
    - path: "app/routers/configuration.py"
      provides: "Authenticated Google Workspace disconnect endpoint"
      contains: "google-workspace"
    - path: "tests/unit/test_integration_manager.py"
      provides: "Lifecycle coverage for stale sync-state cleanup on disconnect"
      contains: "delete_credentials"
    - path: "tests/unit/app/test_google_workspace_auth_service.py"
      provides: "Google Workspace disconnect and router contract coverage"
      contains: "disconnect"
  key_links:
    - from: "app/routers/configuration.py"
      to: "app/services/google_workspace_auth_service.py"
      via: "backend-owned disconnect truth path"
      pattern: "disconnect"
    - from: "frontend/src/app/dashboard/configuration/page.tsx"
      to: "app/routers/configuration.py"
      via: "Google Workspace disconnect/reconnect lifecycle"
      pattern: "google-workspace"
---

<objective>
Make OAuth lifecycle state truthful across disconnect and reconnect boundaries.

Purpose: Phase 39 and Phase 54 established credential storage and Google Workspace callback sync, but disconnect still risks leaving stale sync/error state behind and Google Workspace still lacks an explicit disconnect path. This plan closes that seam before broader concurrency testing begins.

Output: stale-state cleanup for provider-backed integrations, Google Workspace disconnect support, and focused lifecycle regression tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-integration-quality-load-testing/55-CONTEXT.md
@.planning/phases/55-integration-quality-load-testing/55-RESEARCH.md
@.planning/phases/55-integration-quality-load-testing/55-VALIDATION.md

@app/services/integration_manager.py
@app/services/google_workspace_auth_service.py
@app/routers/integrations.py
@app/routers/configuration.py
@frontend/src/app/dashboard/configuration/page.tsx
@tests/unit/test_integration_manager.py
@tests/unit/app/test_google_workspace_auth_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clear stale sync state when a provider is disconnected</name>
  <files>app/services/integration_manager.py, tests/unit/test_integration_manager.py</files>
  <action>
1. Update `IntegrationManager.delete_credentials()` so disconnect cleanup also removes or neutralizes stale `integration_sync_state` for the same `user_id` + `provider`.
2. Ensure the lifecycle result is truthful for later status checks — a disconnected provider should not continue surfacing old error state.
3. Extend `tests/unit/test_integration_manager.py` to prove disconnect cleanup covers both credential removal and stale-state cleanup behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_integration_manager.py -x</automated>
  </verify>
  <done>Generic provider disconnect no longer leaves stale sync/error state behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a backend-owned Google Workspace disconnect lifecycle</name>
  <files>app/services/google_workspace_auth_service.py, app/routers/configuration.py, tests/unit/app/test_google_workspace_auth_service.py</files>
  <action>
1. Add a canonical Google Workspace disconnect helper that removes the stored server-side credential record without moving secrets into browser code.
2. Expose an authenticated configuration endpoint for Google Workspace disconnect.
3. Extend `tests/unit/app/test_google_workspace_auth_service.py` to cover:
   - service-level disconnect behavior
   - router contract and current-user scoping
   - truthful post-disconnect status expectations where applicable
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app/test_google_workspace_auth_service.py -x</automated>
  </verify>
  <done>Google Workspace can be cleanly disconnected through a backend-owned path with regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the configuration UI to the truthful Google Workspace lifecycle</name>
  <files>frontend/src/app/dashboard/configuration/page.tsx</files>
  <action>
1. Update the Google Workspace section in configuration so connected state can initiate a clean disconnect and then refresh the status contract.
2. Preserve the secure server-side token boundary introduced in Phase 54.
3. Keep reconnect behavior aligned with the truthful backend status returned after disconnect.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The configuration surface reflects the real disconnect/reconnect lifecycle for Google Workspace.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_integration_manager.py -x` passes
- `uv run pytest tests/unit/app/test_google_workspace_auth_service.py -x` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Disconnecting an OAuth provider clears stale lifecycle state
- Google Workspace supports a backend-owned disconnect path
- Reconnect starts from a truthful disconnected/reconnectable status
- Regression tests exist for both generic provider lifecycle cleanup and Google Workspace disconnect behavior
</success_criteria>

<output>
After completion, create `.planning/phases/55-integration-quality-load-testing/55-01-SUMMARY.md`
</output>
