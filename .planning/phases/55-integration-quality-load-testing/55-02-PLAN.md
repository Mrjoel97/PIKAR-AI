---
phase: 55-integration-quality-load-testing
plan: "02"
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/fast_api_app.py
  - frontend/src/hooks/useBackgroundStream.ts
  - tests/integration/test_sse_endpoint.py
  - frontend/src/__tests__/hooks/useBackgroundStream.test.ts
autonomous: true
requirements: [INTG-02, INTG-03]

must_haves:
  truths:
    - "SSE chat uses bearer-authenticated identity rather than trusting request-body user_id"
    - "Reusing the same session_id across different users does not leak chat context or ownership"
    - "Frontend background-stream side effects stay scoped to the producing session"
    - "Regression coverage proves there is no cross-session or cross-user SSE leakage in the targeted seams"
  artifacts:
    - path: "app/fast_api_app.py"
      provides: "Chat SSE identity/session isolation guardrails"
      contains: "effective_user_id"
    - path: "frontend/src/hooks/useBackgroundStream.ts"
      provides: "Frontend session-scoped background stream handling"
      contains: "sessionId"
    - path: "tests/integration/test_sse_endpoint.py"
      provides: "Backend SSE isolation regression coverage"
      contains: "session_id"
    - path: "frontend/src/__tests__/hooks/useBackgroundStream.test.ts"
      provides: "Frontend session isolation and side-effect scoping coverage"
      contains: "useBackgroundStream"
---

<objective>
Prove and harden SSE session isolation under concurrent multi-user usage.

Purpose: The backend and frontend already have promising session-scoping logic, but Phase 55 must make cross-session and cross-user leakage explicitly impossible in the covered seams rather than assumed.

Output: targeted backend/frontend guardrails if needed plus regression tests for SSE identity and session isolation.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-integration-quality-load-testing/55-CONTEXT.md
@.planning/phases/55-integration-quality-load-testing/55-RESEARCH.md
@.planning/phases/55-integration-quality-load-testing/55-VALIDATION.md

@app/fast_api_app.py
@frontend/src/hooks/useBackgroundStream.ts
@tests/integration/test_sse_endpoint.py
@frontend/src/__tests__/hooks/useBackgroundStream.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend regression coverage for SSE identity and session isolation</name>
  <files>tests/integration/test_sse_endpoint.py, app/fast_api_app.py</files>
  <action>
1. Extend SSE integration coverage to prove bearer-authenticated identity wins over request-body `user_id`.
2. Add a regression scenario for two users reusing the same `session_id` and verify that ownership and session state remain isolated.
3. If gaps appear while writing the tests, harden `app/fast_api_app.py` in the smallest possible way to enforce the intended boundary.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_sse_endpoint.py -x</automated>
  </verify>
  <done>Backend SSE identity and session boundaries are explicitly tested and hardened if necessary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add frontend regression coverage for background stream isolation</name>
  <files>frontend/src/__tests__/hooks/useBackgroundStream.test.ts, frontend/src/hooks/useBackgroundStream.ts</files>
  <action>
1. Extend `useBackgroundStream` tests to prove widget/activity side effects stay attached to the correct session under concurrent streaming scenarios.
2. Verify that non-visible/background sessions do not cause visible-session leakage.
3. If a gap appears, make the smallest session-scoping fix inside `useBackgroundStream.ts`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/hooks/useBackgroundStream.test.ts</automated>
  </verify>
  <done>Frontend background streaming is covered against cross-session leakage regressions.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_sse_endpoint.py -x` passes
- `cd frontend && npm run test -- src/__tests__/hooks/useBackgroundStream.test.ts` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Request-body user_id spoofing does not influence authenticated SSE ownership
- Same session_id across different users remains isolated
- Background stream side effects do not leak into another session
- Regression coverage protects both backend and frontend seams
</success_criteria>

<output>
After completion, create `.planning/phases/55-integration-quality-load-testing/55-02-SUMMARY.md`
</output>
