---
phase: 55-integration-quality-load-testing
plan: "03"
type: execute
wave: 3
depends_on: ["55-02"]
files_modified:
  - tests/load_test/README.md
  - tests/load_test/locustfile.py
  - tests/load_test/report_assertions.py
autonomous: true
requirements: [LOAD-01, LOAD-02, LOAD-03, LOAD-04]

must_haves:
  truths:
    - "The repo contains one clear on-demand load-test path for staging"
    - "Load-test outputs can be evaluated against explicit pass/fail thresholds including p95 latency"
    - "The harness and report contract account for SSE/chat concurrency and pool-health observations"
    - "A staging operator can run the suite and understand whether the app passed or failed"
  artifacts:
    - path: "tests/load_test/locustfile.py"
      provides: "Canonical authenticated load scenarios for Phase 55"
      contains: "chat_sse"
    - path: "tests/load_test/report_assertions.py"
      provides: "Machine-readable load-test threshold evaluator"
      contains: "max-p95-ms"
    - path: "tests/load_test/README.md"
      provides: "Documented local/staging execution and artifact interpretation"
      contains: "staging"
---

<objective>
Turn the existing load-test groundwork into a repeatable beta-readiness harness with explicit thresholds.

Purpose: Phase 55 must leave the repo with an on-demand suite that can be run against staging, emit report artifacts, and say clearly whether 100-user readiness passed or failed.

Output: canonical Locust usage, threshold assertions, and a staging-ready runbook.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-integration-quality-load-testing/55-CONTEXT.md
@.planning/phases/55-integration-quality-load-testing/55-RESEARCH.md
@.planning/phases/55-integration-quality-load-testing/55-VALIDATION.md

@tests/load_test/README.md
@tests/load_test/locustfile.py
@tests/load_test/load_test.py
@app/fast_api_app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the load harness and thresholds</name>
  <files>tests/load_test/locustfile.py, tests/load_test/report_assertions.py</files>
  <action>
1. Choose the canonical Phase 55 Locust entrypoint and align it with the realistic authenticated scenarios already in the repo.
2. Add a small report/assertion helper that evaluates generated load artifacts against explicit thresholds such as p95 chat latency, failure count, and other must-have checks.
3. Keep the assertions runnable in CI or by operators on demand.
  </action>
  <verify>
    <automated>uv run python tests/load_test/report_assertions.py --help</automated>
  </verify>
  <done>The load harness has an explicit machine-checkable pass/fail contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the staging-ready runbook and artifact interpretation</name>
  <files>tests/load_test/README.md</files>
  <action>
1. Update the README so local and staging execution point to the canonical harness and output paths.
2. Document required auth inputs, target URL setup, generated report files, and what constitutes pass/fail.
3. Keep the instructions aligned with the actual commands produced by the code in this plan.
  </action>
  <verify>
    <automated>uv run python tests/load_test/report_assertions.py --help</automated>
  </verify>
  <done>The load-testing runbook is accurate, staging-ready, and tied to a concrete pass/fail output.</done>
</task>

</tasks>

<verification>
- `uv run python tests/load_test/report_assertions.py --help` passes
- `uv run python tests/load_test/report_assertions.py --input tests/load_test/.results/sample.csv --max-p95-ms 3000` runs on a sample artifact
</verification>

<success_criteria>
- Load testing can be run on demand against staging
- The suite emits interpretable artifacts and threshold-based pass/fail output
- The documented path covers 100-user readiness concerns for latency, SSE stability, and pool-health observation
</success_criteria>

<output>
After completion, create `.planning/phases/55-integration-quality-load-testing/55-03-SUMMARY.md`
</output>
