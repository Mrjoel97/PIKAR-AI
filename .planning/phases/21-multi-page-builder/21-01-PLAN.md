---
phase: 21-multi-page-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260323100000_add_page_slug.sql
  - app/services/multi_page_service.py
  - tests/unit/app_builder/test_multi_page_service.py
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]

must_haves:
  truths:
    - "Baton loop iterates through sitemap pages sequentially, yielding page_started and page_complete events per page, and build_complete at end"
    - "Nav baton grows with each page — page 2 prompt contains page 1 slug/title, page 3 contains pages 1+2"
    - "Design system markdown is prepended to every page prompt"
    - "NavLinkRewriter rewrites /slug style hrefs to absolute Supabase Storage URLs"
    - "Nav injection re-downloads HTML from permanent Supabase URLs, rewrites links, re-uploads with upsert"
  artifacts:
    - path: "supabase/migrations/20260323100000_add_page_slug.sql"
      provides: "page_slug TEXT column on app_screens"
      contains: "ADD COLUMN IF NOT EXISTS page_slug"
    - path: "app/services/multi_page_service.py"
      provides: "build_all_pages async generator, NavLinkRewriter, inject_navigation_links"
      exports: ["build_all_pages", "inject_navigation_links"]
    - path: "tests/unit/app_builder/test_multi_page_service.py"
      provides: "Unit tests for baton loop, nav baton, nav rewriter, nav injection, design system injection"
      min_lines: 80
  key_links:
    - from: "app/services/multi_page_service.py"
      to: "app/services/stitch_mcp.py"
      via: "get_stitch_service().call_tool() — sequential await, never gather"
      pattern: "await service\\.call_tool"
    - from: "app/services/multi_page_service.py"
      to: "app/services/stitch_assets.py"
      via: "persist_screen_assets() called before yielding page_complete"
      pattern: "persist_screen_assets"
    - from: "app/services/multi_page_service.py"
      to: "app/services/iteration_service.py"
      via: "_get_locked_design_markdown() imported for design system fetch"
      pattern: "_get_locked_design_markdown"
---

<objective>
Create the multi-page generation engine: DB migration for page_slug, the baton-loop service that generates pages sequentially via Stitch MCP, and the navigation link injection post-processor.

Purpose: PAGE-01 (autonomous multi-page generation), PAGE-02 (auto-navigation linking), PAGE-03 (shared design system across pages) all depend on this service existing.
Output: `multi_page_service.py` with `build_all_pages` async generator + `inject_navigation_links` post-processor, migration SQL, and unit tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v2.md
@.planning/REQUIREMENTS-v2.md
@.planning/STATE.md
@.planning/phases/21-multi-page-builder/21-RESEARCH.md
@.planning/phases/20-iteration-loop/20-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/services/stitch_mcp.py:
```python
# get_stitch_service() returns StitchMCPService singleton
# StitchMCPService.call_tool(tool_name: str, args: dict) -> dict
# CRITICAL: asyncio.Lock serializes all calls — NEVER asyncio.gather
```

From app/services/stitch_assets.py:
```python
async def persist_screen_assets(
    stitch_response: dict,
    user_id: str,
    project_id: str,
    screen_id: str,
    variant_index: int,
) -> dict:  # returns {"html_url": str, "screenshot_url": str}
```

From app/services/iteration_service.py:
```python
async def _get_locked_design_markdown(
    project_id: str,
    user_id: str,
) -> str | None:
    """Return raw_markdown from design_systems if locked; None otherwise."""
```

From app/services/supabase.py:
```python
def get_service_client():  # returns Supabase client
```

From app/services/screen_generation_service.py (reference pattern):
```python
# Sequential await pattern for Stitch calls:
# for i in range(variant_count):
#     stitch_response = await service.call_tool("generate_screen_from_text", {...})
#     persisted = await persist_screen_assets(...)
#     yield {"step": "variant_generated", ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration + multi_page_service.py with baton loop and nav injection</name>
  <files>supabase/migrations/20260323100000_add_page_slug.sql, app/services/multi_page_service.py, tests/unit/app_builder/test_multi_page_service.py</files>
  <behavior>
    - Test 1 (test_baton_loop_yields_correct_events): Mock call_tool + persist_screen_assets + supabase; feed 3-page sitemap to build_all_pages; collect events; assert 3 page_started + 3 page_complete + 1 build_complete events in correct order with correct page_slug values
    - Test 2 (test_nav_baton_accumulates): Call _build_nav_baton with 0 screens (empty string), then 1 screen (contains page 1 slug), then 2 screens (contains both slugs); verify growing nav context
    - Test 3 (test_design_system_injected_in_prompt): Call _build_page_prompt with design_markdown="DESIGN:red"; verify the returned prompt string starts with "DESIGN SYSTEM:\nDESIGN:red"
    - Test 4 (test_nav_link_rewriter): Create NavLinkRewriter with slug_to_url map; feed HTML containing `<a href="/about">About</a>`; assert output contains the absolute Supabase URL instead of /about
    - Test 5 (test_nav_injection_uploads): Mock httpx.AsyncClient.get to return HTML with nav links; mock supabase storage upload; call inject_navigation_links; assert upload called with rewritten HTML and upsert=true
    - Test 6 (test_design_system_injected_in_every_page_prompt): In the baton loop test, capture prompts passed to call_tool; assert every prompt contains the design markdown string
  </behavior>
  <action>
    1. Create migration `supabase/migrations/20260323100000_add_page_slug.sql`:
       ```sql
       ALTER TABLE app_screens ADD COLUMN IF NOT EXISTS page_slug TEXT;
       ```

    2. Write test file `tests/unit/app_builder/test_multi_page_service.py` FIRST (RED phase):
       - Import from `app.services.multi_page_service` (does not exist yet — confirms RED)
       - 6 tests per behavior spec above
       - Use `unittest.mock.AsyncMock` for `call_tool`, `persist_screen_assets`
       - Use `unittest.mock.MagicMock` for supabase chain `.table().insert().execute()`
       - Patch targets: `app.services.multi_page_service.get_stitch_service`, `app.services.multi_page_service.persist_screen_assets`, `app.services.multi_page_service.get_service_client`
       - For nav injection test: patch `httpx.AsyncClient` and supabase storage

    3. Create `app/services/multi_page_service.py` (GREEN phase) with:
       - `build_all_pages(project_id, user_id, sitemap, design_markdown, stitch_project_id) -> AsyncIterator[dict]`
         - Sequential `for` loop over sitemap entries
         - Yields `{"step": "page_started", "page_index": i, "page_slug": slug, "total_pages": n}`
         - Calls `_build_nav_baton(screens_built)` to get growing nav context
         - Calls `_build_page_prompt(design_markdown, page_title, page_slug, sections, nav_context)` to assemble prompt
         - `await service.call_tool("generate_screen_from_text", {...})` — NEVER gather
         - Inserts `app_screens` row with `page_slug` column
         - `await persist_screen_assets(...)` BEFORE yielding page_complete
         - Inserts `screen_variants` row (single variant, variant_index=0, is_selected=True, iteration=1)
         - Yields `{"step": "page_complete", "page_index": i, "page_slug": slug, "screen_id": ..., "html_url": ..., "screenshot_url": ...}`
         - After loop: yields `{"step": "build_complete", "total_pages": n, "screens": screens_built}`
       - `_build_nav_baton(screens_built: list[dict]) -> str` — returns empty string for first page, growing "Navigation structure" block for subsequent pages
       - `_build_page_prompt(design_markdown, page_title, page_slug, sections, nav_context) -> str` — assembles DESIGN SYSTEM + page spec + nav context
       - `NavLinkRewriter(HTMLParser)` — rewrites `<a href="/slug">` to absolute Supabase URLs using slug_to_url map; handles both `/slug` and `slug` href formats; passes through non-matching hrefs unchanged. Must also handle `handle_decl`, `handle_comment`, `handle_entityref`, `handle_charref` to preserve full HTML fidelity.
       - `inject_navigation_links(screens, user_id, project_id) -> None` — for each screen, downloads HTML from permanent `html_url` via httpx, runs NavLinkRewriter, re-uploads to same Supabase Storage path with `upsert="true"`. Non-fatal: logs warning on failure, does not raise.

    Follow the exact patterns from RESEARCH.md code examples. Import `_get_locked_design_markdown` from `iteration_service.py` — do NOT duplicate it. Use `from uuid import uuid4` for IDs.
  </action>
  <verify>
    <automated>cd /c/Users/expert/Documents/PKA/Pikar-Ai && /c/Users/expert/AppData/Roaming/Python/Python313/Scripts/uv.exe run pytest tests/unit/app_builder/test_multi_page_service.py -x -v</automated>
  </verify>
  <done>All 6 tests pass. Migration SQL exists. multi_page_service.py exports build_all_pages and inject_navigation_links. NavLinkRewriter correctly rewrites /slug hrefs to absolute URLs. Baton loop yields correct event sequence. Design markdown appears in every page prompt.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/app_builder/test_multi_page_service.py -x -v` — all 6+ tests pass
- `uv run ruff check app/services/multi_page_service.py` — no lint errors
- Migration file exists at `supabase/migrations/20260323100000_add_page_slug.sql`
</verification>

<success_criteria>
- build_all_pages generates N page_started + N page_complete + 1 build_complete events for N-page sitemap
- Nav baton grows with each page (page 2 prompt references page 1, etc.)
- Design system markdown prepended to every page prompt
- NavLinkRewriter rewrites /slug hrefs to absolute Supabase Storage URLs
- inject_navigation_links downloads, rewrites, re-uploads HTML for each page
- All tests green, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/21-multi-page-builder/21-01-SUMMARY.md`
</output>
