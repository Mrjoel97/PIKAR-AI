---
phase: 38-solopreneur-unlock-tool-honesty
plan: 03
type: execute
wave: 2
depends_on:
  - 38-02
files_modified:
  - app/personas/policy_registry.py
  - app/personas/behavioral_instructions.py
  - app/routers/org.py
  - frontend/src/components/personas/personaShellConfig.ts
  - frontend/src/components/dashboard/OnboardingChecklist.tsx
  - frontend/src/components/org-chart/AgentInspector.tsx
  - tests/unit/test_persona_behavioral_instructions.py
  - tests/unit/test_persona_policy_registry.py
autonomous: true
requirements:
  - SOLO-04
  - TOOL-08

must_haves:
  truths:
    - "Solopreneur behavioral instructions use 'capable business operator' tone, not 'scrappy saver'"
    - "Solopreneur planning horizon is 30 days, not 7-14 days"
    - "Solopreneur core objectives reflect 'run entire business confidently' vision"
    - "Solopreneur KPIs include revenue trend, active workflows, and compliance score"
    - "Org chart tool list shows [ACTION] or [GUIDE] badge next to each tool name"
    - "Tools that call skills_registry.use_skill() are classified as 'knowledge' (GUIDE badge)"
    - "All other tools are classified as 'action' (ACTION badge)"
  artifacts:
    - path: "app/personas/policy_registry.py"
      provides: "Updated solopreneur PersonaPolicy with capable operator fields"
      contains: "run entire business"
    - path: "app/personas/behavioral_instructions.py"
      provides: "11 updated solopreneur behavioral instruction entries"
      contains: "capable.*operator|full.*business|comprehensive"
    - path: "app/routers/org.py"
      provides: "OrgNode with tool_kinds field for action/knowledge classification"
      contains: "tool_kinds"
    - path: "frontend/src/components/org-chart/AgentInspector.tsx"
      provides: "ACTION/GUIDE badge rendering in tool list"
      contains: "tool_kinds"
    - path: "frontend/src/components/personas/personaShellConfig.ts"
      provides: "Updated solopreneur KPI labels and description"
      contains: "Revenue Trend|Active Workflows|Compliance Score"
    - path: "frontend/src/components/dashboard/OnboardingChecklist.tsx"
      provides: "Updated solopreneur checklist items showcasing full capability set"
  key_links:
    - from: "app/personas/policy_registry.py"
      to: "app/personas/behavioral_instructions.py"
      via: "build_persona_policy_block() combines policy with instructions"
      pattern: "build_persona_policy_block"
    - from: "app/routers/org.py"
      to: "frontend/src/components/org-chart/AgentInspector.tsx"
      via: "GET /org-chart returns OrgNode with tool_kinds field"
      pattern: "tool_kinds"
---

<objective>
Update solopreneur behavioral instructions and policy to reflect a "capable business operator" persona (not limited/scrappy), update KPIs and onboarding checklist, and add [ACTION]/[GUIDE] badges to the org chart tool display.

Purpose: The solopreneur persona currently has instructions tuned for a limited tier ("save time, ship fast, protect cash"). With full feature access (Plan 01), the behavioral instructions must match — comprehensive analysis, 30-day planning horizon, and business health KPIs. The org chart badges (Plan 02 dependency for renamed tools) clearly communicate which tools perform real actions vs. provide knowledge.
Output: Updated persona policy, 11 behavioral instruction entries, KPI labels, onboarding checklist, and org chart with tool kind badges.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-solopreneur-unlock-tool-honesty/38-CONTEXT.md
@.planning/phases/38-solopreneur-unlock-tool-honesty/38-RESEARCH.md
@.planning/phases/38-solopreneur-unlock-tool-honesty/38-02-SUMMARY.md

<interfaces>
<!-- Key types from codebase. Executor needs these to implement correctly. -->

From app/personas/policy_registry.py:
```python
@dataclass
class PersonaPolicy:
    key: str
    label: str
    summary: str
    core_objectives: tuple[str, ...]
    default_kpis: tuple[str, ...]
    planning_horizon: str
    response_style: str
    output_contract: str
    approval_posture: str
    interaction_style: str
    risk_tolerance: str

_PERSONA_POLICIES: dict[str, PersonaPolicy] = {
    "solopreneur": PersonaPolicy(
        key="solopreneur",
        label="Solopreneur",
        summary="...",  # Currently "scrappy saver" tone
        core_objectives=("...",),  # Currently "save time, ship fast, protect cash"
        default_kpis=("cash collected", "weekly pipeline", "content consistency"),
        planning_horizon="Bias to the next 7-14 days...",
        response_style="Be ultra-concise...",
        # ... other fields
    ),
    ...
}
```

From app/personas/behavioral_instructions.py:
```python
_BEHAVIORAL_INSTRUCTIONS: dict[tuple[str, str], str] = {
    # 11 entries with key ("AgentName", "solopreneur"):
    ("FinancialAnalysisAgent", "solopreneur"): "...",
    ("ContentCreationAgent", "solopreneur"): "...",
    ("StrategicPlanningAgent", "solopreneur"): "...",
    ("SalesIntelligenceAgent", "solopreneur"): "...",
    ("MarketingCampaignAgent", "solopreneur"): "...",
    ("OperationsAgent", "solopreneur"): "...",
    ("HRRecruitmentAgent", "solopreneur"): "...",
    ("ComplianceRiskAgent", "solopreneur"): "...",
    ("CustomerSupportAgent", "solopreneur"): "...",
    ("DataAnalyticsAgent", "solopreneur"): "...",
    ("DataReportingAgent", "solopreneur"): "...",
}
```

From app/routers/org.py:
```python
class OrgNode(BaseModel):
    id: str
    type: str  # 'user', 'agent'
    label: str
    role: str | None = None
    reports_to: str | None = None
    status: str = "active"
    tools: list[str] = []       # Keep as-is
    tool_count: int = 0
    capabilities: str = ""
    model: str = ""
    last_activity_at: str | None = None
    active_workflows: int = 0
    recent_decisions: int = 0

def _get_tool_list(agent) -> list[str]:
    """Return a sorted list of unique tool names for the agent."""
```

From frontend/src/components/org-chart/AgentInspector.tsx:
```typescript
export interface OrgNodeData {
    id: string;
    type: 'user' | 'agent';
    label: string;
    role?: string;
    tools: string[];
    tool_count: number;
    capabilities: string;
    model: string;
    last_activity_at?: string | null;
    active_workflows: number;
    recent_decisions: number;
}
```

From frontend/src/components/personas/personaShellConfig.ts:
```typescript
// solopreneur entry has: tagline, description, kpiLabels array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update solopreneur persona policy, behavioral instructions, KPIs, and onboarding</name>
  <files>app/personas/policy_registry.py, app/personas/behavioral_instructions.py, frontend/src/components/personas/personaShellConfig.ts, frontend/src/components/dashboard/OnboardingChecklist.tsx, tests/unit/test_persona_behavioral_instructions.py, tests/unit/test_persona_policy_registry.py</files>
  <action>
    1. Update `app/personas/policy_registry.py` — rewrite the solopreneur PersonaPolicy entry:
       - summary: "Full-featured single-user business operator with access to every non-team capability."
       - core_objectives: ("Run entire business confidently with full AI support", "Automate everything possible to maximize personal leverage", "Maintain full business visibility across revenue, operations, and compliance")
       - default_kpis: ("revenue trend", "active workflows", "compliance score")
       - planning_horizon: "Bias to the next 30 days with weekly milestones."
       - response_style: "Be action-first but comprehensive. Provide complete analysis with clear next steps, not just quick tips."
       - output_contract: Update to reflect comprehensive output expectations (not minimalist)
       - approval_posture: Use Claude's discretion — balance minimal gates for routine work with appropriate escalation for financial transactions and compliance decisions. Suggested: "Auto-approve routine operations. Escalate financial commitments over $500 and compliance-sensitive decisions."
       - interaction_style and risk_tolerance: Update to match "capable operator" tone

    2. Update `app/personas/behavioral_instructions.py` — rewrite ALL 11 solopreneur entries with "capable business operator" tone. For each agent entry:
       - Shift from "save money, be frugal" language to "run business confidently, maximize leverage"
       - Extend planning references from 7-14 days to 30 days
       - Make output expectations comprehensive, not minimalist
       - Keep the domain-specific focus of each agent (financial agent talks revenue, content agent talks publishing, etc.)
       - Use the guidance from CONTEXT.md: solopreneur is "not a limited persona — it's a single-user category that should have full access to real value features"
       - The 11 entries are keyed by: FinancialAnalysisAgent, ContentCreationAgent, StrategicPlanningAgent, SalesIntelligenceAgent, MarketingCampaignAgent, OperationsAgent, HRRecruitmentAgent, ComplianceRiskAgent, CustomerSupportAgent, DataAnalyticsAgent, DataReportingAgent

    3. Update `frontend/src/components/personas/personaShellConfig.ts`:
       - Update solopreneur `tagline` to reflect "full-featured business operator" (not limited tier)
       - Update solopreneur `description` to match the new policy summary
       - Update solopreneur `kpiLabels` to: ["Revenue Trend", "Active Workflows", "Compliance Score"]

    4. Update `frontend/src/components/dashboard/OnboardingChecklist.tsx`:
       - Replace the 5 solopreneur checklist items with items showcasing the full capability set:
         a. "Run your first workflow" — Automate a repetitive business process
         b. "Set up your sales pipeline" — Track deals and manage your funnel
         c. "Do a brain dump" — Get all your ideas organized into action plans
         d. "Run a compliance check" — Ensure your business meets key requirements
         e. "Create a financial forecast" — Project your revenue and plan ahead

    5. Update existing tests:
       - `tests/unit/test_persona_behavioral_instructions.py`: Update assertions that check for specific solopreneur wording. The tests likely assert words like "cash", "frugal", "7-14 days" — update to match new "capable operator" language (e.g., "revenue", "comprehensive", "30 days"). Keep the structural assertions (all 11 entries exist, correct agent keys, no empty strings).
       - `tests/unit/test_persona_policy_registry.py`: Update assertions for solopreneur default_kpis (now "revenue trend", "active workflows", "compliance score"), planning_horizon (now "30 days"), and core_objectives. Keep structural assertions for all personas.

    PITFALL WARNING: Tests at `tests/unit/test_persona_behavioral_instructions.py` have exact word checks per persona. The test `test_solopreneur_financial_agent_contains_cash_flow()` or similar will fail if new instructions don't include the expected keywords. Read the test file FIRST to identify all solopreneur-specific assertions before rewriting instructions.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_persona_behavioral_instructions.py tests/unit/test_persona_policy_registry.py -x -q</automated>
  </verify>
  <done>Solopreneur PersonaPolicy reflects "capable operator" with 30-day horizon, comprehensive response style, and new KPIs. All 11 behavioral instruction entries rewritten. Frontend KPI labels and onboarding checklist updated. All persona tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add tool kind classification to org chart backend and frontend badges</name>
  <files>app/routers/org.py, frontend/src/components/org-chart/AgentInspector.tsx, tests/unit/test_tool_honesty.py</files>
  <action>
    1. In `app/routers/org.py`:
       a. Add a `tool_kinds` field to `OrgNode` model: `tool_kinds: dict[str, str] = {}` — this is ADDITIVE (keep existing `tools: list[str]` unchanged to avoid breaking the API).
       b. Add a module-level constant `_KNOWLEDGE_TOOLS` — a set of tool names that call `skills_registry.use_skill()`. Include ALL knowledge tools, not just the 7 renamed ones:
          ```python
          _KNOWLEDGE_TOOLS = {
              "hubspot_setup_guide", "security_checklist", "container_deployment_guide",
              "cloud_architecture_guide", "seo_fundamentals_guide", "product_roadmap_guide",
              "rag_architecture_guide", "use_skill", "list_available_skills",
              "generate_react_component", "build_portfolio", "generate_remotion_video",
          }
          ```
       c. Create a helper `_build_tool_kinds(tools: list[str]) -> dict[str, str]` that returns `{tool_name: "knowledge" if tool_name in _KNOWLEDGE_TOOLS else "action" for tool_name in tools}`.
       d. In the endpoint function `get_org_chart`, after getting `tools` from `meta.get("tools", [])`, also compute `tool_kinds = _build_tool_kinds(tools)` and pass it to `OrgNode(tool_kinds=tool_kinds, ...)`. Do this for BOTH the executive node and all agent nodes.

    2. In `frontend/src/components/org-chart/AgentInspector.tsx`:
       a. Add `tool_kinds?: Record<string, string>` to the `OrgNodeData` interface (optional field for backward compat).
       b. In the tools list rendering section, add a badge after each tool name:
          - If `agent.tool_kinds?.[tool]` is `"action"`: render a small blue badge with text "ACTION"
          - If `agent.tool_kinds?.[tool]` is `"knowledge"`: render a small amber badge with text "GUIDE"
          - If `tool_kinds` is undefined/missing for a tool: render no badge (graceful degradation)
       c. Badge styling: small rounded pill, uppercase text, 9px font, tracking-wider. Use Tailwind classes:
          - ACTION: `bg-blue-50 text-blue-600 dark:bg-blue-900/30 dark:text-blue-400`
          - GUIDE: `bg-amber-50 text-amber-600 dark:bg-amber-900/30 dark:text-amber-400`

    3. Add a test to `tests/unit/test_tool_honesty.py` (created in Plan 02):
       - `test_org_chart_tool_kinds`: Import `_KNOWLEDGE_TOOLS` and `_build_tool_kinds` from `app.routers.org`. Verify that `_build_tool_kinds(["hubspot_setup_guide", "send_email"])` returns `{"hubspot_setup_guide": "knowledge", "send_email": "action"}`.

    IMPORTANT: Do NOT change `tools: list[str]` to `tools: list[dict]` in OrgNode — that would be a BREAKING change. The `tool_kinds` field is ADDITIVE alongside the existing `tools` field.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tool_honesty.py::test_org_chart_tool_kinds -x -q</automated>
  </verify>
  <done>OrgNode has tool_kinds field classifying each tool as "action" or "knowledge". Frontend AgentInspector renders [ACTION]/[GUIDE] badges next to each tool name. Backend API is backward-compatible (tools field unchanged). Test verifies classification logic.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/unit/test_persona_behavioral_instructions.py tests/unit/test_persona_policy_registry.py tests/unit/test_tool_honesty.py -x -q` — all tests green
2. `grep "30 days" app/personas/policy_registry.py` — matches solopreneur planning_horizon
3. `grep -c "solopreneur" app/personas/behavioral_instructions.py` — should return 11 (one per agent)
4. `grep "tool_kinds" app/routers/org.py` — field exists in OrgNode model
5. `grep "tool_kinds" frontend/src/components/org-chart/AgentInspector.tsx` — badge rendering uses tool_kinds
6. `uv run pytest tests/ -x -q` — full suite passes
</verification>

<success_criteria>
- Solopreneur PersonaPolicy uses "capable business operator" tone with 30-day horizon
- All 11 solopreneur behavioral instructions rewritten with comprehensive, confident language
- Frontend KPI labels show "Revenue Trend", "Active Workflows", "Compliance Score"
- Onboarding checklist showcases workflows, sales pipeline, compliance, financial forecast
- Org chart endpoint returns tool_kinds: {"tool_name": "action"|"knowledge"} for every tool
- Frontend renders [ACTION] and [GUIDE] badges in the tool list
- All tests pass including updated persona tests and new org chart classification test
</success_criteria>

<output>
After completion, create `.planning/phases/38-solopreneur-unlock-tool-honesty/38-03-SUMMARY.md`
</output>
