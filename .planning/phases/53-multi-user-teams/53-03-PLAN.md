---
phase: 53-multi-user-teams
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - frontend/src/app/dashboard/settings/team/page.tsx
  - frontend/src/components/team/InviteLinkGenerator.tsx
  - frontend/src/components/team/PendingInvitesList.tsx
  - frontend/src/components/layout/sidebarNav.ts
autonomous: true
requirements: [TEAM-01, TEAM-03]

must_haves:
  truths:
    - "Admin can type an email, select a role (Admin/Member), and send an invitation"
    - "Admin sees a list of pending invites with email, role, expiry countdown, resend and revoke buttons"
    - "Resending an invite shows success toast and refreshes the list"
    - "Revoking an invite removes it from the pending list"
    - "Role selector shows Admin and Member only (not Editor/Viewer)"
  artifacts:
    - path: "frontend/src/app/dashboard/settings/team/page.tsx"
      provides: "Team settings page at /dashboard/settings/team with invite form, pending invites, and member list"
      contains: "TeamSettingsPage"
    - path: "frontend/src/components/team/PendingInvitesList.tsx"
      provides: "Pending invites section with resend/revoke actions"
      contains: "PendingInvitesList"
    - path: "frontend/src/components/team/InviteLinkGenerator.tsx"
      provides: "Updated invite form with email input and Admin/Member role selector"
      contains: "invited_email"
  key_links:
    - from: "frontend/src/components/team/InviteLinkGenerator.tsx"
      to: "frontend/src/app/api/teams/invite/route.ts"
      via: "fetch POST /api/teams/invite with email and role"
      pattern: "fetch.*api/teams/invite"
    - from: "frontend/src/components/team/PendingInvitesList.tsx"
      to: "app/routers/teams.py"
      via: "fetchWithAuth GET/DELETE/POST /teams/invites endpoints"
      pattern: "fetchWithAuth.*teams/invites"
---

<objective>
Create the team settings page with email-based invite form and pending invites management UI.

Purpose: Per CONTEXT.md, the team management UI lives at `/dashboard/settings/team`. This plan replaces the link-only invite flow with an email-based invite form (type email + select role + send) and adds a pending invites section with resend/revoke, wiring to the backend endpoints from Plan 53-01.

Output: Team settings page, updated invite component with email field, pending invites list component.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-multi-user-teams/53-CONTEXT.md
@.planning/phases/53-multi-user-teams/53-01-SUMMARY.md

@frontend/src/components/team/InviteLinkGenerator.tsx
@frontend/src/components/team/TeamMemberList.tsx
@frontend/src/app/dashboard/team/page.tsx
@frontend/src/contexts/WorkspaceContext.tsx
@frontend/src/components/billing/SubscriptionBadge.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/contexts/WorkspaceContext.tsx:
```typescript
export function useWorkspace(): WorkspaceState;
// WorkspaceState includes: workspaceId, workspaceName, role, isOwner, canManageTeam, refresh
```

From frontend/src/services/api.ts:
```typescript
export async function fetchWithAuth(endpoint: string, options?: FetchOptions): Promise<Response>;
```

Backend endpoints (from Plan 53-01):
- POST /api/teams/invite — { email, role, workspaceId } — sends email invitation
- GET /teams/invites — returns list of pending invites (via fetchWithAuth)
- DELETE /teams/invites/{id} — revokes an invite
- POST /teams/invites/{id}/resend — regenerates token and re-sends email

Pending invite shape (from backend):
```typescript
interface PendingInvite {
  id: string;
  invited_email: string | null;
  role: string;
  created_by: string;
  inviter_name?: string;
  expires_at: string;
  created_at: string;
  is_active: boolean;
}
```

CONTEXT.md decisions:
- Role selector: Admin and Member only (maps to "admin" and "editor" in DB schema)
- Role badges: same styling as SubscriptionBadge (visual consistency)
- Admin badge = blue, Member badge = gray
- Owner has permanent "Owner" badge, cannot be demoted
- Invite form: email input + role selector + "Send Invite" button
- Pending invites: below member list, shows email, role, sent date, expiry countdown, Resend + Revoke buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild InviteLinkGenerator as email invite form and create PendingInvitesList</name>
  <files>frontend/src/components/team/InviteLinkGenerator.tsx, frontend/src/components/team/PendingInvitesList.tsx</files>
  <action>
1. Rewrite `frontend/src/components/team/InviteLinkGenerator.tsx`:
   - Change the component to be an email-based invite form (not link generator)
   - Props: `{ workspaceId: string; onInviteSent?: () => void }` (callback to refresh pending list)
   - State: email (string), role ("admin" | "member"), loading, error, success
   - UI structure:
     - Section title: "Invite Team Member"
     - Inline form with:
       a. Email input (type="email", placeholder="colleague@company.com", required)
       b. Role selector: two buttons "Admin" and "Member" (toggle, same style as current role selector but with only these two options)
       c. "Send Invite" button (indigo-600 primary button, loading spinner when sending)
     - Role description text below selector:
       - Admin: "Full access -- can manage team members, billing, and admin settings."
       - Member: "Can create and edit initiatives, workflows, and content."
   - On submit:
     a. Validate email format (basic regex)
     b. POST to `/api/teams/invite` with `{ email, role, workspaceId }`
     c. On success: show success toast via sonner ("Invitation sent to {email}"), clear form, call onInviteSent()
     d. On error: show error in component (not toast -- inline error message below button)
   - No more link display -- the invite is sent directly via email

2. Create `frontend/src/components/team/PendingInvitesList.tsx`:
   - Props: `{ workspaceId: string; refreshKey?: number }` (refreshKey increments to trigger refetch)
   - Fetches pending invites from `GET /teams/invites` via fetchWithAuth on mount and when refreshKey changes
   - Shows section title: "Pending Invitations"
   - For each invite, render a row/card with:
     a. Invited email (or "Link invite" if no email)
     b. Role badge: "Admin" (blue) or "Member" (gray) — map "editor" -> "Member", "admin" -> "Admin", "viewer" -> "Viewer"
     c. Sent date: "Sent {date}" using toLocaleDateString
     d. Expiry countdown: calculate remaining time from expires_at, display "Expires in X days" or "Expires in X hours" or "Expired" (red text)
     e. Action buttons: "Resend" and "Revoke"
   - Resend action:
     a. POST `/teams/invites/{id}/resend` via fetchWithAuth
     b. Then POST `/api/teams/invite` with the invite's email and role to re-send the email
     c. Toast success: "Invitation resent to {email}"
     d. Refetch the list
   - Revoke action:
     a. DELETE `/teams/invites/{id}` via fetchWithAuth
     b. Toast success: "Invitation revoked"
     c. Refetch the list
   - Empty state: "No pending invitations" (centered, muted text)
   - Loading state: shimmer rows (2-3 rows of h-12 rounded bg-slate-100 animate-pulse)
   - Style: rounded-2xl border border-slate-100 bg-white p-6 shadow-sm (matching existing team page sections)
  </action>
  <verify>
    <automated>cd /c/Users/expert/documents/pka/pikar-ai/frontend && npx tsc --noEmit src/components/team/InviteLinkGenerator.tsx src/components/team/PendingInvitesList.tsx 2>&1 | head -20</automated>
  </verify>
  <done>InviteLinkGenerator sends email invitations with Admin/Member role selector. PendingInvitesList shows pending invites with expiry countdown, resend, and revoke actions. Both use sonner toasts for feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Create team settings page and add sidebar nav entry</name>
  <files>frontend/src/app/dashboard/settings/team/page.tsx, frontend/src/components/layout/sidebarNav.ts</files>
  <action>
1. Create `frontend/src/app/dashboard/settings/team/page.tsx`:
   - Per CONTEXT.md: team management page location is `/dashboard/settings/team`
   - Import: PremiumShell, DashboardErrorBoundary, PermissionGate, TeamMemberList, InviteLinkGenerator, PendingInvitesList, useWorkspace
   - Page structure (wrapped in DashboardErrorBoundary + PremiumShell):
     a. Page header: "Team Settings" heading + workspace name subtitle
     b. Admin-only section (wrapped in PermissionGate require="manage-team" fallback="hide"):
        - InviteLinkGenerator component with onInviteSent callback that increments refreshKey
        - PendingInvitesList component with refreshKey prop
     c. Member list section (visible to all roles):
        - TeamMemberList component (already handles role dropdowns and remove)
     d. Role reference card (reuse the RoleInfoCard pattern from the existing team page, but update to show only Admin and Member roles)
   - State: refreshKey counter (incremented when invite is sent, to trigger PendingInvitesList refetch)
   - Layout: max-w-3xl mx-auto px-4 py-8 sm:px-6 (same as existing team page)
   - For members: they only see the member list (PermissionGate hides invite form and pending invites)

2. Update `frontend/src/components/layout/sidebarNav.ts`:
   - Add a "Team Settings" nav item with:
     - label: "Team Settings"
     - href: "/dashboard/settings/team"
     - icon: Users (from lucide-react — already imported in other components)
     - adminOnly: true (per CONTEXT.md: team settings is admin-only)
   - Insert it near the Configuration item (bottom of nav)
   - Import Users from lucide-react

Note: The existing `/dashboard/team` page continues to work as the team dashboard/analytics page. The new `/dashboard/settings/team` is the team management page (invites, member management). They serve different purposes and can coexist.
  </action>
  <verify>
    <automated>cd /c/Users/expert/documents/pka/pikar-ai/frontend && npx tsc --noEmit src/app/dashboard/settings/team/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Team settings page at /dashboard/settings/team with email invite form, pending invites, and member list. Sidebar shows "Team Settings" for admins only. Members see only the member list when accessing the page.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- Team settings page renders at /dashboard/settings/team
- Admin sees: invite form + pending invites + member list + role reference
- Member sees: member list only (invite form and pending invites hidden by PermissionGate)
- Sidebar shows "Team Settings" for admins and solo users, hidden for members
- Sending invite shows success toast and refreshes pending invites list
- Resend and Revoke actions work with toast feedback
</verification>

<success_criteria>
- Admin can type email, select Admin/Member role, and send invitation from team settings page
- Pending invites section shows email, role badge, expiry countdown, resend/revoke buttons
- Role selector shows only Admin and Member (not Editor/Viewer)
- Role badges use consistent styling (Admin=blue, Member=gray)
- Team Settings nav item visible to admins, hidden from members
- Page location is /dashboard/settings/team per CONTEXT.md decision
</success_criteria>

<output>
After completion, create `.planning/phases/53-multi-user-teams/53-03-SUMMARY.md`
</output>
