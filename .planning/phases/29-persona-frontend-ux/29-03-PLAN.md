---
phase: 29-persona-frontend-ux
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - frontend/src/app/(personas)/solopreneur/page.tsx
  - frontend/src/app/(personas)/startup/page.tsx
  - frontend/src/app/(personas)/sme/page.tsx
  - frontend/src/app/(personas)/enterprise/page.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/onboarding/processing/page.tsx
  - frontend/src/components/dashboard/PersonaDashboardLayout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Each persona page wraps content in its respective shell component, rendering the persona-specific header"
    - "Dashboard page renders persona-differentiated default widget layout — different sections per persona type"
    - "Onboarding processing page redirects to the persona-specific route (e.g., /solopreneur) not /dashboard/command-center"
    - "The dashboard/page.tsx redirects to the persona-specific route when persona is known"
    - "Each persona page passes persona-specific title, description, and content to PersonaDashboardLayout"
  artifacts:
    - path: "frontend/src/app/(personas)/solopreneur/page.tsx"
      provides: "Solopreneur page using SolopreneurShell with differentiated content"
      min_lines: 15
    - path: "frontend/src/app/(personas)/enterprise/page.tsx"
      provides: "Enterprise page using EnterpriseShell with differentiated content"
      min_lines: 15
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Dashboard page with persona redirect and differentiated widget layout"
      contains: "router.replace"
    - path: "frontend/src/app/onboarding/processing/page.tsx"
      provides: "Onboarding processing with persona-specific redirect"
      contains: "persona"
  key_links:
    - from: "frontend/src/app/(personas)/solopreneur/page.tsx"
      to: "frontend/src/components/personas/SolopreneurShell.tsx"
      via: "import SolopreneurShell"
      pattern: "SolopreneurShell"
    - from: "frontend/src/app/onboarding/processing/page.tsx"
      to: "frontend/src/app/(personas)"
      via: "router.push persona route"
      pattern: "router\\.push.*persona"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "frontend/src/contexts/PersonaContext.tsx"
      via: "usePersona to determine redirect target"
      pattern: "usePersona"
---

<objective>
Wire the shell components into persona pages, make the dashboard page render differently per persona, and ensure onboarding correctly routes to the persona-specific dashboard.

Purpose: Success criteria #2 (tailored default widget layout), #4 (dashboard renders differently per persona), and #5 (onboarding sets persona and first dashboard matches) all require this wiring.
Output: Persona pages use shells, dashboard renders persona-aware content, onboarding redirects to persona route.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/app/(personas)/solopreneur/page.tsx
@frontend/src/app/(personas)/enterprise/page.tsx
@frontend/src/app/(personas)/startup/page.tsx
@frontend/src/app/(personas)/sme/page.tsx
@frontend/src/app/dashboard/page.tsx
@frontend/src/app/onboarding/processing/page.tsx
@frontend/src/components/dashboard/PersonaDashboardLayout.tsx
@frontend/src/components/dashboard/CommandCenter.tsx

<interfaces>
<!-- From Plan 29-01 (will exist when this runs): -->
```typescript
// frontend/src/components/layout/personaNavConfig.ts
export function getPersonaNavItems(persona: 'solopreneur' | 'startup' | 'sme' | 'enterprise' | null): NavItem[];
```

<!-- From Plan 29-02 (will exist when this runs): -->
```typescript
// frontend/src/components/personas/SolopreneurShell.tsx
export function SolopreneurShell({ children }: { children?: React.ReactNode }): JSX.Element;

// frontend/src/components/personas/StartupShell.tsx
export function StartupShell({ children }: { children?: React.ReactNode }): JSX.Element;

// frontend/src/components/personas/SmeShell.tsx
export function SmeShell({ children }: { children?: React.ReactNode }): JSX.Element;

// frontend/src/components/personas/EnterpriseShell.tsx
export function EnterpriseShell({ children }: { children?: React.ReactNode }): JSX.Element;

// frontend/src/components/personas/personaShellConfig.ts
export const PERSONA_SHELL_CONFIG: Record<PersonaKey, PersonaShellConfig>;
```

<!-- Existing interfaces used: -->
```typescript
// frontend/src/components/dashboard/PersonaDashboardLayout.tsx
interface PersonaDashboardLayoutProps {
  persona: PersonaType;
  title: string;
  description: string;
  children?: React.ReactNode;
  agentName?: string;
  showChat?: boolean;
  mobileLayout?: 'tabs' | 'fab';
}

// frontend/src/contexts/PersonaContext.tsx
export function usePersona(): {
  persona: 'solopreneur' | 'startup' | 'sme' | 'enterprise' | null;
  userId: string | null;
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update persona pages to use shell components and provide differentiated content</name>
  <files>frontend/src/app/(personas)/solopreneur/page.tsx, frontend/src/app/(personas)/startup/page.tsx, frontend/src/app/(personas)/sme/page.tsx, frontend/src/app/(personas)/enterprise/page.tsx, frontend/src/components/dashboard/PersonaDashboardLayout.tsx</files>
  <action>
**Update each persona page** to wrap `PersonaDashboardLayout` inside the persona's shell component. The shell provides the persona-specific header/theming on top, and `PersonaDashboardLayout` provides the chat interface + workspace below.

**SolopreneurPage (`(personas)/solopreneur/page.tsx`):**
```tsx
'use client';

import PersonaDashboardLayout from '@/components/dashboard/PersonaDashboardLayout';
import { SolopreneurShell } from '@/components/personas/SolopreneurShell';

export default function SolopreneurPage() {
  return (
    <SolopreneurShell>
      <PersonaDashboardLayout
        persona="solopreneur"
        title="Solopreneur Command Center"
        description="Agile tools for rapid execution and growth."
        showChat={true}
      />
    </SolopreneurShell>
  );
}
```

**StartupPage (`(personas)/startup/page.tsx`):**
Same pattern with `StartupShell`, persona="startup", title="Startup Growth Engine", description="Scale your operations and accelerate product-market fit."

**SMEPage (`(personas)/sme/page.tsx`):**
Same pattern with `SmeShell`, persona="sme", title="SME Management Hub", description="Optimize departmental efficiency and resource allocation."

**EnterprisePage (`(personas)/enterprise/page.tsx`):**
Same pattern with `EnterpriseShell`, persona="enterprise", title="Enterprise Strategy Suite", description="Global oversight, compliance, and strategic intelligence."

All pages MUST add `'use client'` directive since they import client components.

**IMPORTANT architectural note:** The shells wrap `PersonaDashboardLayout` as children. `PersonaDashboardLayout` already uses `PremiumShell` which handles the sidebar nav. The shell adds the persona-specific header ABOVE the PremiumShell layout. This means the shell header appears at the very top, and the full PremiumShell (sidebar + chat + workspace) appears below it.

However, this creates a nesting issue — `PremiumShell` is a full-viewport flex layout with `min-h-screen h-[100dvh]`. Nesting the shell's `min-h-screen` div around PremiumShell's `min-h-screen` will cause double full-height containers.

**Fix:** Update the shell components to NOT use `min-h-screen` on their outer div. Instead use a simple wrapper:
- Change shell outer div from `min-h-screen ${config.bgColor}` to just `flex flex-col min-h-0`
- The `<header>` stays as-is (it's fixed height)
- The `<main>` wrapping children should use `flex-1 min-h-0 overflow-hidden` instead of `p-4 sm:p-6`

Actually, a cleaner approach: the shells should NOT wrap the full PremiumShell layout. Instead, the shells should inject their header content INTO the PersonaDashboardLayout. Let me reconsider.

**Better approach — inject shell header into PersonaDashboardLayout:**

1. Add an optional `headerContent` prop to `PersonaDashboardLayout`:
   ```typescript
   interface PersonaDashboardLayoutProps {
     // ... existing props
     headerContent?: React.ReactNode;
   }
   ```

2. In `PersonaDashboardLayout`, render `headerContent` at the top of the content area (before the mismatch banner), inside the `<div className="relative">` wrapper, as the first child:
   ```tsx
   <div className="relative">
     {headerContent}
     {isMismatch && (...)}
     {/* rest of existing content */}
   </div>
   ```

3. In each persona page, import the shell config and render the header inline:
   ```tsx
   import { PERSONA_SHELL_CONFIG } from '@/components/personas/personaShellConfig';
   import { Rocket, ArrowRight } from 'lucide-react';
   import Link from 'next/link';

   const config = PERSONA_SHELL_CONFIG['solopreneur'];

   export default function SolopreneurPage() {
     return (
       <PersonaDashboardLayout
         persona="solopreneur"
         title="Solopreneur Command Center"
         description="Agile tools for rapid execution and growth."
         showChat={true}
         headerContent={
           <header className={`relative overflow-hidden bg-gradient-to-r ${config.gradient} px-4 sm:px-6 lg:px-8 py-5`}>
             <div className="absolute -right-8 -top-8 h-32 w-32 rounded-full bg-white/10 blur-2xl" />
             <div className="relative flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
               <div className="flex items-center gap-3">
                 <div className="h-10 w-10 rounded-xl bg-white/20 backdrop-blur-sm flex items-center justify-center">
                   <Rocket className="h-5 w-5 text-white" />
                 </div>
                 <div>
                   <h1 className="text-lg font-bold text-white">{config.tagline}</h1>
                   <p className="text-xs text-white/70">{config.description}</p>
                 </div>
               </div>
               <nav className="flex flex-wrap gap-2" aria-label="Quick actions">
                 {config.quickActions.map((action) => (
                   <Link key={action.href} href={action.href} className="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg bg-white/15 hover:bg-white/25 text-white text-xs font-medium transition-colors">
                     {action.label} <ArrowRight size={10} />
                   </Link>
                 ))}
               </nav>
             </div>
           </header>
         }
       />
     );
   }
   ```

Wait, this duplicates shell rendering code across 4 pages. Better to keep the shells as reusable components but use them differently.

**FINAL APPROACH — Shell as header-only component, rendered via headerContent prop:**

1. Modify `PersonaDashboardLayout` to accept `headerContent?: React.ReactNode` prop. Render it as first child inside the `<div className="relative">`.

2. Modify each Shell component to export BOTH a full wrapper version AND a header-only version. Add a new export `SolopreneurShellHeader` (etc.) that renders just the header section without the `<main>` wrapper. Or simpler: add a `headerOnly` boolean prop. When `headerOnly` is true, render only the header, not the wrapping div or main.

3. In each persona page:
   ```tsx
   <PersonaDashboardLayout
     persona="solopreneur"
     title="..."
     description="..."
     showChat={true}
     headerContent={<SolopreneurShell headerOnly />}
   />
   ```

**Simplest approach — just have shells accept `headerOnly` prop:**

Update each shell component (from Plan 02) to accept `{ children, headerOnly }` props:
- When `headerOnly` is true: render ONLY the `<header>` element, ignore children
- When `headerOnly` is false/undefined: render the full wrapper (header + main with children) as built in Plan 02

This is backward-compatible with the test from Plan 02 (which doesn't pass `headerOnly`).

**Implementation steps:**

1. Update `PersonaDashboardLayout.tsx`: Add `headerContent?: React.ReactNode` prop. Render it as first child inside `<div className="relative">` (before the mismatch banner).

2. Update all 4 shell components: Add `headerOnly?: boolean` to props interface. When `headerOnly` is true, return just the `<header>` element. When false, return the full wrapper.

3. Update all 4 persona pages to use the pattern:
   ```tsx
   import { [Shell] } from '@/components/personas/[Shell]';

   export default function [Persona]Page() {
     return (
       <PersonaDashboardLayout
         persona="[persona]"
         title="[title]"
         description="[description]"
         showChat={true}
         headerContent={<[Shell] headerOnly />}
       />
     );
   }
   ```

   Specific per-page values:
   - solopreneur: SolopreneurShell, "Solopreneur Command Center", "Agile tools for rapid execution and growth."
   - startup: StartupShell, "Startup Growth Engine", "Scale your operations and accelerate product-market fit."
   - sme: SmeShell, "SME Management Hub", "Optimize departmental efficiency and resource allocation."
   - enterprise: EnterpriseShell, "Enterprise Strategy Suite", "Global oversight, compliance, and strategic intelligence."
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Each persona page renders its shell header via headerContent prop. PersonaDashboardLayout renders headerContent at top of content area. Each persona route shows a visually distinct header with persona-specific gradient, icon, tagline, and quick actions. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Persona-differentiated dashboard and onboarding redirect</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/app/onboarding/processing/page.tsx</files>
  <action>
**Update `frontend/src/app/dashboard/page.tsx`:**

Currently this page always shows "Your Widgets" with a WidgetGallery. It should redirect users with a known persona to their persona-specific route, and for users without a persona, show a fallback.

Replace the entire file content:

```tsx
'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
import PersonaDashboardLayout from '@/components/dashboard/PersonaDashboardLayout';
import WidgetGallery from '@/components/widgets/WidgetGallery';
import { usePersona } from '@/contexts/PersonaContext';

export default function DashboardPage() {
  const router = useRouter();
  const { persona, isLoading, userId } = usePersona();

  // Redirect to persona-specific route when persona is known
  useEffect(() => {
    if (!isLoading && persona) {
      router.replace(`/${persona}`);
    }
  }, [isLoading, persona, router]);

  // Show loading while determining persona
  if (isLoading) {
    return (
      <div className="flex items-center justify-center min-h-screen bg-slate-50">
        <div className="animate-pulse text-slate-400 text-sm">Loading your workspace...</div>
      </div>
    );
  }

  // Fallback for users without persona (edge case: persona not set)
  const resolvedPersona = persona || 'startup';
  return (
    <PersonaDashboardLayout
      persona={resolvedPersona}
      title="Dashboard"
      description="Your personalized workspace"
      showChat={false}
    >
      <div className="p-6">
        <h2 className="text-2xl font-bold mb-4 text-slate-900">Your Widgets</h2>
        {userId && <WidgetGallery userId={userId} />}
      </div>
    </PersonaDashboardLayout>
  );
}
```

**Update `frontend/src/app/onboarding/processing/page.tsx`:**

Change the redirect destination from `/dashboard/command-center` to the persona-specific route. There are TWO places where `router.push` is called:

1. Line ~70: After successful onboarding completion, currently `router.push('/dashboard/command-center')`. Change to:
   ```typescript
   const personaRoute = result.persona ? `/${result.persona}` : '/dashboard/command-center';
   router.push(personaRoute);
   ```

2. The `persona` state is already set from `result.persona` on line 63. Use it for the redirect.

Find the block around line 66-71:
```typescript
// Wait for celebration animation then redirect
await new Promise(r => setTimeout(r, 2500));

if (isMounted) {
    router.push('/dashboard/command-center');
}
```

Replace with:
```typescript
// Wait for celebration animation then redirect to persona dashboard
await new Promise(r => setTimeout(r, 2500));

if (isMounted) {
    const personaRoute = result.persona ? `/${result.persona}` : '/dashboard/command-center';
    router.push(personaRoute);
}
```

Also update the redirect text "Redirecting to your dashboard..." to be more specific — no change needed here actually, the text is fine as-is.

**Do NOT modify** the error case redirect (`router.push('/onboarding/agent-setup')`) — that's for error recovery, not success flow.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit src/app/dashboard/page.tsx src/app/onboarding/processing/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard page redirects to persona-specific route (e.g., /solopreneur) when persona is known. Onboarding processing page redirects to persona route after completion. Users without persona see fallback startup layout. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes for all modified files
- Persona pages render shell header + PremiumShell layout without visual overlap or scrolling issues
- Dashboard page redirects to /solopreneur (or appropriate route) when persona is known
- Onboarding completion routes to persona-specific dashboard
- All 4 persona routes show visually distinct headers
</verification>

<success_criteria>
- Solopreneur route shows blue/teal header with Brain Dump, Create Initiative, Content, Sales quick actions
- Enterprise route shows slate header with Compliance, Reports, Approvals, Active Workflows quick actions
- /dashboard redirects to /{persona} when persona is loaded
- Onboarding processing completes and redirects to /{persona}
- Each persona page renders differently (not same generic layout for all)
</success_criteria>

<output>
After completion, create `.planning/phases/29-persona-frontend-ux/29-03-SUMMARY.md`
</output>
