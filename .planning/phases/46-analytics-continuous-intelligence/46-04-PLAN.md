---
phase: 46-analytics-continuous-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - app/agents/data/agent.py
  - app/agents/operations/agent.py
  - app/agents/sales/agent.py
  - app/agents/research/agent.py
  - frontend/src/app/dashboard/configuration/page.tsx
autonomous: true
requirements: [XDATA-01, XDATA-02, XDATA-04, XDATA-05, CAL-01, CAL-02, CAL-04, INTEL-01]

must_haves:
  truths:
    - "DataAnalysisAgent has external_db_query, confirm_and_run_query, and list_db_connections tools"
    - "OperationsAgent and SalesAgent have extended calendar tools including get_meeting_context and suggest_followup_meeting"
    - "ResearchAgent has monitoring tools for creating and managing monitoring jobs"
    - "Configuration page has a Database Connections section with guided form and connection string paste"
    - "Configuration page has a Monitoring Jobs section showing active jobs with edit/pause/delete"
  artifacts:
    - path: "app/agents/data/agent.py"
      provides: "DataAnalysisAgent with EXTERNAL_DB_TOOLS"
      contains: "EXTERNAL_DB_TOOLS"
    - path: "app/agents/operations/agent.py"
      provides: "OperationsAgent with extended CALENDAR_TOOLS (already has calendar tools, just needs the extended version)"
    - path: "app/agents/sales/agent.py"
      provides: "SalesAgent with CALENDAR_TOOLS for follow-up scheduling"
      contains: "CALENDAR_TOOLS"
    - path: "app/agents/research/agent.py"
      provides: "ResearchAgent with MONITORING_TOOLS"
      contains: "MONITORING_TOOLS"
    - path: "frontend/src/app/dashboard/configuration/page.tsx"
      provides: "DBConnectionsSection + MonitoringJobsSection components"
      contains: "DBConnectionsSection"
  key_links:
    - from: "app/agents/data/agent.py"
      to: "app/agents/tools/external_db_tools.py"
      via: "import EXTERNAL_DB_TOOLS"
      pattern: "EXTERNAL_DB_TOOLS"
    - from: "app/agents/sales/agent.py"
      to: "app/agents/tools/calendar_tool.py"
      via: "import CALENDAR_TOOLS"
      pattern: "CALENDAR_TOOLS"
    - from: "app/agents/research/agent.py"
      to: "app/agents/research/tools/monitoring_tools.py"
      via: "import MONITORING_TOOLS"
      pattern: "MONITORING_TOOLS"
    - from: "frontend/src/app/dashboard/configuration/page.tsx"
      to: "/monitoring-jobs"
      via: "fetchWithAuth for REST CRUD"
      pattern: "monitoring-jobs"
---

<objective>
Wire all Phase 46 tools into their respective agents and add frontend configuration sections for database connections and monitoring jobs.

Purpose: Connect the backend services from Plans 01-03 to their agent consumers and give users UI for managing DB connections and monitoring jobs.
Output: Updated agent files with new tool lists, DBConnectionsSection and MonitoringJobsSection in configuration page.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-analytics-continuous-intelligence/46-CONTEXT.md
@.planning/phases/46-analytics-continuous-intelligence/46-01-SUMMARY.md
@.planning/phases/46-analytics-continuous-intelligence/46-02-SUMMARY.md
@.planning/phases/46-analytics-continuous-intelligence/46-03-SUMMARY.md
@app/agents/data/agent.py
@app/agents/operations/agent.py
@app/agents/sales/agent.py
@app/agents/research/agent.py
@frontend/src/app/dashboard/configuration/page.tsx

<interfaces>
<!-- Key types and contracts from Plans 01-03 that this plan wires. -->

From app/agents/tools/external_db_tools.py (Plan 01):
```python
EXTERNAL_DB_TOOLS = [external_db_query, confirm_and_run_query, list_db_connections]
```

From app/agents/tools/calendar_tool.py (Plan 02 — extended):
```python
CALENDAR_TOOLS = [
    list_events, create_calendar_event, check_availability, schedule_meeting,
    find_free_slots, get_meeting_context, suggest_followup_meeting, detect_calendar_patterns,
]
```

From app/agents/research/tools/monitoring_tools.py (Plan 03):
```python
MONITORING_TOOLS = [
    create_monitoring_job, list_monitoring_jobs, pause_monitoring_job,
    resume_monitoring_job, delete_monitoring_job,
]
```

REST endpoints (Plan 03):
```
GET    /monitoring-jobs          → list user's jobs
POST   /monitoring-jobs          → create job {topic, monitoring_type, importance, keyword_triggers, pinned_urls, excluded_urls}
PATCH  /monitoring-jobs/{job_id} → update job
DELETE /monitoring-jobs/{job_id} → delete job
```

Existing frontend patterns:
```typescript
// BudgetCapSection, PMSyncSection, NotificationRulesSection — all follow same pattern:
// - Receive props from parent IntegrationProviderCard
// - Local form state synced from config prop via useEffect
// - fetchWithAuth for backend calls
// - SectionHeader component for consistent styling
```

Existing agent tool wiring patterns:
```python
# DataAnalysisAgent: uses *DATA_IO_TOOLS, *DOCUMENT_GEN_TOOLS spread in DATA_AGENT_TOOLS
# OperationsAgent: uses *PM_TASK_TOOLS, *COMMUNICATION_TOOLS spread in OPERATIONS_AGENT_TOOLS
# SalesAgent: uses *HUBSPOT_TOOLS spread in SALES_AGENT_TOOLS
# ResearchAgent: uses *GRAPH_WRITER_TOOLS, *COST_TRACKER_TOOLS in RESEARCH_AGENT_TOOLS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tools into agents (Data, Operations, Sales, Research)</name>
  <files>
    app/agents/data/agent.py,
    app/agents/operations/agent.py,
    app/agents/sales/agent.py,
    app/agents/research/agent.py
  </files>
  <action>
    1. **DataAnalysisAgent** (`app/agents/data/agent.py`):
       - Add import: `from app.agents.tools.external_db_tools import EXTERNAL_DB_TOOLS`
       - Add `*EXTERNAL_DB_TOOLS,` to `DATA_AGENT_TOOLS = sanitize_tools(...)` list
       - Add a comment `# External DB query tools` above the spread for readability

    2. **SalesAgent** (`app/agents/sales/agent.py`):
       - Add import: `from app.agents.tools.calendar_tool import CALENDAR_TOOLS`
       - Add `*CALENDAR_TOOLS,` to `SALES_AGENT_TOOLS = sanitize_tools(...)` list
       - This gives the SalesAgent calendar awareness for follow-up scheduling after sales calls (CAL-02)
       - Add comment `# Calendar tools for meeting prep and follow-up scheduling`

    3. **OperationsAgent** (`app/agents/operations/agent.py`):
       - CALENDAR_TOOLS is NOT currently imported in OperationsAgent (confirmed by grep — only reporting and HR agents import it)
       - Add import: `from app.agents.tools.calendar_tool import CALENDAR_TOOLS`
       - Add `*CALENDAR_TOOLS,` to `OPERATIONS_AGENT_TOOLS = sanitize_tools(...)` list
       - Add comment `# Calendar tools for scheduling and meeting context`

    4. **ResearchAgent** (`app/agents/research/agent.py`):
       - Add import: `from app.agents.research.tools.monitoring_tools import MONITORING_TOOLS`
       - Add `*MONITORING_TOOLS,` to `RESEARCH_AGENT_TOOLS` list
       - Add comment `# Continuous intelligence monitoring tools`

    Pattern: All tool list spreads use `*TOOL_LIST,` syntax inside `sanitize_tools()` or the direct list, matching existing patterns in each agent file.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run python -c "from app.agents.data.agent import DATA_AGENT_TOOLS; print(f'Data tools: {len(DATA_AGENT_TOOLS)}')" && uv run python -c "from app.agents.research.agent import RESEARCH_AGENT_TOOLS; print(f'Research tools: {len(RESEARCH_AGENT_TOOLS)}')" && uv run ruff check app/agents/data/agent.py app/agents/operations/agent.py app/agents/sales/agent.py app/agents/research/agent.py</automated>
  </verify>
  <done>
    - DataAnalysisAgent has EXTERNAL_DB_TOOLS (external_db_query, confirm_and_run_query, list_db_connections)
    - SalesAgent has CALENDAR_TOOLS including suggest_followup_meeting and get_meeting_context
    - OperationsAgent has CALENDAR_TOOLS including find_free_slots and detect_calendar_patterns
    - ResearchAgent has MONITORING_TOOLS (create/list/pause/resume/delete monitoring jobs)
    - All imports resolve, no lint errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend DBConnectionsSection + MonitoringJobsSection in configuration page</name>
  <files>
    frontend/src/app/dashboard/configuration/page.tsx
  </files>
  <action>
    Add two new sections to the configuration page following the established pattern (BudgetCapSection, PMSyncSection, NotificationRulesSection):

    1. **DBConnectionsSection component** (inside the configuration page file):
       - Rendered in a new "Analytics" section card on the configuration page (after the existing Google Workspace section)
       - Shows connected PostgreSQL/BigQuery databases as cards with status indicator
       - "Add Database Connection" button opens a form with:
         - Provider selector: PostgreSQL or BigQuery
         - For PostgreSQL: Guided form fields (host, port, database, username, password) PLUS a "Paste Connection String" button that auto-fills all fields from a `postgresql://user:pass@host:port/dbname` DSN — per user decision in CONTEXT.md
         - For BigQuery: "Connect with Google" OAuth button (uses existing OAuth flow since bigquery is already in PROVIDER_REGISTRY)
       - "Test Connection" button that calls `POST /integrations/{provider}/test` to verify connectivity before saving
       - Connected databases show: display name (host:port/dbname), provider icon, connected date, disconnect button
       - Connection form saves via `POST /integrations/postgresql/credentials` (for PostgreSQL — stores encrypted DSN as access_token, host:port/dbname as account_name)
       - Use `fetchWithAuth` for all backend calls (matches existing pattern)

    2. **MonitoringJobsSection component** (inside the configuration page file):
       - Rendered in a new "Continuous Intelligence" section card below Analytics
       - Lists active monitoring jobs from `GET /monitoring-jobs`
       - Each job card shows: topic, monitoring_type badge (competitor/market/topic), importance badge (critical=red, normal=blue, low=gray), schedule text, last_run_at, is_active toggle
       - "Create Monitoring Job" button opens a form with:
         - Topic (text input, required)
         - Type selector (competitor/market/topic)
         - Importance selector (critical=daily/normal=weekly/low=biweekly) — show schedule in parentheses
         - Keyword triggers (comma-separated text, optional)
         - Pinned URLs (add/remove list, optional)
         - Excluded URLs (add/remove list, optional)
       - Create form submits via `POST /monitoring-jobs`
       - Active toggle calls `PATCH /monitoring-jobs/{id}` with `{is_active: bool}`
       - Delete button calls `DELETE /monitoring-jobs/{id}` with confirmation
       - Styling matches existing section patterns: rounded-[28px], border-slate-100/80, shadow

    3. **Integration into page layout:**
       - Add "Analytics" section with SectionHeader (icon: Database or BarChart3 from lucide)
       - Add "Continuous Intelligence" section with SectionHeader (icon: Radar or Eye from lucide)
       - Place both after the Google Workspace section, before MCP Tools section
       - Use consistent grid layout matching other sections

    Note: The DB connection form stores PostgreSQL credentials via the existing integration credentials API. The connection string is encrypted with Fernet before storage (handled by backend IntegrationManager). BigQuery uses the existing OAuth flow already set up in PROVIDER_REGISTRY.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai/frontend && npx next lint --no-cache 2>&1 | head -20</automated>
  </verify>
  <done>
    - DBConnectionsSection shows connected databases with guided form and paste-connection-string button
    - MonitoringJobsSection lists active monitoring jobs with create/edit/toggle/delete
    - Both sections follow established configuration page patterns (SectionHeader, fetchWithAuth, card layout)
    - Importance levels clearly show schedule mapping (critical=daily, normal=weekly, low=biweekly)
    - No lint errors in configuration page
  </done>
</task>

</tasks>

<verification>
- `uv run python -c "from app.agents.data.agent import DATA_AGENT_TOOLS"` — imports succeed
- `uv run python -c "from app.agents.research.agent import RESEARCH_AGENT_TOOLS"` — imports succeed
- `uv run ruff check app/agents/data/agent.py app/agents/operations/agent.py app/agents/sales/agent.py app/agents/research/agent.py` — no lint errors
- `cd frontend && npx next lint --no-cache` — no lint errors
- `grep "EXTERNAL_DB_TOOLS" app/agents/data/agent.py` — present
- `grep "MONITORING_TOOLS" app/agents/research/agent.py` — present
- `grep "CALENDAR_TOOLS" app/agents/sales/agent.py` — present
- `grep "DBConnectionsSection\|MonitoringJobsSection" frontend/src/app/dashboard/configuration/page.tsx` — both present
</verification>

<success_criteria>
- DataAnalysisAgent can execute NL-to-SQL queries against connected external databases (XDATA-04, XDATA-05)
- SalesAgent can suggest follow-up meetings with calendar awareness (CAL-01, CAL-02)
- OperationsAgent provides meeting prep context with CRM data (CAL-04)
- ResearchAgent can create and manage monitoring jobs via chat (INTEL-01)
- Configuration page has DB Connections section with guided form + connection string paste (XDATA-01, XDATA-02)
- Configuration page has Monitoring Jobs section with create/edit/toggle/delete
- All agent imports resolve and lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/46-analytics-continuous-intelligence/46-04-SUMMARY.md`
</output>
