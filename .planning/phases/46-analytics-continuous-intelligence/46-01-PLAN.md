---
phase: 46-analytics-continuous-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config/integration_providers.py
  - app/services/external_db_service.py
  - app/agents/tools/external_db_tools.py
  - supabase/migrations/20260406000000_external_db_monitoring.sql
  - tests/unit/test_external_db_service.py
  - tests/unit/tools/test_external_db_tools.py
autonomous: true
requirements: [XDATA-01, XDATA-02, XDATA-03, XDATA-04, XDATA-05, XDATA-06]

must_haves:
  truths:
    - "User can connect a PostgreSQL database as an integration provider"
    - "User can connect a BigQuery data warehouse with read-only access"
    - "All external database queries execute in read-only mode and abort after 30 seconds"
    - "Simple queries run immediately while complex queries require user confirmation before execution"
    - "User asks a question in natural language and receives tabular results with chart suggestions"
    - "Database credentials are never exposed in error messages shown to the user"
  artifacts:
    - path: "app/config/integration_providers.py"
      provides: "postgresql ProviderConfig entry in PROVIDER_REGISTRY"
      contains: "\"postgresql\": ProviderConfig"
    - path: "app/services/external_db_service.py"
      provides: "ExternalDbQueryService with PostgreSQL + BigQuery query execution"
      exports: ["ExternalDbQueryService"]
      min_lines: 120
    - path: "app/agents/tools/external_db_tools.py"
      provides: "Agent tools for NL-to-SQL query and connection listing"
      exports: ["EXTERNAL_DB_TOOLS"]
      min_lines: 80
    - path: "supabase/migrations/20260406000000_external_db_monitoring.sql"
      provides: "monitoring_jobs table with RLS policies"
      contains: "CREATE TABLE public.monitoring_jobs"
    - path: "tests/unit/test_external_db_service.py"
      provides: "Unit tests for ExternalDbQueryService"
      min_lines: 60
    - path: "tests/unit/tools/test_external_db_tools.py"
      provides: "Unit tests for external DB agent tools"
      min_lines: 40
  key_links:
    - from: "app/agents/tools/external_db_tools.py"
      to: "app/services/external_db_service.py"
      via: "lazy import ExternalDbQueryService"
      pattern: "ExternalDbQueryService"
    - from: "app/services/external_db_service.py"
      to: "app/config/integration_providers.py"
      via: "provider lookup for connection credentials"
      pattern: "get_provider|PROVIDER_REGISTRY"
---

<objective>
External database query backend: PostgreSQL provider registration, ExternalDbQueryService with read-only enforcement and 30-second timeouts, agent tools for NL-to-SQL execution, and shared migration for the monitoring_jobs table.

Purpose: Enable users to connect external PostgreSQL/BigQuery databases and query them through the DataAnalysisAgent with strict safety guarantees (read-only, timeout, row cap).
Output: ExternalDbQueryService, EXTERNAL_DB_TOOLS list, postgresql provider entry, migration file, unit tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-analytics-continuous-intelligence/46-CONTEXT.md
@.planning/phases/46-analytics-continuous-intelligence/46-RESEARCH.md
@app/config/integration_providers.py
@app/agents/tools/calendar_tool.py
@app/services/intelligence_scheduler.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/config/integration_providers.py:
```python
@dataclass(frozen=True)
class ProviderConfig:
    name: str
    auth_type: Literal["oauth2", "api_key"]
    auth_url: str
    token_url: str
    scopes: list[str]
    client_id_env: str
    client_secret_env: str
    webhook_secret_header: str | None
    icon_url: str
    category: Literal["crm_sales", "finance_commerce", "productivity", "analytics", "communication"]

PROVIDER_REGISTRY: dict[str, ProviderConfig] = { ... }
# "teams" uses auth_type="api_key" — same pattern for "postgresql"
# "bigquery" already registered with oauth2 and readonly scopes
```

From app/services/encryption.py (used for credential storage):
```python
def encrypt_secret(plaintext: str) -> str: ...
def decrypt_secret(ciphertext: str) -> str: ...
```

From app/services/supabase.py:
```python
def get_service_client() -> Client: ...
```

From app/services/supabase_async.py:
```python
async def execute_async(query) -> Any: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + PostgreSQL provider registration + ExternalDbQueryService</name>
  <files>
    supabase/migrations/20260406000000_external_db_monitoring.sql,
    app/config/integration_providers.py,
    app/services/external_db_service.py,
    tests/unit/test_external_db_service.py
  </files>
  <behavior>
    - Test: postgresql provider registered in PROVIDER_REGISTRY with auth_type="api_key" and category="analytics"
    - Test: bigquery provider has "bigquery.readonly" in scopes
    - Test: query_postgres enforces read-only (set_session readonly=True called on mock connection)
    - Test: query_postgres sets statement_timeout to 30000ms on mock cursor
    - Test: query_postgres caps results at 1000 rows (fetchmany(1000))
    - Test: query_postgres raises TimeoutError when asyncio.wait_for exceeds 32s
    - Test: query_bigquery passes timeout=30 to result()
    - Test: classify_sql returns "auto" for simple SELECT, "needs_confirmation" for JOINs/subqueries
    - Test: connection string password is sanitized in error messages (no plaintext password in logged OperationalError)
  </behavior>
  <action>
    1. Create migration `supabase/migrations/20260406000000_external_db_monitoring.sql`:
       - `monitoring_jobs` table with columns: id (uuid PK), user_id (uuid FK auth.users), topic (text NOT NULL), monitoring_type (text NOT NULL DEFAULT 'competitor' — competitor|market|topic), importance (text NOT NULL DEFAULT 'normal' — critical|normal|low), is_active (boolean DEFAULT true), pinned_urls (text[] DEFAULT '{}'), excluded_urls (text[] DEFAULT '{}'), keyword_triggers (text[] DEFAULT '{}'), last_run_at (timestamptz), last_brief_id (uuid), previous_state_hash (text), created_at/updated_at (timestamptz DEFAULT now())
       - RLS: "Users can manage own monitoring jobs" FOR ALL USING (auth.uid() = user_id), "Service role full access" FOR ALL USING (auth.role() = 'service_role')
       - This migration is shared with Plan 03 (INTEL track) — created here since Plan 01 runs first or in parallel, and migration files must not conflict

    2. Add `"postgresql"` entry to PROVIDER_REGISTRY in `app/config/integration_providers.py`:
       - auth_type="api_key", empty auth_url/token_url/scopes/client_id_env/client_secret_env
       - webhook_secret_header=None, icon_url="https://cdn.pikar.ai/icons/postgresql.svg", category="analytics"
       - Pattern matches the existing "teams" provider entry (api_key auth_type)

    3. Create `app/services/external_db_service.py` with class `ExternalDbQueryService`:
       - `classify_sql(sql: str) -> Literal["auto", "needs_confirmation"]`: Keyword heuristic — "needs_confirmation" if lowercased SQL contains any of: `join`, `group by`, subquery pattern (nested SELECT), `union`, `having`, `with ` (CTE). All else returns "auto".
       - `async query_postgres(connection_string: str, sql: str, timeout_sec: int = 30) -> dict`: Uses `asyncio.to_thread()` wrapping sync psycopg2. Inside: `conn.set_session(readonly=True)`, `SET statement_timeout = {timeout_sec * 1000}`, execute SQL, `fetchmany(1000)`, return `{"columns": [...], "rows": [...], "row_count": N, "truncated": bool}`. Wrap outer call in `asyncio.wait_for(timeout=timeout_sec + 2)`. Catch `psycopg2.OperationalError` and re-raise with sanitized message (strip password from DSN). Append `?sslmode=require` to connection string if no sslmode present.
       - `async query_bigquery(project_id: str, credentials_json: str, sql: str, timeout_sec: int = 30) -> dict`: Uses `asyncio.to_thread()` wrapping `google.cloud.bigquery.Client`. Pass `timeout=timeout_sec` to `query().result()`. Cap at 1000 rows. Same return format.
       - `suggest_chart_type(columns: list[str], rows: list) -> str | None`: Simple heuristic — if one string column + one numeric column -> "bar", two numeric columns -> "scatter", one date-like column + one numeric -> "line", else None.
       - All psycopg2 and google.cloud.bigquery imports are lazy (inside method bodies) per project pattern.

    4. Create `tests/unit/test_external_db_service.py`:
       - Mock psycopg2.connect, verify set_session(readonly=True) called
       - Mock cursor.execute, verify statement_timeout SET call
       - Mock fetchmany, verify 1000 cap
       - Test classify_sql with various SQL strings
       - Test password sanitization on OperationalError
       - Test timeout enforcement (mock slow query, verify TimeoutError)
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/test_external_db_service.py -x -q</automated>
  </verify>
  <done>
    - postgresql registered in PROVIDER_REGISTRY with api_key auth_type
    - ExternalDbQueryService handles PostgreSQL and BigQuery with read-only enforcement, 30s timeout, 1000-row cap
    - SQL classification heuristic distinguishes simple vs complex queries
    - Connection string passwords never appear in error messages
    - monitoring_jobs migration exists (shared with Plan 03)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: External DB agent tools (NL-to-SQL + results display)</name>
  <files>
    app/agents/tools/external_db_tools.py,
    tests/unit/tools/test_external_db_tools.py
  </files>
  <behavior>
    - Test: external_db_query returns dict with keys: sql_generated, columns, rows, row_count, chart_suggestion, nl_summary, needs_confirmation
    - Test: list_db_connections returns list of connected database providers for the user
    - Test: when classify_sql returns "needs_confirmation", tool returns needs_confirmation=True and does NOT execute query
    - Test: when classify_sql returns "auto", tool executes query and returns results
    - Test: EXTERNAL_DB_TOOLS list contains external_db_query and list_db_connections
  </behavior>
  <action>
    Create `app/agents/tools/external_db_tools.py` following the pattern of `app/agents/tools/ad_platform_tools.py` (raw function exports, lazy imports):

    1. `async def external_db_query(natural_language_query: str, database: str | None = None) -> dict[str, Any]`:
       - Get user_id from tool context state (follow existing tool patterns — check `tool_context.state` for credentials)
       - Lazy import `ExternalDbQueryService`
       - Look up connected DB credentials from `integration_credentials` table for the user (provider in ["postgresql", "bigquery"])
       - If `database` not specified and multiple connections exist, return list for user to choose
       - Generate SQL from natural_language_query using a prompt to Gemini (use `google.genai` generate_content with a system prompt containing the table schema). The SQL generation prompt should instruct: "Generate a single SELECT statement. Never generate INSERT, UPDATE, DELETE, DROP, or any DDL. Return only the SQL, no explanation."
       - Parse generated SQL, run `classify_sql()` — if "needs_confirmation", return `{"needs_confirmation": True, "sql_generated": sql, "explanation": "..."}`
       - If "auto", execute via `ExternalDbQueryService.query_postgres()` or `query_bigquery()` based on provider
       - Return `{"sql_generated": sql, "columns": [...], "rows": [...], "row_count": N, "chart_suggestion": suggest_chart_type(...), "nl_summary": "Found N rows showing...", "needs_confirmation": False}`

    2. `async def confirm_and_run_query(sql: str, database: str | None = None) -> dict[str, Any]`:
       - For confirmed complex queries — executes pre-generated SQL directly (no re-generation)
       - Same execution + result format as external_db_query

    3. `async def list_db_connections() -> dict[str, Any]`:
       - Query `integration_credentials` for user's postgresql/bigquery connections
       - Return list with provider, account_name (display name), connected_at

    4. Export: `EXTERNAL_DB_TOOLS = [external_db_query, confirm_and_run_query, list_db_connections]`

    5. Create `tests/unit/tools/test_external_db_tools.py`:
       - Mock ExternalDbQueryService and credential lookup
       - Test external_db_query returns correct dict shape
       - Test needs_confirmation flow (complex SQL)
       - Test auto-execute flow (simple SQL)
       - Test list_db_connections returns provider list
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/tools/test_external_db_tools.py -x -q</automated>
  </verify>
  <done>
    - external_db_query generates SQL from natural language, classifies complexity, auto-executes simple queries, returns results with chart suggestions
    - confirm_and_run_query executes pre-confirmed complex SQL
    - list_db_connections lists user's connected databases
    - EXTERNAL_DB_TOOLS exported as list
    - Agent gives NL summary first with data tables underneath (per user decision)
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_external_db_service.py tests/unit/tools/test_external_db_tools.py -x -q` — all tests pass
- `uv run ruff check app/services/external_db_service.py app/agents/tools/external_db_tools.py app/config/integration_providers.py` — no lint errors
- `grep -c "postgresql" app/config/integration_providers.py` returns >= 1
- `grep -c "readonly" app/services/external_db_service.py` returns >= 2 (session-level + SQL classification)
</verification>

<success_criteria>
- PostgreSQL registered as api_key provider in PROVIDER_REGISTRY
- BigQuery readonly scopes confirmed
- ExternalDbQueryService enforces read-only + 30s timeout + 1000 row cap on both PostgreSQL and BigQuery
- SQL classification correctly identifies simple vs complex queries
- Agent tools return NL summary + tabular data + chart suggestion
- Connection string passwords sanitized in all error paths
- monitoring_jobs migration created (shared with INTEL track)
</success_criteria>

<output>
After completion, create `.planning/phases/46-analytics-continuous-intelligence/46-01-SUMMARY.md`
</output>
