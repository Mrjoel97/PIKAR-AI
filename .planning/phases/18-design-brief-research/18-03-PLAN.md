---
phase: 18-design-brief-research
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified: []
autonomous: false
requirements: [FLOW-02, FLOW-03, FLOW-04]

must_haves:
  truths:
    - "End-to-end flow works: questioning wizard -> research page -> SSE progress -> editable brief -> approve -> build plan"
    - "Backend tests and frontend tests both pass in the same run"
    - "No regressions in existing Phase 17 tests"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 18 integration: backend research service + SSE endpoints + frontend research page with editable cards and approval flow.

Purpose: Plans 01 and 02 build backend and frontend independently. This checkpoint verifies they integrate correctly and the full user flow works end-to-end.

Output: Verification confirmation. No new files created.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/phases/18-design-brief-research/18-01-SUMMARY.md
@.planning/phases/18-design-brief-research/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify no regressions</name>
  <files>tests/unit/app_builder/, frontend/src/__tests__/</files>
  <action>
    Run the complete test suite covering both backend and frontend:

    1. Backend tests: `uv run pytest tests/unit/app_builder/ -x -v` — must pass all design_brief_service tests and app_builder_router tests (including the 7 original Phase 17 tests and the 4+ new Phase 18 tests).

    2. Frontend tests: `cd frontend && node scripts/run-vitest.mjs` — must pass all tests including GsdProgressBar (4 tests from Phase 17), QuestioningWizard (5 tests from Phase 17), DesignBriefCard (3 tests from Phase 18), and ResearchPage (4 tests from Phase 18).

    3. Lint check: `uv run ruff check app/services/design_brief_service.py app/routers/app_builder.py --fix && uv run ruff format app/services/design_brief_service.py app/routers/app_builder.py`

    4. Migration file check: Verify `supabase/migrations/20260321700000_design_brief_unique.sql` exists and contains the UNIQUE constraint SQL.

    If any tests fail, fix the issue in the relevant file and re-run. Do not proceed to the checkpoint until all tests pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app_builder/ -x -v && cd C:/Users/expert/Documents/PKA/Pikar-Ai/frontend && node scripts/run-vitest.mjs</automated>
  </verify>
  <done>All backend and frontend tests pass. No regressions from Phase 17 tests. Lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end design research and approval flow</name>
  <files></files>
  <action>
    Present the human verification checklist. The user needs to start both servers and walk through the complete flow from questioning wizard through research, editing, approval, and build plan display.
  </action>
  <verify>Human confirms the flow works or reports issues.</verify>
  <done>User has verified the end-to-end design research and approval flow works correctly.</done>
  <what-built>
    Complete Phase 18 design research flow:
    - Backend: design_brief_service.py with Tavily research + Gemini synthesis + build plan generation
    - Backend: POST /app-builder/projects/{id}/research (SSE stream) and POST /approve-brief endpoints
    - Frontend: Dynamic GsdProgressBar in [projectId] layout
    - Frontend: Research page with SSE progress animation, editable DesignBriefCard, editable SitemapCard, Approve button, and BuildPlanView
    - DB: UNIQUE constraint migration on design_systems(project_id)
  </what-built>
  <how-to-verify>
    1. Start the backend: `make local-backend` (port 8000)
    2. Start the frontend: `cd frontend && npm run dev` (port 3000)
    3. Navigate to http://localhost:3000/app-builder/new
    4. Complete the 5-step questioning wizard (pick choices, enter a project name, click "Start Building")
    5. Verify you are redirected to /app-builder/{projectId}/research
    6. Verify the GsdProgressBar shows "Research" as the current stage (not "Questioning")
    7. Verify the SSE progress indicator shows steps: "Researching design space..." then "Generating design brief..." then "Saving..."
    8. After research completes, verify DesignBriefCard appears with color swatches, typography fields, spacing inputs, and DESIGN.md textarea
    9. Verify SitemapCard appears with proposed pages, sections, and device target checkboxes
    10. Edit a color hex value — verify the swatch updates
    11. Edit a page title in the sitemap — verify it updates
    12. Click "Approve & Generate Build Plan"
    13. Verify the build plan appears below showing phases with screen names and dependencies
    14. Verify the GsdProgressBar updates (or the next page shows "Building" stage)
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes: backend (pytest) + frontend (vitest)
- User can complete the questioning -> research -> approve -> build plan flow end-to-end
- No console errors in browser during the flow
- GsdProgressBar reflects actual project stage dynamically
</verification>

<success_criteria>
- All automated tests pass in a single run
- Human verification confirms the end-to-end flow works
- Editable design brief and sitemap cards allow modifications before approval
- Approval is an explicit action, not automatic
- Build plan is visible after approval with phase/dependency structure
</success_criteria>

<output>
After completion, create `.planning/phases/18-design-brief-research/18-03-SUMMARY.md`
</output>
