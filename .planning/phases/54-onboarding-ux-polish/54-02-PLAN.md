---
phase: 54-onboarding-ux-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/google_workspace_auth_service.py
  - app/routers/configuration.py
  - app/routers/briefing.py
  - app/integrations/google/client.py
  - app/agents/tools/calendar_tool.py
  - app/agents/tools/gmail.py
  - app/agents/tools/gmail_inbox.py
  - app/agents/tools/docs.py
  - app/agents/tools/forms.py
  - app/agents/tools/google_sheets.py
  - app/services/briefing_digest_service.py
  - app/services/email_triage_worker.py
  - frontend/src/app/auth/callback/route.ts
  - frontend/src/app/api/configuration/google-workspace-status/route.ts
  - frontend/src/app/dashboard/configuration/page.tsx
  - frontend/src/__tests__/services/google-workspace-status.test.ts
  - tests/unit/app/test_google_workspace_auth_service.py
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Google Workspace credentials are synchronized through a canonical backend path after Supabase auth callback"
    - "Google Workspace status reflects usable stored credentials, not only Google identity presence"
    - "Later Gmail/Calendar-capable actions can resolve credentials without forcing the user to re-authenticate immediately"
    - "Foreground Google tools and background Google consumers share one clear credential resolution path with controlled compatibility fallback if needed"
    - "Frontend status/reconnect UX reflects the real backend state"
  artifacts:
    - path: "app/services/google_workspace_auth_service.py"
      provides: "Canonical Google Workspace credential sync and resolution service"
      exports: ["GoogleWorkspaceAuthService", "get_google_workspace_auth_service"]
    - path: "app/routers/configuration.py"
      provides: "Authenticated Google Workspace sync + truthful status endpoints"
      contains: "google-workspace"
    - path: "frontend/src/app/auth/callback/route.ts"
      provides: "Server-side sync of Google provider tokens after auth callback"
      contains: "exchangeCodeForSession"
    - path: "tests/unit/app/test_google_workspace_auth_service.py"
      provides: "Backend regression coverage for sync/status/credential lookup"
      contains: "google workspace"
  key_links:
    - from: "frontend/src/app/auth/callback/route.ts"
      to: "app/routers/configuration.py"
      via: "server-side Google Workspace credential sync after Supabase callback"
      pattern: "google-workspace"
    - from: "app/agents/tools/calendar_tool.py"
      to: "app/integrations/google/client.py"
      via: "fallback credential resolution for later-session Google actions"
      pattern: "get_google_credentials"
---

<objective>
Persist Google Workspace credentials correctly and make later Gmail/Calendar actions work without an immediate re-auth requirement.

Purpose: The app currently asks Google for the right scopes, but the post-callback flow does not establish one canonical persisted credential path. Status can therefore look green while later Google-backed actions still fail. This plan makes the connection real, reusable, and truthful in the UI.

Output: canonical backend Google Workspace auth service, callback-time credential sync, truthful status/reconnect UX, and backend/frontend tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-onboarding-ux-polish/54-CONTEXT.md
@.planning/phases/54-onboarding-ux-polish/54-RESEARCH.md
@.planning/phases/54-onboarding-ux-polish/54-VALIDATION.md

@frontend/src/services/auth.ts
@frontend/src/app/auth/callback/route.ts
@frontend/src/app/dashboard/configuration/page.tsx
@app/routers/configuration.py
@app/integrations/google/client.py
@app/agents/tools/calendar_tool.py
@app/agents/tools/gmail.py
@app/agents/tools/gmail_inbox.py
@app/agents/tools/docs.py
@app/agents/tools/forms.py
@app/agents/tools/google_sheets.py
@app/services/briefing_digest_service.py
@app/services/email_triage_worker.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a canonical backend Google Workspace credential service and truthful status path</name>
  <files>app/services/google_workspace_auth_service.py, app/routers/configuration.py, tests/unit/app/test_google_workspace_auth_service.py</files>
  <action>
1. Add a canonical backend service that can:
   - persist Google Workspace provider/access/refresh token material for an authenticated user
   - report whether the user has a usable Google Workspace connection
   - resolve the user’s stored credential metadata through one backend-owned path
2. Update `app/routers/configuration.py` so Google Workspace status reflects usable stored credentials, not only the existence of a Google identity in Supabase Auth.
3. Add backend tests covering token sync, truthful connection status, and compatibility behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app/test_google_workspace_auth_service.py -x</automated>
  </verify>
  <done>Backend has one canonical Google Workspace credential/status path with tests proving the connection state is truthful.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Google provider tokens after Supabase auth callback using a server-only flow</name>
  <files>frontend/src/app/auth/callback/route.ts, app/routers/configuration.py, frontend/src/__tests__/services/google-workspace-status.test.ts</files>
  <action>
1. After `exchangeCodeForSession()` succeeds in the auth callback route, resolve the Google provider token/refresh token from the server-side session.
2. Call a backend-authenticated sync endpoint so the credential persistence happens in a backend-owned flow without exposing tokens to browser code.
3. Keep existing returnUrl behavior intact and fail gracefully if sync cannot complete, with logs that make the failure diagnosable.
4. Add frontend regression coverage for the callback/status contract where practical.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/google-workspace-status.test.ts</automated>
  </verify>
  <done>Google Workspace credentials are synchronized at auth-callback time through a server-only flow that preserves the current auth UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Let later Google-backed actions resolve stored credentials without re-auth</name>
  <files>app/integrations/google/client.py, app/agents/tools/calendar_tool.py, app/agents/tools/gmail.py, app/agents/tools/gmail_inbox.py, app/agents/tools/docs.py, app/agents/tools/forms.py, app/agents/tools/google_sheets.py, app/services/briefing_digest_service.py, app/services/email_triage_worker.py, tests/unit/app/test_google_workspace_auth_service.py, frontend/src/app/dashboard/configuration/page.tsx</files>
  <action>
1. Extend the canonical Google credential resolution path so Google tools can fall back to stored credentials when live session state tokens are absent.
2. Update the targeted foreground/background Google consumers in this plan’s scope to use that canonical resolution path or a compatibility wrapper around it.
3. Update the configuration/integrations UX to show the real state clearly, including reconnect guidance when stored credentials are missing or incomplete.
4. Preserve compatibility where needed during migration, but leave one clear canonical path as the final source of truth.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app/test_google_workspace_auth_service.py -x && cd frontend && npm run test -- src/__tests__/services/google-workspace-status.test.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>Later Gmail/Calendar-capable actions and the status UI both rely on a real persisted Google Workspace connection rather than optimistic identity checks.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/app/test_google_workspace_auth_service.py -x` passes
- `cd frontend && npm run test -- src/__tests__/services/google-workspace-status.test.ts` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Google Workspace credentials are synced and stored through a canonical backend path
- Status only reports “connected” when later use is actually possible
- Gmail/Calendar-capable actions can resolve stored credentials after callback/navigation
- Reconnect guidance in the UI reflects the real backend state
</success_criteria>

<output>
After completion, create `.planning/phases/54-onboarding-ux-polish/54-02-SUMMARY.md`
</output>
