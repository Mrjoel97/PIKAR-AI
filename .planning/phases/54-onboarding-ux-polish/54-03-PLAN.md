---
phase: 54-onboarding-ux-polish
plan: "03"
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - frontend/src/components/ui/EmptyState.tsx
  - frontend/src/app/dashboard/finance/page.tsx
  - frontend/src/app/dashboard/governance/page.tsx
  - frontend/src/app/dashboard/content/page.tsx
  - frontend/src/app/dashboard/portfolio/page.tsx
  - frontend/src/app/dashboard/departments/page.tsx
  - frontend/src/__tests__/dashboard/empty-states.test.tsx
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "Targeted dashboard zero-data sections show descriptive empty states instead of passive placeholder text"
    - "Each targeted empty state includes a suggested next action or clear CTA"
    - "The upgraded pages reuse the shared empty-state design language rather than page-specific ad hoc placeholders"
    - "Representative frontend tests cover the empty-state contract"
  artifacts:
    - path: "frontend/src/components/ui/EmptyState.tsx"
      provides: "Shared empty-state building block for dashboard pages"
      contains: "actionLabel"
    - path: "frontend/src/app/dashboard/finance/page.tsx"
      provides: "Actionable finance zero-data states"
      contains: "EmptyState"
    - path: "frontend/src/app/dashboard/governance/page.tsx"
      provides: "Actionable governance zero-data states"
      contains: "EmptyState"
    - path: "frontend/src/__tests__/dashboard/empty-states.test.tsx"
      provides: "Regression coverage for representative zero-data pages"
      contains: "empty state"
  key_links:
    - from: "frontend/src/app/dashboard/finance/page.tsx"
      to: "frontend/src/components/ui/EmptyState.tsx"
      via: "shared no-data UX with CTA"
      pattern: "EmptyState"
    - from: "frontend/src/app/dashboard/governance/page.tsx"
      to: "frontend/src/components/ui/EmptyState.tsx"
      via: "shared no-data UX with CTA"
      pattern: "EmptyState"
---

<objective>
Polish the remaining zero-data dashboard experiences so users always see a meaningful next step instead of a passive blank or placeholder message.

Purpose: Phase 54 does not need a dashboard redesign, but it does need the remaining no-data surfaces to feel intentional and actionable. This plan upgrades the main dashboard pages still relying on raw placeholder copy.

Output: shared empty-state polish where needed, targeted dashboard page upgrades, and representative frontend regression tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-onboarding-ux-polish/54-CONTEXT.md
@.planning/phases/54-onboarding-ux-polish/54-RESEARCH.md
@.planning/phases/54-onboarding-ux-polish/54-VALIDATION.md

@frontend/src/components/ui/EmptyState.tsx
@frontend/src/app/dashboard/finance/page.tsx
@frontend/src/app/dashboard/governance/page.tsx
@frontend/src/app/dashboard/content/page.tsx
@frontend/src/app/dashboard/portfolio/page.tsx
@frontend/src/app/dashboard/departments/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the shared empty-state contract for dashboard use</name>
  <files>frontend/src/components/ui/EmptyState.tsx</files>
  <action>
1. Review the shared `EmptyState` component and extend it only as needed to support the Phase 54 dashboard CTA patterns.
2. Keep the existing visual language intact; this is polish, not a redesign.
3. Ensure the component can express descriptive copy plus one clear next action.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The shared empty-state component supports the actionable dashboard patterns needed for the targeted pages.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the main zero-data dashboard pages to actionable empty states</name>
  <files>frontend/src/app/dashboard/finance/page.tsx, frontend/src/app/dashboard/governance/page.tsx, frontend/src/app/dashboard/content/page.tsx, frontend/src/app/dashboard/portfolio/page.tsx, frontend/src/app/dashboard/departments/page.tsx</files>
  <action>
1. Replace passive zero-data copy in the targeted pages with deliberate empty states using the shared component where appropriate.
2. Each targeted page should tell the user what to do next, for example:
   - go to chat/workspace
   - create the first initiative/workflow
   - connect data/integrations
3. Preserve existing data-present layouts and loading behavior.
4. Keep the empty-state copy persona-neutral unless the page already has persona-aware context.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The targeted dashboard pages now present clear zero-data UX with CTA guidance instead of passive placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add representative regression coverage for dashboard empty states</name>
  <files>frontend/src/__tests__/dashboard/empty-states.test.tsx</files>
  <action>
1. Add a representative frontend test file covering at least the key targeted zero-data pages or the extracted empty-state render contract they share.
2. Verify that the rendered empty states include descriptive copy and CTA affordances rather than raw fallback text.
3. Keep tests resilient by focusing on user-visible behavior, not implementation trivia.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/dashboard/empty-states.test.tsx && npx tsc -p . --noEmit</automated>
  </verify>
  <done>Representative tests protect the actionable empty-state experience on the upgraded dashboard pages.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run test -- src/__tests__/dashboard/empty-states.test.tsx` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- The targeted zero-data pages no longer rely on passive placeholder text
- Users see a clear next step on each targeted empty state
- Shared empty-state UI is reused consistently
</success_criteria>

<output>
After completion, create `.planning/phases/54-onboarding-ux-polish/54-03-SUMMARY.md`
</output>
