---
phase: 54-onboarding-ux-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/onboarding/components/OnboardingTransition.tsx
  - frontend/src/app/onboarding/processing/page.tsx
  - frontend/src/components/dashboard/PersonaDashboardLayout.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/lib/onboarding/navigation.ts
  - frontend/src/__tests__/services/onboarding-launch.test.ts
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "Completing onboarding lands the user on a chat-enabled surface rather than a dashboard view with chat hidden"
    - "A selected onboarding first action becomes the first live chat prompt without manual re-entry"
    - "PersonaDashboardLayout consumes sanitized initialPrompt launch data exactly once and clears it from the URL"
    - "Command-center checklist actions still work even when the current surface hides chat"
    - "ChatInterface treats a welcome-only session as launch-ready so initial prompts are not blocked by the default greeting state"
    - "Regression tests cover launch URL building and initial prompt extraction/consumption"
  artifacts:
    - path: "frontend/src/lib/onboarding/navigation.ts"
      provides: "Shared onboarding/chat launch helpers"
      contains: "initialPrompt"
    - path: "frontend/src/app/onboarding/components/OnboardingTransition.tsx"
      provides: "Post-onboarding redirect to live chat surface"
      contains: "router.push"
    - path: "frontend/src/components/dashboard/PersonaDashboardLayout.tsx"
      provides: "Initial chat prompt parsing and one-shot consumption"
      contains: "initialPrompt"
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "Welcome-only session launch handling for initial prompts"
      contains: "welcome-message"
    - path: "frontend/src/__tests__/services/onboarding-launch.test.ts"
      provides: "Frontend regression coverage for onboarding launch behavior"
      contains: "initialPrompt"
  key_links:
    - from: "frontend/src/app/onboarding/components/OnboardingTransition.tsx"
      to: "frontend/src/lib/onboarding/navigation.ts"
      via: "shared helper builds launch destination for first action"
      pattern: "build"
    - from: "frontend/src/components/dashboard/OnboardingChecklist.tsx"
      to: "frontend/src/components/dashboard/PersonaDashboardLayout.tsx"
      via: "checklist prompt reaches a chat-enabled layout path"
      pattern: "onActionClick"
---

<objective>
Complete the onboarding-to-first-chat handoff so new users reach a real live conversation immediately after setup.

Purpose: The current conversational onboarding already collects everything it needs, but the final “first action” handoff routes into a surface where chat is hidden and the launch prompt is ignored. This plan fixes that handoff without changing the overall onboarding experience.

Output: shared onboarding launch helper, prompt-aware dashboard layout, chat-enabled first-action landing behavior, and frontend regression tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-onboarding-ux-polish/54-CONTEXT.md
@.planning/phases/54-onboarding-ux-polish/54-RESEARCH.md
@.planning/phases/54-onboarding-ux-polish/54-VALIDATION.md

@frontend/src/app/onboarding/components/OnboardingTransition.tsx
@frontend/src/app/onboarding/processing/page.tsx
@frontend/src/components/dashboard/PersonaDashboardLayout.tsx
@frontend/src/components/dashboard/OnboardingChecklist.tsx
@frontend/src/app/dashboard/command-center/page.tsx
@frontend/src/app/dashboard/workspace/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one shared onboarding/chat launch helper</name>
  <files>frontend/src/lib/onboarding/navigation.ts, frontend/src/__tests__/services/onboarding-launch.test.ts</files>
  <action>
1. Create a small shared helper module that centralizes:
   - the chat-enabled route to use for onboarding/checklist launches
   - safe `initialPrompt` query construction
   - safe prompt extraction from URL search params
2. Ensure the helper rejects unsafe or empty prompt payloads and preserves the existing sanitized query conventions.
3. Add frontend tests that cover route building and prompt extraction behavior.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/onboarding-launch.test.ts -x</automated>
  </verify>
  <done>There is one tested helper that defines how onboarding/checklist prompt launches are encoded and decoded.</done>
</task>

<task type="auto">
  <name>Task 2: Route onboarding completion and checklist actions into a live chat surface</name>
  <files>frontend/src/app/onboarding/components/OnboardingTransition.tsx, frontend/src/app/onboarding/processing/page.tsx, frontend/src/components/dashboard/PersonaDashboardLayout.tsx</files>
  <action>
1. Update `OnboardingTransition.tsx` so the selected first action redirects to the shared chat-enabled destination instead of a hidden-chat command-center landing.
2. Align the legacy `frontend/src/app/onboarding/processing/page.tsx` completion destination with the same launch behavior or compatibility route.
3. Ensure checklist actions triggered from command-center do not dead-end on a hidden chat surface. They should either navigate to the chat-enabled destination or open a chat-visible surface consistently.
4. Preserve the existing command-center/dashboard product structure unless a compatibility redirect is required for this exact UX fix.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/onboarding-launch.test.ts -x</automated>
  </verify>
  <done>Both onboarding completion and checklist actions can move the user into a chat-visible flow with their prompt intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teach the dashboard/chat flow to consume initialPrompt once</name>
  <files>frontend/src/components/dashboard/PersonaDashboardLayout.tsx, frontend/src/components/chat/ChatInterface.tsx, frontend/src/__tests__/services/onboarding-launch.test.ts</files>
  <action>
1. Update `PersonaDashboardLayout.tsx` to read the shared `initialPrompt` query parameter in addition to the existing initiative/journey/braindump launch paths.
2. After the prompt is captured, clear the URL so refreshes do not replay the same first-action prompt.
3. Update `ChatInterface.tsx` so a fresh session with only the default welcome message still accepts the launch prompt exactly once.
4. Keep existing `context`, `initiativeId`, `fromJourney`, `outcomesPrompt`, and `braindump_id` behavior intact.
5. Extend tests so one-shot `initialPrompt` handling is covered explicitly.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/onboarding-launch.test.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>Prompt-aware layout behavior is tested, one-shot, and backwards-compatible with existing dashboard launch flows.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run test -- src/__tests__/services/onboarding-launch.test.ts` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- New users finish onboarding on a chat-enabled surface
- Selected first-action prompts become the first live chat prompt
- Checklist launches work from command-center without hidden-chat dead ends
- `initialPrompt` is parsed safely and consumed once
</success_criteria>

<output>
After completion, create `.planning/phases/54-onboarding-ux-polish/54-01-SUMMARY.md`
</output>
