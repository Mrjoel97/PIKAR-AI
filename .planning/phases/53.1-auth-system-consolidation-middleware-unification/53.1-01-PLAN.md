---
phase: 53.1-auth-system-consolidation-middleware-unification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app_utils/auth.py
  - app/routers/onboarding.py
  - app/routers/action_history.py
  - app/middleware/workspace_role.py
  - app/fast_api_app.py
  - tests/unit/test_distributed_rate_limiter.py
  - tests/unit/app/test_auth_utils.py
autonomous: true
requirements: [AUTH-01, TEAM-04]

must_haves:
  truths:
    - "The canonical FastAPI user-resolution dependency lives in app/app_utils/auth.py"
    - "Onboarding and action-history no longer implement route-local auth verification logic"
    - "Middleware resolves authenticated user identity from bearer JWTs before considering x-user-id style headers"
    - "Rate-limit enforcement no longer depends on spoofable user-id headers for authenticated requests"
    - "Regression tests cover shared auth resolution and bearer-first middleware identity"
  artifacts:
    - path: "app/app_utils/auth.py"
      provides: "Shared FastAPI auth dependency and request identity helpers"
      contains: "get_current_user_id"
    - path: "app/routers/onboarding.py"
      provides: "Compatibility import path for canonical auth dependency without local verification logic"
      contains: "get_current_user_id"
    - path: "app/routers/action_history.py"
      provides: "Action history routes using canonical auth dependency"
      contains: "Depends(get_current_user_id)"
    - path: "app/fast_api_app.py"
      provides: "Bearer-first middleware identity population for logging and distributed rate limiting"
      contains: "RateLimitHeaderMiddleware"
    - path: "tests/unit/app/test_auth_utils.py"
      provides: "Unit coverage for shared auth dependency and request identity resolution"
      contains: "test_get_current_user_id_uses_shared_verify_token"
  key_links:
    - from: "app/fast_api_app.py"
      to: "app/app_utils/auth.py"
      via: "shared request identity helper used before Redis rate-limit enforcement"
      pattern: "resolve_request_user_id"
    - from: "app/routers/onboarding.py"
      to: "app/app_utils/auth.py"
      via: "compatibility re-export of canonical get_current_user_id"
      pattern: "from app\\.app_utils\\.auth import get_current_user_id"
---

<objective>
Unify backend route authentication and middleware identity on one canonical auth path.

Purpose: The backend already has a strong auth utility, but some routes and middleware still bypass it. This plan removes duplicate auth logic, ensures request-state identity comes from bearer tokens, and closes the header-spoofing gap for distributed rate limiting.

Output: shared auth dependency in `app/app_utils/auth.py`, onboarding/action-history compatibility cleanup, bearer-first request identity resolution in middleware, and Python regression tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-CONTEXT.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-RESEARCH.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-VALIDATION.md

@app/app_utils/auth.py
@app/routers/onboarding.py
@app/routers/action_history.py
@app/middleware/workspace_role.py
@app/fast_api_app.py
@app/middleware/rate_limiter.py
@tests/unit/test_distributed_rate_limiter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical FastAPI auth dependency and request identity helpers</name>
  <files>app/app_utils/auth.py, tests/unit/app/test_auth_utils.py</files>
  <action>
1. In `app/app_utils/auth.py`, add canonical dependency helpers built on the existing verification path:
   - `get_current_user(credentials=Security(security)) -> dict`
   - `get_current_user_id(user=Depends(get_current_user)) -> str`
   - request helpers for middleware, such as bearer token extraction and `resolve_request_user_id(request)` that prefers bearer JWT identity and can fall back to headers only when there is no authenticated identity.
2. Reuse existing local JWT verification helpers where possible instead of adding another token verification path.
3. Create `tests/unit/app/test_auth_utils.py` covering at minimum:
   - canonical `get_current_user_id` delegates to the shared verification path
   - bearer token identity is preferred over user-id headers
   - missing/invalid bearer token falls back safely without raising from the request helper
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app/test_auth_utils.py -x</automated>
  </verify>
  <done>Shared auth dependency exists in app_utils with tests proving canonical route identity resolution and bearer-first request user extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate route auth logic with compatibility imports</name>
  <files>app/routers/onboarding.py, app/routers/action_history.py, app/middleware/workspace_role.py</files>
  <action>
1. Remove the route-local auth verification implementation from `app/routers/onboarding.py` and replace it with a compatibility import or re-export from `app.app_utils.auth`.
2. Update `app/routers/action_history.py` to consume the canonical shared auth dependency instead of maintaining its own copy.
3. Update any directly affected middleware/module imports in this plan's write scope so they refer to the shared dependency path clearly.
4. Keep public behavior and endpoint signatures unchanged.
  </action>
  <verify>
    <automated>uv run python -c "from app.routers.onboarding import get_current_user_id; from app.routers.action_history import router; print('auth imports ok')"</automated>
  </verify>
  <done>Onboarding and action-history stop owning auth logic locally and now resolve user identity through the shared auth module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden request-state identity for logging and distributed rate limiting</name>
  <files>app/fast_api_app.py, tests/unit/test_distributed_rate_limiter.py</files>
  <action>
1. Update `app/fast_api_app.py` so request logging and rate-limit enforcement use the shared request identity helper from `app.app_utils.auth`.
2. Ensure authenticated bearer identity cannot be overridden by `x-user-id` / `user-id` headers.
3. Keep header fallback only for non-authenticated/internal cases where no bearer identity exists.
4. Extend `tests/unit/test_distributed_rate_limiter.py` (or companion tests in the same scope) to verify that authenticated requests are keyed by bearer-derived identity rather than header spoofing.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_distributed_rate_limiter.py tests/unit/app/test_auth_utils.py -x</automated>
  </verify>
  <done>Middleware populates request.state.user_id from bearer auth first, and distributed rate limiting no longer depends on spoofable user-id headers for authenticated traffic.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/app/test_auth_utils.py -x` passes
- `uv run pytest tests/unit/test_distributed_rate_limiter.py -x` passes
- `uv run python -c "from app.routers.onboarding import get_current_user_id; from app.routers.action_history import router; print('auth imports ok')"` passes
</verification>

<success_criteria>
- Canonical route auth dependency exists in `app/app_utils/auth.py`
- Onboarding and action-history no longer maintain local user-resolution logic
- Middleware request identity is bearer-first and header spoofing no longer controls authenticated rate-limit identity
- Regression tests exist for both shared auth dependency behavior and middleware identity resolution
</success_criteria>

<output>
After completion, create `.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-01-SUMMARY.md`
</output>
