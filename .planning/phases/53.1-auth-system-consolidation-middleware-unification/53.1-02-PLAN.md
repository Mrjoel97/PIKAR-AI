---
phase: 53.1-auth-system-consolidation-middleware-unification
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/routers/teams_public.py
  - app/services/workspace_service.py
  - app/fast_api_app.py
  - frontend/src/proxy.ts
  - frontend/src/lib/supabase/proxy.ts
  - frontend/src/app/invite/[token]/page.tsx
  - frontend/src/services/api.ts
  - frontend/src/components/auth/ProtectedRoute.tsx
  - frontend/src/__tests__/services/invite-flow.test.ts
autonomous: true
requirements: [AUTH-01, TEAM-02, TEAM-04]

must_haves:
  truths:
    - "The active Next.js proxy uses the hardened shared Supabase proxy helper for auth validation"
    - "Protected-route redirects preserve sanitized returnUrl values"
    - "Invite metadata is served by backend/server-only code, not a frontend service-role route"
    - "The /invite/[token] page fetches public invite metadata from the backend path"
    - "Unused client-side ProtectedRoute wrapper is removed"
  artifacts:
    - path: "frontend/src/proxy.ts"
      provides: "Active Next.js protected-route proxy using shared helper and returnUrl redirects"
      contains: "returnUrl"
    - path: "app/routers/teams_public.py"
      provides: "Public backend endpoint for invite metadata lookup"
      contains: "invites/details"
    - path: "app/services/workspace_service.py"
      provides: "Display-safe invite metadata lookup for public invite page"
      contains: "get_invite_details"
    - path: "frontend/src/app/invite/[token]/page.tsx"
      provides: "Invite page loading metadata from backend public API"
      contains: "fetchPublicApi"
    - path: "frontend/src/__tests__/services/invite-flow.test.ts"
      provides: "Frontend regression coverage for public invite metadata and auth redirect sanitization"
      contains: "returnUrl"
  key_links:
    - from: "frontend/src/proxy.ts"
      to: "frontend/src/lib/supabase/proxy.ts"
      via: "updateSession helper"
      pattern: "updateSession"
    - from: "frontend/src/app/invite/[token]/page.tsx"
      to: "app/routers/teams_public.py"
      via: "public backend invite metadata request"
      pattern: "teams/invites/details"
---

<objective>
Unify the frontend proxy trust path and move privileged invite metadata lookup fully into the backend boundary.

Purpose: Phase 53 added invite acceptance and auth continuation, but the active proxy still duplicates weaker auth logic and the invite metadata path still depends on a frontend service-role route. This plan consolidates both on the approved secure architecture.

Output: hardened active Next.js proxy, protected-route `returnUrl` preservation, public backend invite metadata endpoint, invite page switch to backend public fetch, and cleanup of the unused client guard.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-CONTEXT.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-RESEARCH.md
@.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-VALIDATION.md
@.planning/phases/53-multi-user-teams/53-04-SUMMARY.md

@frontend/src/lib/supabase/proxy.ts
@frontend/src/proxy.ts
@frontend/src/services/auth.ts
@frontend/src/services/api.ts
@frontend/src/app/invite/[token]/page.tsx
@frontend/src/app/api/teams/invite-details/route.ts
@frontend/src/components/auth/ProtectedRoute.tsx
@app/routers/teams.py
@app/services/workspace_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move invite metadata lookup behind a backend-owned public endpoint</name>
  <files>app/services/workspace_service.py, app/routers/teams_public.py, app/fast_api_app.py</files>
  <action>
1. Add a public backend endpoint for invite metadata lookup in a backend-owned router (new file is acceptable) that:
   - accepts `token`
   - validates active/not accepted/not expired status
   - returns only display-safe metadata for the invite page
2. Add a corresponding service method in `app/services/workspace_service.py` (for example `get_invite_details`) so invite lookup logic is not embedded in the router.
3. Include the new router in `app/fast_api_app.py`.
4. Preserve the existing invite acceptance endpoint behavior; this task is only for metadata lookup boundary cleanup.
  </action>
  <verify>
    <automated>uv run python -c "from app.routers.teams_public import router; from app.services.workspace_service import WorkspaceService; print('public invite router ok')"</automated>
  </verify>
  <done>Invite metadata is available from a backend-owned public endpoint backed by workspace service logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate active proxy flow onto the hardened helper with returnUrl preservation</name>
  <files>frontend/src/proxy.ts, frontend/src/lib/supabase/proxy.ts, frontend/src/__tests__/services/invite-flow.test.ts</files>
  <action>
1. Update `frontend/src/proxy.ts` so protected-route auth validation delegates to `updateSession()` from `frontend/src/lib/supabase/proxy.ts`.
2. Preserve the current onboarding/persona routing behavior.
3. When redirecting an unauthenticated user from a protected route, include a sanitized `returnUrl` covering pathname and search.
4. If redirects occur after session refresh, preserve any cookies emitted by the shared proxy helper.
5. Add frontend regression coverage for redirect sanitization and/or helper behavior in `frontend/src/__tests__/services/invite-flow.test.ts`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/invite-flow.test.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The active proxy uses the hardened helper and generic protected-route redirects preserve returnUrl correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Point invite page at backend public metadata and remove legacy client guard</name>
  <files>frontend/src/app/invite/[token]/page.tsx, frontend/src/services/api.ts, frontend/src/components/auth/ProtectedRoute.tsx, frontend/src/__tests__/services/invite-flow.test.ts</files>
  <action>
1. Update `frontend/src/app/invite/[token]/page.tsx` to fetch invite metadata from the backend public endpoint using the existing public API helper instead of `/api/teams/invite-details`.
2. Add or adjust any small frontend API helper needed for public invite fetches while preserving existing patterns.
3. Delete `frontend/src/app/api/teams/invite-details/route.ts` from the active flow if it becomes unused.
4. Remove `frontend/src/components/auth/ProtectedRoute.tsx` if search still shows no references.
5. Extend frontend tests to cover the new invite metadata fetch path.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/invite-flow.test.ts && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The invite page no longer depends on a frontend service-role route, and the unused ProtectedRoute wrapper is removed.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from app.routers.teams_public import router; from app.services.workspace_service import WorkspaceService; print('public invite router ok')"` passes
- `cd frontend && npm run test -- src/__tests__/services/invite-flow.test.ts` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Invite metadata is served from a backend-owned public endpoint
- The active proxy delegates auth validation to the hardened helper
- Protected-route redirects preserve sanitized `returnUrl`
- The invite page uses the backend public metadata path
- The unused ProtectedRoute component is removed from the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/53.1-auth-system-consolidation-middleware-unification/53.1-02-SUMMARY.md`
</output>
