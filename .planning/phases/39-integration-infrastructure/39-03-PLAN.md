---
phase: 39-integration-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - frontend/src/app/dashboard/configuration/page.tsx
  - frontend/src/services/integrations.ts
autonomous: true
requirements: [INFRA-08]

must_haves:
  truths:
    - "Frontend integration configuration page shows connection status for every supported provider"
    - "Providers are grouped by category (CRM & Sales, Finance & Commerce, Productivity, Communication, Analytics)"
    - "Each provider shows 3-state status: green dot (Connected), gray dot (Disconnected), red dot (Error)"
    - "Connect button opens OAuth popup; page refreshes when popup closes"
    - "Disconnect button removes credentials via DELETE /integrations/{provider}"
  artifacts:
    - path: "frontend/src/services/integrations.ts"
      provides: "API client for /integrations endpoints"
      exports: ["fetchProviders", "fetchIntegrationStatus", "disconnectProvider"]
    - path: "frontend/src/app/dashboard/configuration/page.tsx"
      provides: "Updated configuration page with provider category cards"
      min_lines: 200
  key_links:
    - from: "frontend/src/services/integrations.ts"
      to: "frontend/src/services/api.ts"
      via: "fetchWithAuth import"
      pattern: "from.*api.*import.*fetchWithAuth"
    - from: "frontend/src/app/dashboard/configuration/page.tsx"
      to: "frontend/src/services/integrations.ts"
      via: "provider and status fetching"
      pattern: "from.*services/integrations"
---

<objective>
Add integration provider cards to the frontend configuration page, showing per-provider connection status with category grouping and OAuth popup flow.

Purpose: INFRA-08 requires the configuration page to show connection status for all supported providers. This depends on Plan 39-01 which creates the backend /integrations/providers and /integrations/status endpoints.

Output: Updated configuration page with category card layout, integrations service client, OAuth popup handling.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-integration-infrastructure/39-CONTEXT.md
@.planning/phases/39-integration-infrastructure/39-RESEARCH.md

@frontend/src/app/dashboard/configuration/page.tsx
@frontend/src/services/api.ts

<interfaces>
Backend endpoints (from Plan 39-01):
- GET /integrations/providers → [{key, name, auth_type, category, icon_url, scopes}]
- GET /integrations/status → [{provider, name, category, connected, account_name, last_sync_at, error_count, last_error}]
- GET /integrations/{provider}/authorize → Redirect to OAuth consent (user opens in popup)
- GET /integrations/{provider}/callback → Processes OAuth, sends postMessage to opener, closes popup
- DELETE /integrations/{provider} → {disconnected: true, provider}

Category values: "crm_sales", "finance_commerce", "productivity", "analytics", "communication"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration service client + provider category cards on configuration page</name>
  <files>
    frontend/src/services/integrations.ts,
    frontend/src/app/dashboard/configuration/page.tsx
  </files>
  <action>
    1. **Create integrations service** (`frontend/src/services/integrations.ts`):
       - `fetchProviders()` → GET /integrations/providers via `fetchWithAuth`
       - `fetchIntegrationStatus()` → GET /integrations/status via `fetchWithAuth`
       - `disconnectProvider(provider: string)` → DELETE /integrations/{provider} via `fetchWithAuth`
       - Type interfaces: `IntegrationProvider`, `IntegrationStatus`

    2. **Add provider card section to configuration page** (`frontend/src/app/dashboard/configuration/page.tsx`):
       Add a new "Integrations" section ABOVE the existing Google Workspace / Social / MCP sections.

       Structure:
       - Fetch providers and status on mount via the new service
       - Group providers by `category` using a category label map:
         ```
         crm_sales → "CRM & Sales"
         finance_commerce → "Finance & Commerce"
         productivity → "Productivity"
         communication → "Communication"
         analytics → "Analytics"
         ```
       - For each category, render a section header + card grid (2-3 columns)
       - Each provider card shows:
         - Provider icon (from `icon_url` or a lucide icon fallback based on provider key)
         - Provider name
         - Status dot: green (`CheckCircle2`) if connected, gray (`XCircle`) if disconnected, red (`AlertCircle`) if error_count > 0
         - If connected: account name + "Disconnect" button
         - If disconnected: "Connect" button
         - Click to expand: last sync timestamp, error message if any

    3. **OAuth popup flow:**
       - "Connect" button calls `window.open(backendUrl + '/integrations/{provider}/authorize', 'oauth-popup', 'width=600,height=700')`
       - Add `useEffect` with `window.addEventListener('message', handler)` to listen for `{type: 'oauth-callback', provider, success}` from the popup
       - On successful callback: re-fetch integration status to update cards
       - Clean up event listener on unmount

    4. **Disconnect flow:**
       - "Disconnect" button calls `disconnectProvider(provider)` then re-fetches status
       - Show brief loading state during the API call

    5. **Styling:**
       - Follow existing design patterns in the configuration page (rounded-[28px], shadow-[0_18px_60px_-30px_...], teal/slate color system)
       - Cards use motion.div with entrance animations (consistent with other dashboard cards)
       - Status dots: `text-emerald-500` (connected), `text-slate-400` (disconnected), `text-red-500` (error)
       - Responsive: 1 column on mobile, 2 on tablet, 3 on desktop
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - integrations.ts service with fetchProviders, fetchIntegrationStatus, disconnectProvider
    - Configuration page shows provider cards grouped by category
    - Each card shows 3-state status dot (green/gray/red)
    - Connect opens OAuth popup, page refreshes on callback
    - Disconnect removes credentials and updates UI
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` compiles without errors
- Configuration page renders provider sections above existing sections
- Provider cards show correct grouping by category
</verification>

<success_criteria>
- Frontend configuration page displays all supported providers with real-time connection status
- OAuth popup flow works end-to-end (button → popup → callback → status refresh)
- Disconnect button removes credentials and updates the UI
- TypeScript clean, consistent with existing design system
</success_criteria>

<output>
After completion, create `.planning/phases/39-integration-infrastructure/39-03-SUMMARY.md`
</output>
