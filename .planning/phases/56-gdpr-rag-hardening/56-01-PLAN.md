---
phase: 56-gdpr-rag-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/personal_data_export_service.py
  - app/routers/account.py
  - frontend/src/app/settings/page.tsx
  - tests/unit/services/test_personal_data_export_service.py
  - tests/unit/app/routers/test_account_router.py
autonomous: true
requirements: [GDPR-01]

must_haves:
  truths:
    - "An authenticated user can request a full personal-data export from the account/settings surface and receive a downloadable signed archive"
    - "The export bundle covers the main user-owned domains the app currently stores: profile/account metadata, sessions, initiatives, workflows, content, Knowledge Vault records, integrations metadata, and other first-party business records discovered during implementation"
    - "Sensitive secrets are excluded or redacted: encrypted credential blobs, OAuth tokens, API keys, and other backend-only secrets are never dumped into the archive"
    - "Regression tests cover the archive manifest/content rules and the authenticated router contract"
  artifacts:
    - path: "app/services/personal_data_export_service.py"
      provides: "Backend-owned privacy export bundle builder and signed archive uploader"
      contains: "PersonalDataExportService"
    - path: "app/routers/account.py"
      provides: "Authenticated account export endpoint"
      contains: "export"
    - path: "frontend/src/app/settings/page.tsx"
      provides: "User-facing privacy export action in Settings"
      contains: "Export"
    - path: "tests/unit/services/test_personal_data_export_service.py"
      provides: "Coverage for archive scope and redaction rules"
      contains: "PersonalDataExportService"
    - path: "tests/unit/app/routers/test_account_router.py"
      provides: "Router contract coverage for authenticated export"
      contains: "account"
  key_links:
    - from: "app/routers/account.py"
      to: "app/services/personal_data_export_service.py"
      via: "authenticated export request creates a signed archive"
      pattern: "export"
    - from: "frontend/src/app/settings/page.tsx"
      to: "app/routers/account.py"
      via: "privacy export CTA"
      pattern: "/account"
---

<objective>
Deliver the missing self-service personal-data export required for GDPR-01.

Purpose: The repo already has deletion and single-table CSV export primitives, but users still do not have one truthful way to export all personal/business data the product stores for them. This plan adds that account-level export without exposing secrets or moving privileged behavior into the browser.

Output: backend-owned export bundle service, authenticated export endpoint, and a Settings entrypoint for users.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-gdpr-rag-hardening/56-CONTEXT.md
@.planning/phases/56-gdpr-rag-hardening/56-RESEARCH.md
@.planning/phases/56-gdpr-rag-hardening/56-VALIDATION.md

@app/routers/account.py
@app/services/data_export_service.py
@app/routers/data_io.py
@frontend/src/app/settings/page.tsx
@tests/unit/services/test_data_export_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a backend-owned personal data export bundle service</name>
  <files>app/services/personal_data_export_service.py, tests/unit/services/test_personal_data_export_service.py</files>
  <action>
1. Create `PersonalDataExportService` that gathers the authenticated user's exportable data domains into one downloadable archive.
2. Use the existing signed-storage pattern already proven by `DataExportService`; do not invent a second delivery mechanism.
3. Include a manifest describing each exported section and the generated timestamp.
4. Exclude or redact raw secrets and backend-only sensitive fields, including:
   - OAuth access/refresh tokens
   - encrypted credential blobs
   - API keys or secret env-derived material
5. Add unit tests proving:
   - the service includes the intended top-level sections
   - excluded/redacted fields never leak into the archive
   - empty sections are handled gracefully rather than crashing export generation
  </action>
  <verify>
    <automated>uv run pytest tests/unit/services/test_personal_data_export_service.py -x</automated>
  </verify>
  <done>A full privacy export archive can be generated safely on the backend with deterministic coverage for redaction rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an authenticated account export endpoint</name>
  <files>app/routers/account.py, tests/unit/app/routers/test_account_router.py</files>
  <action>
1. Add a new authenticated export route under the existing account router so privacy actions stay together.
2. Return a truthful response contract containing the signed download URL and any lightweight export metadata needed by the UI.
3. Keep the route backend-owned: the browser should trigger export, not assemble the bundle itself.
4. Add router tests covering:
   - authenticated success
   - service error handling
   - scoping to the current authenticated user
  </action>
  <verify>
    <automated>uv run pytest tests/unit/app/routers/test_account_router.py -k export -x</automated>
  </verify>
  <done>The account router exposes one authenticated export entrypoint with regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Settings privacy surface to the new export flow</name>
  <files>frontend/src/app/settings/page.tsx</files>
  <action>
1. Add a clear privacy export action to the existing Settings / Account area beside the delete-account controls.
2. Keep the UX honest:
   - explain that the export is generated server-side
   - surface loading/success/error states
   - avoid implying raw secrets will be included
3. Reuse the existing authenticated fetch pattern already used elsewhere in Settings.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>Users can trigger their privacy export from Settings without any frontend secret exposure.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/services/test_personal_data_export_service.py -x` passes
- `uv run pytest tests/unit/app/routers/test_account_router.py -k export -x` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Users can request a full personal-data export through an authenticated account surface
- The export archive is downloadable through a signed backend-owned link
- Sensitive secrets are excluded or redacted
- Regression tests cover the archive rules and router contract
</success_criteria>

<output>
After completion, create `.planning/phases/56-gdpr-rag-hardening/56-01-SUMMARY.md`
</output>
