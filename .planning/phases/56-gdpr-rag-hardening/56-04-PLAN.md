---
phase: 56-gdpr-rag-hardening
plan: "04"
type: execute
wave: 4
depends_on: ["56-03"]
files_modified:
  - tests/eval_datasets/knowledge_vault_eval.json
  - tests/rag/run_knowledge_vault_eval.py
  - tests/integration/test_knowledge_vault.py
  - tests/integration/test_rag_services.py
  - app/rag/knowledge_vault.py
  - app/rag/search_service.py
autonomous: true
requirements: [RAG-01, RAG-02, RAG-03]

must_haves:
  truths:
    - "Knowledge Vault relevance is measured against a named eval dataset and fails if the threshold drops below 80 percent"
    - "Typical user-vault search latency is measured through the real search path and fails above 2 seconds"
    - "Concurrent ingestion and search are regression-tested for corruption, race, or deadlock behavior"
    - "The final RAG contract is machine-checkable rather than a manual judgment call"
  artifacts:
    - path: "tests/eval_datasets/knowledge_vault_eval.json"
      provides: "Named relevance dataset for user-vault search"
      contains: "queries"
    - path: "tests/rag/run_knowledge_vault_eval.py"
      provides: "Threshold-based relevance and latency evaluator"
      contains: "min-relevance"
    - path: "tests/integration/test_knowledge_vault.py"
      provides: "Knowledge Vault concurrency and retrieval regression coverage"
      contains: "search"
    - path: "tests/integration/test_rag_services.py"
      provides: "RAG service integration coverage under the governed contract"
      contains: "knowledge_vault"
  key_links:
    - from: "tests/rag/run_knowledge_vault_eval.py"
      to: "app/rag/search_service.py"
      via: "measured search path"
      pattern: "search"
    - from: "tests/integration/test_knowledge_vault.py"
      to: "app/rag/knowledge_vault.py"
      via: "concurrent ingestion + search regression scenarios"
      pattern: "ingest"
---

<objective>
Turn the Knowledge Vault requirements into a governed, repeatable RAG pass/fail contract.

Purpose: After auth and ingestion correctness are fixed, v7 still needs proof that retrieval is relevant enough, fast enough, and safe under concurrent use. This plan creates the threshold-based dataset/runner/tests needed to actually close RAG-01, RAG-02, and RAG-03.

Output: named relevance dataset, machine-checkable eval runner, latency guardrail, and concurrent ingestion/search regression coverage.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-gdpr-rag-hardening/56-CONTEXT.md
@.planning/phases/56-gdpr-rag-hardening/56-RESEARCH.md
@.planning/phases/56-gdpr-rag-hardening/56-VALIDATION.md

@app/rag/knowledge_vault.py
@app/rag/search_service.py
@app/rag/ingestion_service.py
@tests/unit/test_rag_services.py
@tests/integration/test_rag_services.py
@tests/integration/test_knowledge_vault.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a named Knowledge Vault relevance dataset and threshold runner</name>
  <files>tests/eval_datasets/knowledge_vault_eval.json, tests/rag/run_knowledge_vault_eval.py</files>
  <action>
1. Create a small fixed dataset of representative documents, queries, and expected relevance targets for the user-vault path.
2. Implement a script that:
   - ingests or references the fixture documents
   - runs the real search path
   - computes pass/fail relevance results
   - measures latency per query
   - exits non-zero if relevance drops below `0.8` or latency exceeds `2000ms`
3. Keep the output machine-readable so the phase can be re-run on demand.
  </action>
  <verify>
    <automated>uv run python tests/rag/run_knowledge_vault_eval.py --dataset tests/eval_datasets/knowledge_vault_eval.json --min-relevance 0.8 --max-latency-ms 2000</automated>
  </verify>
  <done>The repo has one named RAG dataset and a threshold-based runner instead of an undocumented qualitative benchmark.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add concurrent ingestion + search regression coverage</name>
  <files>tests/integration/test_knowledge_vault.py, tests/integration/test_rag_services.py</files>
  <action>
1. Extend existing integration coverage to exercise overlapping ingestion and search against the same user-vault space.
2. Verify there is no corruption, duplicate/invalid embedding state, or deadlock/hang under the targeted concurrent scenarios.
3. Keep the tests focused on the real v7 seams rather than broad synthetic load generation.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_knowledge_vault.py tests/integration/test_rag_services.py -k "concurrent or search or latency" -x</automated>
  </verify>
  <done>Concurrent vault ingestion/search behavior is covered by automated regressions instead of assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Make the smallest retrieval/performance fixes required by the new contract</name>
  <files>app/rag/knowledge_vault.py, app/rag/search_service.py</files>
  <action>
1. Run the new eval/tests and identify any concrete relevance or latency regressions they expose.
2. Make only the smallest code changes needed to satisfy the governed thresholds.
3. Keep the final command(s) documented in code comments or script help so reruns are straightforward for future v7 follow-up.
  </action>
  <verify>
    <automated>uv run python tests/rag/run_knowledge_vault_eval.py --dataset tests/eval_datasets/knowledge_vault_eval.json --min-relevance 0.8 --max-latency-ms 2000 && uv run pytest tests/integration/test_knowledge_vault.py tests/integration/test_rag_services.py -k "concurrent or search or latency" -x</automated>
  </verify>
  <done>The governed RAG contract passes without needing a parallel benchmark system outside GSD.</done>
</task>

</tasks>

<verification>
- `uv run python tests/rag/run_knowledge_vault_eval.py --dataset tests/eval_datasets/knowledge_vault_eval.json --min-relevance 0.8 --max-latency-ms 2000` passes
- `uv run pytest tests/integration/test_knowledge_vault.py tests/integration/test_rag_services.py -k "concurrent or search or latency" -x` passes
</verification>

<success_criteria>
- Relevance is measured and passes the 80 percent threshold
- Search latency is measured and stays below 2 seconds on the defined dataset
- Concurrent ingestion/search is regression-tested
- The final RAG contract is repeatable and machine-checkable
</success_criteria>

<output>
After completion, create `.planning/phases/56-gdpr-rag-hardening/56-04-SUMMARY.md`
</output>
