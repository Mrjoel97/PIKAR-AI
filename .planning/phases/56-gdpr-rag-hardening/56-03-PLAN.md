---
phase: 56-gdpr-rag-hardening
plan: "03"
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - app/services/document_text_extraction.py
  - app/services/knowledge_service.py
  - app/routers/vault.py
  - frontend/src/app/api/vault/search/route.ts
  - frontend/src/app/api/vault/process/route.ts
  - frontend/src/components/vault/VaultInterface.tsx
  - tests/unit/services/test_document_text_extraction.py
  - tests/unit/app/routers/test_vault_router.py
  - frontend/src/__tests__/services/vault-proxy.test.ts
autonomous: true
requirements: [RAG-01]

must_haves:
  truths:
    - "Next.js vault proxy routes forward bearer auth to the backend instead of relying on browser-session presence alone"
    - "The backend vault route keeps authenticated token identity authoritative and rejects cross-user access even if a body user_id is supplied"
    - "Searchable user-vault document types (at minimum TXT, MD, PDF, DOCX) are parsed through file-type-aware extraction rather than raw byte decoding"
    - "The UI truthfully distinguishes searchable formats from storage-only formats if some uploads are not embedded in this phase"
  artifacts:
    - path: "app/services/document_text_extraction.py"
      provides: "Shared MIME-aware text extraction for vault ingestion"
      contains: "extract"
    - path: "app/routers/vault.py"
      provides: "User-vault processing route backed by shared extraction"
      contains: "process"
    - path: "frontend/src/app/api/vault/search/route.ts"
      provides: "Authenticated search proxy that forwards bearer tokens"
      contains: "Authorization"
    - path: "frontend/src/app/api/vault/process/route.ts"
      provides: "Authenticated process proxy that forwards bearer tokens"
      contains: "Authorization"
    - path: "frontend/src/__tests__/services/vault-proxy.test.ts"
      provides: "Regression coverage for vault proxy auth forwarding"
      contains: "vault"
    - path: "tests/unit/services/test_document_text_extraction.py"
      provides: "Coverage for PDF/DOCX/text extraction behavior"
      contains: "extract"
  key_links:
    - from: "frontend/src/app/api/vault/search/route.ts"
      to: "app/routers/vault.py"
      via: "bearer-authenticated search request"
      pattern: "Authorization"
    - from: "frontend/src/app/api/vault/process/route.ts"
      to: "app/routers/vault.py"
      via: "bearer-authenticated process request"
      pattern: "Authorization"
    - from: "app/routers/vault.py"
      to: "app/services/document_text_extraction.py"
      via: "shared extraction before embedding ingestion"
      pattern: "extract"
---

<objective>
Make the user-facing Knowledge Vault path truthful and secure before performance evaluation begins.

Purpose: The current vault experience has two real gaps: the active Next.js proxy routes are not forwarding bearer auth to the backend, and the processing path still treats many uploaded document types like raw text bytes. This plan fixes those seams and aligns the UI with what is actually searchable.

Output: hardened vault proxy auth, shared document extraction, truthful vault-processing behavior, and regression coverage.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-gdpr-rag-hardening/56-CONTEXT.md
@.planning/phases/56-gdpr-rag-hardening/56-RESEARCH.md
@.planning/phases/56-gdpr-rag-hardening/56-VALIDATION.md

@app/routers/vault.py
@app/services/knowledge_service.py
@app/rag/knowledge_vault.py
@frontend/src/app/api/vault/search/route.ts
@frontend/src/app/api/vault/process/route.ts
@frontend/src/components/vault/VaultInterface.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create one shared document-text extraction path for searchable vault formats</name>
  <files>app/services/document_text_extraction.py, app/services/knowledge_service.py, app/routers/vault.py, tests/unit/services/test_document_text_extraction.py, tests/unit/app/routers/test_vault_router.py</files>
  <action>
1. Extract the MIME-aware parsing logic needed for user-vault ingestion into one shared helper.
2. Support the searchable text/document formats the user vault should truthfully claim during v7, at minimum:
   - plain text
   - markdown
   - PDF
   - DOCX
3. Update the vault processing route to use this helper before chunking/embedding.
4. Reuse the same helper from any existing admin knowledge path that currently duplicates extraction behavior.
5. Add unit tests covering:
   - successful extraction for text/PDF/DOCX
   - unsupported format behavior
   - backend route handling when extraction fails
  </action>
  <verify>
    <automated>uv run pytest tests/unit/services/test_document_text_extraction.py tests/unit/app/routers/test_vault_router.py -x</automated>
  </verify>
  <done>User-vault ingestion no longer relies on raw byte decode for searchable document types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the authenticated vault proxy routes to forward bearer auth</name>
  <files>frontend/src/app/api/vault/search/route.ts, frontend/src/app/api/vault/process/route.ts, frontend/src/__tests__/services/vault-proxy.test.ts</files>
  <action>
1. Update both active vault proxy routes so backend calls include `Authorization: Bearer ${session.access_token}`.
2. Keep the backend route authoritative; the frontend proxy should forward auth, not become the new trust boundary.
3. Add vitest coverage proving:
   - authenticated requests forward bearer auth
   - unauthenticated requests fail cleanly
   - body `user_id` does not replace the token-based auth contract
  </action>
  <verify>
    <automated>cd frontend && npm run test -- src/__tests__/services/vault-proxy.test.ts</automated>
  </verify>
  <done>The active user-vault proxy path now preserves the backend auth boundary and has regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Align VaultInterface with the truthful searchable-format contract</name>
  <files>frontend/src/components/vault/VaultInterface.tsx</files>
  <action>
1. Review the upload/process copy and status messaging in `VaultInterface`.
2. If some formats remain non-searchable in this phase (for example image/video uploads), label them as storage-only or pending richer extraction rather than implying they were embedded.
3. Surface meaningful processing failures so users do not think a document is searchable when ingestion actually failed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The Knowledge Vault UI accurately reflects which uploads become searchable during v7.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/services/test_document_text_extraction.py tests/unit/app/routers/test_vault_router.py -x` passes
- `cd frontend && npm run test -- src/__tests__/services/vault-proxy.test.ts` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Active vault proxy routes forward bearer auth
- Backend vault processing uses shared file-type-aware extraction
- Searchable formats are handled truthfully
- The UI no longer over-promises storage-only formats as searchable
</success_criteria>

<output>
After completion, create `.planning/phases/56-gdpr-rag-hardening/56-03-SUMMARY.md`
</output>
