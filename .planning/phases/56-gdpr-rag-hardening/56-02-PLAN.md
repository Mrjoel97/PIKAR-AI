---
phase: 56-gdpr-rag-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - supabase/migrations/20260411153000_gdpr_deletion_hardening.sql
  - app/routers/account.py
  - frontend/src/app/settings/page.tsx
  - tests/integration/test_account_deletion_cascade.py
  - tests/unit/app/routers/test_account_router.py
autonomous: true
requirements: [GDPR-02, GDPR-03]

must_haves:
  truths:
    - "Account deletion removes or neutralizes every current user-linked row that would otherwise orphan or block auth.users deletion"
    - "Governance/history records that must survive are anonymized rather than leaving directly identifying actor references behind"
    - "data_deletion_requests remains the durable audit trail for the deletion request itself"
    - "Automated proof exists for both deletion cascade behavior and audit-log anonymization expectations"
  artifacts:
    - path: "supabase/migrations/20260411153000_gdpr_deletion_hardening.sql"
      provides: "Schema-accurate delete_user_account hardening and audit anonymization"
      contains: "delete_user_account"
    - path: "app/routers/account.py"
      provides: "Truthful deletion response and status contract"
      contains: "delete"
    - path: "tests/integration/test_account_deletion_cascade.py"
      provides: "Deletion cascade proof against representative user-linked data"
      contains: "delete_user_account"
    - path: "tests/unit/app/routers/test_account_router.py"
      provides: "Router contract coverage for failure/success paths"
      contains: "delete"
  key_links:
    - from: "app/routers/account.py"
      to: "supabase/migrations/20260411153000_gdpr_deletion_hardening.sql"
      via: "account deletion RPC path"
      pattern: "delete_user_account"
    - from: "tests/integration/test_account_deletion_cascade.py"
      to: "governance_audit_log"
      via: "post-deletion anonymization and no-orphan verification"
      pattern: "governance_audit_log"
---

<objective>
Harden account deletion so the current schema truly satisfies GDPR-02 and GDPR-03.

Purpose: The repo already has a deletion path, but the SQL truth source predates newer tables and the governance audit system. This plan refreshes the real cascade/anonymization behavior and proves it with targeted automated coverage.

Output: hardened deletion migration, truthful account deletion contract, and automated proof for cascade + anonymization.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-gdpr-rag-hardening/56-CONTEXT.md
@.planning/phases/56-gdpr-rag-hardening/56-RESEARCH.md
@.planning/phases/56-gdpr-rag-hardening/56-VALIDATION.md

@supabase/migrations/20260316000000_data_deletion.sql
@supabase/migrations/20260403300000_enterprise_governance.sql
@app/routers/account.py
@app/routers/admin/governance_audit.py
@frontend/src/app/settings/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh the database deletion function against the current schema</name>
  <files>supabase/migrations/20260411153000_gdpr_deletion_hardening.sql</files>
  <action>
1. Inventory all current `auth.users(id)` references and other user-linked tables added after the original deletion migration.
2. Create a follow-up migration that hardens `delete_user_account()` so it handles the newly discovered tables instead of relying on the March snapshot.
3. Add explicit anonymization handling for surviving governance/history rows where v7 requires the audit trail to remain useful without preserving personal identity.
4. Preserve the existing `data_deletion_requests` audit row behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_account_deletion_cascade.py -x</automated>
  </verify>
  <done>The database truth source for account deletion is refreshed for the current schema and surviving audit/history rows are anonymized correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add automated proof for cascade completeness and audit anonymization</name>
  <files>tests/integration/test_account_deletion_cascade.py, tests/unit/app/routers/test_account_router.py</files>
  <action>
1. Add an integration test that seeds representative user-linked rows across the scoped tables, runs the deletion path, and proves no orphaned rows remain in the covered domains.
2. Explicitly verify the post-deletion state of governance/history rows:
   - they still render/usefully exist where intended
   - directly identifying actor references are anonymized per the new contract
3. Keep a lightweight router-level unit test for API success/failure behavior and deletion status response expectations.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_account_deletion_cascade.py tests/unit/app/routers/test_account_router.py -k deletion -x</automated>
  </verify>
  <done>Deletion behavior is proven by automated tests instead of assumed from the migration text alone.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the Settings deletion UX truthful about scope and status</name>
  <files>app/routers/account.py, frontend/src/app/settings/page.tsx</files>
  <action>
1. Refresh any deletion response/copy that currently over-promises or omits the anonymized-audit behavior and completion confirmation flow.
2. Keep the delete action clearly irreversible and scoped to the authenticated account.
3. If a confirmation/status link or message needs small adjustments for the hardened flow, make them here rather than leaving the UI stale.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p . --noEmit</automated>
  </verify>
  <done>The deletion UX matches the hardened backend behavior instead of describing the older March-era contract.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_account_deletion_cascade.py -x` passes
- `uv run pytest tests/unit/app/routers/test_account_router.py -k deletion -x` passes
- `cd frontend && npx tsc -p . --noEmit` passes
</verification>

<success_criteria>
- Account deletion cascades correctly through the current user-linked schema
- Governance/history rows are anonymized where they must survive
- `data_deletion_requests` remains truthful and durable
- Automated proof exists for no-orphan and anonymization behavior
</success_criteria>

<output>
After completion, create `.planning/phases/56-gdpr-rag-hardening/56-02-SUMMARY.md`
</output>
