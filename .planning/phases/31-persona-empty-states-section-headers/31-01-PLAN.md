---
phase: 31-persona-empty-states-section-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/personas/personaEmptyStates.ts
  - frontend/src/components/widgets/PersonaEmptyState.tsx
  - frontend/src/components/widgets/RevenueChart.tsx
  - frontend/src/components/widgets/MorningBriefing.tsx
  - frontend/src/components/widgets/KanbanWidget.tsx
  - frontend/src/components/widgets/CampaignHubWidget.tsx
  - frontend/src/components/widgets/InitiativeDashboard.tsx
  - frontend/src/components/widgets/WorkflowObservabilityWidget.tsx
  - frontend/src/components/widgets/DepartmentActivityWidget.tsx
  - frontend/src/components/widgets/BoardroomWidget.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "When a solopreneur's Revenue Chart has no data, it shows 'Connect Stripe to track your cash flow' with a link to /settings/integrations"
    - "When a startup's Initiative Dashboard has no initiatives, it shows 'Create your first growth experiment' with a link to /dashboard/initiatives/new"
    - "Empty state messages reference the persona's core objectives and KPIs — not generic text"
    - "All empty states include a primary CTA button that routes to the most relevant setup/creation page"
  artifacts:
    - path: "frontend/src/components/personas/personaEmptyStates.ts"
      provides: "Per-persona, per-widget-type empty state config (headline, description, CTA label, CTA href, icon, gradient)"
      exports: ["PERSONA_EMPTY_STATES", "getPersonaEmptyState"]
    - path: "frontend/src/components/widgets/PersonaEmptyState.tsx"
      provides: "Shared empty state component with icon, headline, description, CTA button using persona accent colors"
      exports: ["PersonaEmptyState"]
  key_links:
    - from: "frontend/src/components/widgets/RevenueChart.tsx"
      to: "PersonaEmptyState"
      via: "import and render when data.values is empty or all zeros"
      pattern: "PersonaEmptyState.*revenue_chart"
    - from: "frontend/src/components/widgets/InitiativeDashboard.tsx"
      to: "PersonaEmptyState"
      via: "import and render when initiatives array is empty"
      pattern: "PersonaEmptyState.*initiative_dashboard"
---

<objective>
Create persona-tailored empty states for all 8 default widget types, replacing generic "No data" messages with actionable setup prompts that reference each persona's core objectives and include a CTA button linking to the relevant setup page.

Purpose: New users see helpful, persona-specific guidance instead of blank widgets — driving them toward the first meaningful action (connect Stripe, create initiative, etc.)
Output: personaEmptyStates.ts config, PersonaEmptyState.tsx component, 8 updated widget components
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/personas/personaShellConfig.ts
@frontend/src/components/personas/personaWidgetDefaults.ts
@frontend/src/components/ui/EmptyState.tsx
@frontend/src/contexts/PersonaContext.tsx
@frontend/src/components/widgets/WidgetRegistry.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/personas/personaShellConfig.ts:
```typescript
export interface PersonaShellConfig {
  key: string;
  label: string;
  tagline: string;
  description: string;
  gradient: string;       // e.g. 'from-blue-600 via-teal-500 to-cyan-400'
  accentColor: string;    // e.g. 'text-blue-600'
  bgColor: string;
  headerIcon: string;
  quickActions: QuickAction[];
  kpiLabels: string[];    // e.g. ['Cash Collected', 'Weekly Pipeline', 'Content Consistency']
}
```

From frontend/src/contexts/PersonaContext.tsx:
```typescript
type Persona = 'solopreneur' | 'startup' | 'sme' | 'enterprise' | null;
export function usePersona(): { persona: Persona; isLoading: boolean; userId: string | null; ... }
```

From frontend/src/components/widgets/WidgetRegistry.tsx:
```typescript
export interface WidgetProps {
  definition: WidgetDefinition;
  onAction?: (action: string, payload?: unknown) => void;
  onDismiss?: () => void;
}
```

From frontend/src/components/ui/EmptyState.tsx (existing pattern to follow):
```typescript
interface EmptyStateProps {
  icon: LucideIcon;
  title: string;
  description: string;
  actionLabel?: string;
  onAction?: () => void;
  gradient?: string;
}
// Uses motion.div for fade-in, rounded-[28px] container, gradient icon background, teal CTA button
```

Persona accent colors (from personaShellConfig):
- solopreneur: gradient 'from-blue-600 via-teal-500 to-cyan-400'
- startup: gradient 'from-indigo-600 via-violet-500 to-purple-400'
- sme: gradient 'from-emerald-600 via-green-500 to-lime-400'
- enterprise: gradient 'from-slate-700 via-slate-600 to-slate-500'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create persona empty state config and shared component</name>
  <files>
    frontend/src/components/personas/personaEmptyStates.ts
    frontend/src/components/widgets/PersonaEmptyState.tsx
  </files>
  <action>
**Create `frontend/src/components/personas/personaEmptyStates.ts`:**

Define an `EmptyStateConfig` interface with fields: `icon` (string — lucide icon name), `headline` (string), `description` (string), `ctaLabel` (string), `ctaHref` (string).

Create a nested config: `PERSONA_EMPTY_STATES: Record<PersonaKey, Partial<Record<WidgetType, EmptyStateConfig>>>` with the following entries:

**solopreneur:**
- `revenue_chart`: icon='dollar-sign', headline='Track your cash flow', description='Connect Stripe to track your cash flow and see revenue trends at a glance.', ctaLabel='Connect Stripe', ctaHref='/settings/integrations'
- `morning_briefing`: icon='sun', headline='Your daily command center', description='Once your agents start working, your morning briefing will summarize pending actions and team status.', ctaLabel='Chat with your agent', ctaHref='/solopreneur'
- `kanban_board`: icon='layout-grid', headline='Organize your pipeline', description='Ask your agent to create a task board to track deals, content, and projects in one view.', ctaLabel='Create a board', ctaHref='/solopreneur'
- `campaign_hub`: icon='megaphone', headline='Launch your first campaign', description='Plan and track marketing campaigns — from content drafts to published posts — all in one hub.', ctaLabel='Start a campaign', ctaHref='/dashboard/content'

**startup:**
- `revenue_chart`: icon='trending-up', headline='Monitor your MRR', description='Connect Stripe to track MRR growth, activation rates, and revenue experiments.', ctaLabel='Connect Stripe', ctaHref='/settings/integrations'
- `morning_briefing`: icon='sun', headline='Your daily growth pulse', description='Your briefing will surface experiment results, pending approvals, and team blockers each morning.', ctaLabel='Chat with your agent', ctaHref='/startup'
- `initiative_dashboard`: icon='rocket', headline='Create your first growth experiment', description='Launch initiatives to test hypotheses, track progress across phases, and measure what moves the needle.', ctaLabel='Create initiative', ctaHref='/dashboard/initiatives/new'
- `workflow_observability`: icon='activity', headline='See your automation health', description='Once workflows are running, monitor success rates, failure patterns, and execution times here.', ctaLabel='Browse templates', ctaHref='/dashboard/workflows/templates'

**sme:**
- `revenue_chart`: icon='bar-chart-3', headline='Revenue at a glance', description='Connect Stripe to monitor revenue health, margins, and period comparisons across your departments.', ctaLabel='Connect Stripe', ctaHref='/settings/integrations'
- `morning_briefing`: icon='sun', headline='Your operational briefing', description='A daily summary of department performance, pending actions, and system status will appear here.', ctaLabel='Chat with your agent', ctaHref='/sme'
- `department_activity`: icon='building-2', headline='Set up departments', description='Configure your departments to see real-time activity feeds, decision logs, and workflow triggers.', ctaLabel='Configure departments', ctaHref='/departments'
- `workflow_observability`: icon='activity', headline='Monitor process health', description='Track workflow execution across departments — success rates, cycle times, and failing steps.', ctaLabel='Browse templates', ctaHref='/dashboard/workflows/templates'

**enterprise:**
- `revenue_chart`: icon='dollar-sign', headline='Portfolio revenue view', description='Connect Stripe to see consolidated revenue, plan distribution, and cross-portfolio financial health.', ctaLabel='Connect Stripe', ctaHref='/settings/integrations'
- `morning_briefing`: icon='sun', headline='Executive daily digest', description='Your briefing will cover governance items, approval queues, risk flags, and reporting quality each morning.', ctaLabel='Chat with your agent', ctaHref='/enterprise'
- `department_activity`: icon='building-2', headline='Department oversight', description='Activate departments to see activity feeds, decision audit trails, and operational pulse across the organization.', ctaLabel='Configure departments', ctaHref='/departments'
- `boardroom`: icon='users', headline='Convene the boardroom', description='Ask your agent to run a strategic debate — the AI boardroom simulates CMO, CFO, and CEO perspectives on your proposals.', ctaLabel='Start a debate', ctaHref='/enterprise'

Export a helper function `getPersonaEmptyState(persona: string | null, widgetType: string): EmptyStateConfig | null` that looks up the config.

**Create `frontend/src/components/widgets/PersonaEmptyState.tsx`:**

A 'use client' component that:
1. Accepts props: `widgetType: string` and optional `className?: string`
2. Uses `usePersona()` to get the current persona
3. Calls `getPersonaEmptyState(persona, widgetType)` to get config
4. If no config found, renders a generic fallback: "No data yet" with no CTA
5. If config found, renders:
   - A container similar to the existing `EmptyState.tsx` pattern: `flex flex-col items-center justify-center` with `py-12 px-6`
   - An icon from lucide-react resolved dynamically from the config icon name (import all icons from 'lucide-react' and use a lookup map for the 8 icons used: DollarSign, Sun, LayoutGrid, Megaphone, TrendingUp, Rocket, Activity, BarChart3, Building2, Users)
   - Icon rendered inside a rounded-2xl div with gradient background from `PERSONA_SHELL_CONFIG[persona].gradient`
   - Headline as `h3` with `text-lg font-semibold text-slate-900`
   - Description as `p` with `text-sm text-slate-500 text-center max-w-xs`
   - CTA as a Next.js `Link` styled as a button with `rounded-2xl bg-teal-600 px-5 py-2.5 text-sm font-medium text-white hover:bg-teal-700 transition-colors` (matching existing EmptyState CTA style)
6. Use `motion.div` from framer-motion for a subtle fade-in animation (opacity 0 to 1, y 18 to 0, duration 0.5s) — same pattern as existing EmptyState.tsx

Do NOT use `useRouter` for navigation — use Next.js `Link` component for the CTA href. This avoids client-side JS dependency for the CTA.
  </action>
  <verify>
    <automated>cd C:/Users/expert/Documents/PKA/Pikar-Ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>personaEmptyStates.ts exports config for all 4 personas across all 8 widget types (16 entries total). PersonaEmptyState.tsx renders persona-aware empty state with icon, headline, description, and CTA link. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PersonaEmptyState into all 8 default widget components</name>
  <files>
    frontend/src/components/widgets/RevenueChart.tsx
    frontend/src/components/widgets/MorningBriefing.tsx
    frontend/src/components/widgets/KanbanWidget.tsx
    frontend/src/components/widgets/CampaignHubWidget.tsx
    frontend/src/components/widgets/InitiativeDashboard.tsx
    frontend/src/components/widgets/WorkflowObservabilityWidget.tsx
    frontend/src/components/widgets/DepartmentActivityWidget.tsx
    frontend/src/components/widgets/BoardroomWidget.tsx
  </files>
  <action>
For each of the 8 widget components, import `PersonaEmptyState` and add an empty-data check that renders it instead of the current generic empty state.

**RevenueChart.tsx:**
Current behavior: shows hardcoded demo data when `data` is incomplete. Add check: if `!data || (!data.values?.length && !data.currentPeriod)`, render `<PersonaEmptyState widgetType="revenue_chart" />`. Keep the existing demo data fallback for when data object exists but is partially populated (existing behavior is fine for that case).

**MorningBriefing.tsx:**
Current behavior: shows "Failed to load briefing." when data is null after fetch. Change the `!data` case (after loading completes and data is null): render `<PersonaEmptyState widgetType="morning_briefing" />` instead of the red error text. Keep the loading spinner as-is.

**KanbanWidget.tsx:**
Current behavior: shows "No columns defined for this board." when columns array is empty. Replace that dashed-border empty state with `<PersonaEmptyState widgetType="kanban_board" />` when `columns.length === 0 && cards.length === 0`.

**CampaignHubWidget.tsx:**
Add empty check: if the campaign data has no campaigns/items, render `<PersonaEmptyState widgetType="campaign_hub" />`. Check for the empty condition based on how the component currently gets its data (likely `data.campaigns` or similar array being empty).

**InitiativeDashboard.tsx:**
Current behavior: shows "No initiatives found" paragraph when `initiatives.length === 0`. Replace that with `<PersonaEmptyState widgetType="initiative_dashboard" />`.

**WorkflowObservabilityWidget.tsx:**
Current behavior: shows error state when stats fails to load. Add check: if stats loaded successfully but `stats.total === 0`, render `<PersonaEmptyState widgetType="workflow_observability" />`.

**DepartmentActivityWidget.tsx:**
Add empty check: if departments array is empty after loading, render `<PersonaEmptyState widgetType="department_activity" />`.

**BoardroomWidget.tsx:**
Add empty check: if `!data?.transcript || data.transcript.length === 0`, render `<PersonaEmptyState widgetType="boardroom" />`.

**Important:** Each widget must import PersonaEmptyState from `@/components/widgets/PersonaEmptyState`. Preserve ALL existing widget functionality — only add the empty state as an early return or conditional branch when data is absent/empty.
  </action>
  <verify>
    <automated>cd C:/Users/expert/Documents/PKA/Pikar-Ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 8 widget components render PersonaEmptyState when their data is empty. RevenueChart shows "Connect Stripe to track your cash flow" for solopreneur. InitiativeDashboard shows "Create your first growth experiment" for startup. No generic "No data" messages remain in the empty-data paths of default widgets. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd frontend && npx tsc --noEmit` passes
2. All 8 widget files import and render PersonaEmptyState
3. personaEmptyStates.ts has entries for all 4 personas x relevant widget types (16 entries)
4. PersonaEmptyState.tsx uses usePersona() and renders Link-based CTA
</verification>

<success_criteria>
- Empty RevenueChart for solopreneur shows "Connect Stripe to track your cash flow" with CTA to /settings/integrations
- Empty InitiativeDashboard for startup shows "Create your first growth experiment" with CTA to /dashboard/initiatives/new
- All empty states include persona-specific headline, description referencing persona objectives, and CTA button
- Existing widget behavior is preserved when data IS present
</success_criteria>

<output>
After completion, create `.planning/phases/31-persona-empty-states-section-headers/31-01-SUMMARY.md`
</output>
