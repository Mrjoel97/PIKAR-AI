---
phase: 58-non-technical-ux-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/suggestions.py
  - app/services/suggestion_service.py
  - frontend/src/services/suggestions.ts
  - frontend/src/components/chat/SuggestionChips.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - tests/unit/app/services/test_suggestion_service.py
autonomous: true
requirements:
  - NTUX-01

must_haves:
  truths:
    - "User sees 4-6 clickable suggestion chips on every new chat screen"
    - "Suggestions are personalized by persona, time of day, and recent activity"
    - "Clicking a chip sends that text as a chat message"
    - "Chips disappear once the user starts typing or sends a message"
  artifacts:
    - path: "app/routers/suggestions.py"
      provides: "GET /suggestions endpoint returning persona-aware chips"
      exports: ["router"]
    - path: "app/services/suggestion_service.py"
      provides: "SuggestionService with persona, time-of-day, and activity-based chip generation"
      exports: ["get_suggestions"]
    - path: "frontend/src/components/chat/SuggestionChips.tsx"
      provides: "Reusable chip strip component"
      exports: ["SuggestionChips"]
    - path: "frontend/src/services/suggestions.ts"
      provides: "Frontend API client for suggestions endpoint"
      exports: ["fetchSuggestions"]
  key_links:
    - from: "frontend/src/components/chat/SuggestionChips.tsx"
      to: "/suggestions"
      via: "fetchSuggestions service call"
      pattern: "fetchSuggestions.*persona"
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "SuggestionChips"
      via: "component import replacing hardcoded displaySuggestions"
      pattern: "SuggestionChips"
---

<objective>
Create a backend-driven suggestion chip system that replaces the hardcoded client-side `displaySuggestions` in ChatInterface with persona-aware, time-of-day-sensitive, activity-based suggestions fetched from a new API endpoint.

Purpose: Non-technical users currently see generic hardcoded prompts. This plan delivers personalized, contextual suggestion chips so every persona sees relevant starting points (e.g., solopreneur at 9am sees "Review yesterday's revenue", enterprise user sees "Check portfolio health").

Output: `GET /suggestions` endpoint + `SuggestionChips` component + backend `SuggestionService` with tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/chat/ChatInterface.tsx
@frontend/src/components/personas/personaShellConfig.ts
@frontend/src/contexts/PersonaContext.tsx
@frontend/src/hooks/useAgentChat.ts
@app/routers/workflows.py
@app/fast_api_app.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/hooks/useAgentChat.ts:
```typescript
export type Message = {
  id?: string;
  role: 'user' | 'agent' | 'system';
  text?: string;
  widget?: WidgetDefinition;
  agentName?: string;
  isThinking?: boolean;
  isMinimized?: boolean;
  traces?: TraceStep[];
  isQueued?: boolean;
  metadata?: import('@/lib/chatMetadata').MessageMetadata;
};

export type AgentMode = 'auto' | 'collab' | 'ask';
```

From frontend/src/components/personas/personaShellConfig.ts:
```typescript
export interface PersonaShellConfig {
  key: string;
  label: string;
  quickActions: QuickAction[];
  kpiLabels: string[];
}

export const PERSONA_SHELL_CONFIG: Record<
  'solopreneur' | 'startup' | 'sme' | 'enterprise',
  PersonaShellConfig
>;
```

From frontend/src/contexts/PersonaContext.tsx:
```typescript
export function usePersona(): { persona: string; userId: string; userEmail: string; agentName: string; isLoading: boolean; };
```

Existing displaySuggestions pattern in ChatInterface.tsx (lines 150-184):
- Currently uses React.useMemo with hardcoded defaults + random user_journeys
- Shows 4 chips, shuffled randomly
- Displayed when: !isRecording && !isSpeechTranscribing && !isUploading && !isStreaming && input.trim().length === 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend suggestion service and API endpoint</name>
  <files>app/services/suggestion_service.py, app/routers/suggestions.py, app/fast_api_app.py, tests/unit/app/services/test_suggestion_service.py</files>
  <behavior>
    - Test 1: get_suggestions(persona="solopreneur", hour=9) returns 4-6 items, each with `text` and `category` fields
    - Test 2: get_suggestions(persona="enterprise", hour=9) returns different suggestions than solopreneur
    - Test 3: Time-of-day buckets: morning (6-12) returns morning-appropriate suggestions (e.g., "Review yesterday's metrics"), afternoon (12-17) returns action-oriented suggestions, evening (17-22) returns planning suggestions
    - Test 4: get_suggestions with recent_activity=["workflow:content_creation"] includes follow-up suggestions related to content
    - Test 5: Always returns between 4 and 6 suggestions
  </behavior>
  <action>
    1. Create `app/services/suggestion_service.py`:
       - Define `SuggestionItem` Pydantic model: `text: str`, `category: str` (one of: "quick_start", "persona_specific", "time_aware", "activity_followup")
       - Define `PERSONA_SUGGESTIONS` dict mapping each persona to 10-15 persona-relevant prompts:
         - solopreneur: revenue-focused, content creation, brain dump, workflow discovery
         - startup: growth metrics, PMF validation, experiment velocity, fundraising prep
         - sme: department coordination, compliance checks, reporting, process optimization
         - enterprise: portfolio health, governance, risk management, cross-functional coordination
       - Define `TIME_BUCKET_SUGGESTIONS` dict with morning/afternoon/evening buckets containing 5-8 time-appropriate prompts each
       - Define `ACTIVITY_FOLLOWUP_MAP` dict mapping recent activity types to follow-up suggestions (e.g., "workflow:content_creation" -> "Review your latest content draft")
       - Implement `async def get_suggestions(persona: str, hour: int, recent_activity: list[str] | None = None, limit: int = 6) -> list[SuggestionItem]`:
         - Collect persona suggestions (weight 3), time-bucket suggestions (weight 2), activity followups (weight 1)
         - Shuffle weighted pool, deduplicate, return `limit` items (minimum 4)
       - Add module docstring, type hints, logger

    2. Create `app/routers/suggestions.py`:
       - `GET /suggestions` endpoint
       - Query params: `persona: str` (required), `hour: int | None` (defaults to server UTC hour), `recent_activity: str | None` (comma-separated)
       - Auth: use `verify_token` dependency from `app/app_utils/auth.py`
       - Returns `list[SuggestionItem]`

    3. Register router in `app/fast_api_app.py`:
       - Import and include_router with prefix="/suggestions", tags=["suggestions"]

    4. Create `tests/unit/app/services/test_suggestion_service.py` with the 5 behavior tests above.
  </action>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai" && uv run pytest tests/unit/app/services/test_suggestion_service.py -x -v</automated>
  </verify>
  <done>GET /suggestions returns 4-6 persona-aware, time-of-day-aware suggestion items; all 5 tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Frontend SuggestionChips component replacing hardcoded displaySuggestions</name>
  <files>frontend/src/services/suggestions.ts, frontend/src/components/chat/SuggestionChips.tsx, frontend/src/components/chat/ChatInterface.tsx</files>
  <action>
    1. Create `frontend/src/services/suggestions.ts`:
       - `interface SuggestionItem { text: string; category: string; }`
       - `export async function fetchSuggestions(persona: string): Promise<SuggestionItem[]>` — calls `GET /suggestions?persona={persona}&hour={new Date().getHours()}` via `fetchWithAuth` from `@/services/api`
       - Include 30-second cache (simple timestamp + stored result) to avoid refetching on every render

    2. Create `frontend/src/components/chat/SuggestionChips.tsx`:
       - `'use client'` directive
       - Props: `{ onSelect: (text: string) => void; persona: string; visible: boolean; }`
       - Uses `useState` + `useEffect` to call `fetchSuggestions(persona)` on mount and when `persona` changes
       - Renders a horizontally scrollable strip of pill-shaped buttons (matching existing style: `px-3 py-1.5 bg-white border border-slate-200 rounded-lg shadow-sm text-xs font-medium text-slate-600 hover:bg-teal-50 hover:text-teal-700 hover:border-teal-200 transition-colors`)
       - Each chip shows the `text` property and calls `onSelect(text)` on click
       - When `visible` is false, renders null
       - Graceful fallback: if fetch fails, show 4 hardcoded generic suggestions ("Review my business", "Create a strategic plan", "Start a brain dump session", "Show available workflows")
       - Add subtle fade-in animation with Tailwind `animate-fadeIn` (or inline opacity transition)

    3. Modify `frontend/src/components/chat/ChatInterface.tsx`:
       - Import `SuggestionChips` from `./SuggestionChips` and `usePersona` from `@/contexts/PersonaContext`
       - Remove the `displaySuggestions` useMemo block (lines ~150-184) and the `userJourneys` state + fetch effect (lines ~133-147)
       - Replace the inline suggestion rendering block (around line 1330) with:
         ```tsx
         <SuggestionChips
           persona={persona}
           visible={!isRecording && !isSpeechTranscribing && !isUploading && !isStreaming && input.trim().length === 0 && messages.length === 0}
           onSelect={(text) => sendMessage(text, agentMode)}
         />
         ```
       - The `messages.length === 0` condition ensures chips show only on fresh/new sessions (the existing ongoing-session contextual suggestions were random and not backend-driven; they will be re-added in a future iteration if needed)
  </action>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai/frontend" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>SuggestionChips component renders persona-aware chips from backend API; ChatInterface uses the new component instead of hardcoded suggestions; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. Backend: `uv run pytest tests/unit/app/services/test_suggestion_service.py -x -v` -- all 5 tests pass
2. Frontend: `cd frontend && npx tsc --noEmit` -- no type errors
3. Lint: `cd "C:/Users/expert/documents/pka/pikar-ai" && uv run ruff check app/services/suggestion_service.py app/routers/suggestions.py`
</verification>

<success_criteria>
- GET /suggestions?persona=solopreneur returns 4-6 contextual suggestion items
- Different personas receive different suggestion sets
- SuggestionChips component renders on fresh chat screens
- Clicking a chip sends it as a chat message
- Hardcoded displaySuggestions removed from ChatInterface
</success_criteria>

<output>
After completion, create `.planning/phases/58-non-technical-ux-foundation/58-01-SUMMARY.md`
</output>
