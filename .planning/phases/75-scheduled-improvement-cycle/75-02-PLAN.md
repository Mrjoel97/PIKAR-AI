---
phase: 75-scheduled-improvement-cycle
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - app/routers/self_improvement.py
  - app/services/self_improvement_engine.py
  - app/services/governance_service.py
  - tests/unit/test_approval_queue_and_audit.py
autonomous: true
requirements: [SCH-05, SCH-06, SCH-07]

must_haves:
  truths:
    - "Admin can view pending_approval actions via GET /self-improvement/actions?status=pending_approval"
    - "Admin can approve an action via POST /self-improvement/actions/{id}/approve and it executes immediately"
    - "Admin can reject an action via POST /self-improvement/actions/{id}/reject and it is marked declined without executing"
    - "Every auto-executed action writes a governance_audit_log row with action_type, skill_name, actor identity, and before/after effectiveness"
    - "Every admin-approved action writes a governance_audit_log row with the admin as actor"
    - "After two consecutive cycles that regress average effectiveness by >5%, auto_execute_enabled flips to false"
  artifacts:
    - path: "app/routers/self_improvement.py"
      provides: "Approve and reject endpoints for pending actions"
      contains: "approve"
    - path: "app/services/self_improvement_engine.py"
      provides: "Governance audit logging on execution + circuit breaker logic"
      contains: "circuit_breaker"
    - path: "tests/unit/test_approval_queue_and_audit.py"
      provides: "Tests for approval, rejection, audit logging, and circuit breaker"
  key_links:
    - from: "app/routers/self_improvement.py"
      to: "app/services/self_improvement_engine.py"
      via: "execute_improvement on approve"
      pattern: "execute_improvement"
    - from: "app/services/self_improvement_engine.py"
      to: "app/services/governance_service.py"
      via: "log_event after execution"
      pattern: "log_event"
    - from: "app/services/self_improvement_engine.py"
      to: "app/services/self_improvement_settings.py"
      via: "update_self_improvement_settings on circuit breaker trip"
      pattern: "update_self_improvement_settings"
---

<objective>
Approval queue, governance audit logging, and safety circuit breaker for the self-improvement cycle.

Purpose: Ensure high-risk improvement actions require admin approval before execution, every execution is audit-logged for compliance, and the system automatically disables auto_execute if consecutive regressions are detected.
Output: Approve/reject API endpoints, governance audit integration, circuit breaker logic.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-scheduled-improvement-cycle/75-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan builds on -->

From app/services/self_improvement_settings.py (created in 75-01):
```python
async def get_self_improvement_settings() -> dict:
    # Returns {"auto_execute_enabled": bool, "auto_execute_risk_tiers": list[str]}

async def update_self_improvement_settings(key: str, value: Any, updated_by: str) -> None:
    # Upserts a row in self_improvement_settings
```

From app/services/governance_service.py (existing):
```python
class GovernanceService:
    async def log_event(
        self, user_id: str, action_type: str, resource_type: str,
        resource_id: str | None = None, details: dict[str, Any] | None = None,
        ip_address: str | None = None,
    ) -> None:
        # Inserts into governance_audit_log. NEVER raises.
```

From app/routers/self_improvement.py (existing):
```python
router = APIRouter(prefix="/self-improvement", tags=["Self-Improvement"])
# Existing endpoints: /dashboard, /scores, /actions, /gaps, /run-cycle, /skills/{name}/history, /interactions/{id}/feedback
# The /actions endpoint already supports ?status= filter
```

From supabase/migrations/20260318000000_self_improvement.sql (improvement_actions columns):
```
id, action_type, skill_name, agent_id, trigger_reason, details, status,
effectiveness_before, effectiveness_after, created_at, validated_at
-- After 75-01 migration also: approved_by, approved_at
```

From app/services/self_improvement_engine.py:
```python
_SYSTEM_USER_ID = "system:self-improvement-engine"

async def execute_improvement(self, action: dict[str, Any]) -> dict[str, Any]:
    # Dispatches by action_type, updates status to 'applied' or 'failed'

async def evaluate_skills(self, days: int = 7) -> list[dict[str, Any]]:
    # Returns list of score dicts with effectiveness_score per skill
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Approve/reject endpoints + governance audit logging</name>
  <files>
    app/routers/self_improvement.py,
    app/services/self_improvement_engine.py,
    tests/unit/test_approval_queue_and_audit.py
  </files>
  <behavior>
    - Test 1: POST /self-improvement/actions/{id}/approve on a pending_approval action calls execute_improvement and sets status=applied, approved_by, approved_at
    - Test 2: POST /self-improvement/actions/{id}/reject on a pending_approval action sets status=declined without calling execute_improvement
    - Test 3: POST /self-improvement/actions/{id}/approve on a non-pending_approval action returns 409 Conflict
    - Test 4: Every auto-executed action in run_improvement_cycle produces a governance_audit_log row with action_type, skill_name, actor='system:self-improvement-engine', and effectiveness_before in details
    - Test 5: Admin-approved action produces a governance_audit_log row with the admin's user_id as actor
    - Test 6: Rejected action does NOT produce an execution audit log row (only a rejection audit row)
  </behavior>
  <action>
    1. Add to `app/routers/self_improvement.py`:

       **POST `/self-improvement/actions/{action_id}/approve`:**
       - Depends on get_current_user_id (admin auth).
       - Fetch action from improvement_actions by id. If not found, 404. If status != 'pending_approval', return 409.
       - Lazy import SelfImprovementEngine, call engine.execute_improvement(action).
       - Update action row: status from execute result, approved_by=current_user_id, approved_at=now().
       - Call governance audit logging (see step 2).
       - Return the execution result.

       **POST `/self-improvement/actions/{action_id}/reject`:**
       - Depends on get_current_user_id (admin auth).
       - Fetch action from improvement_actions by id. If not found, 404. If status != 'pending_approval', return 409.
       - Update action row: status='declined', approved_by=current_user_id, approved_at=now().
       - Log rejection to governance_audit_log with action_type='self_improvement_action_rejected'.
       - Return confirmation.

    2. Add governance audit logging to `app/services/self_improvement_engine.py`:

       **In `execute_improvement` method**, after successful execution (status='applied'):
       - Lazy import GovernanceService (or use get_governance_service).
       - Call `governance_service.log_event(user_id=actor_id, action_type='self_improvement_action_executed', resource_type='improvement_action', resource_id=action_id, details={...})` where details includes: action_type, skill_name, trigger_reason, effectiveness_before (from action metadata), effectiveness_after (None at execution time -- filled on validation).
       - The actor_id is `_SYSTEM_USER_ID` for auto-executed actions. For admin-approved actions, the approve endpoint passes the admin's user_id.
       - Add an optional `actor_id: str | None = None` parameter to `execute_improvement`. Default to `_SYSTEM_USER_ID`.

       **In the approve endpoint**, pass `actor_id=current_user_id` to `execute_improvement`.

       Governance logging is fire-and-forget (try/except, never raises) matching the existing GovernanceService pattern.

    3. Write tests in `tests/unit/test_approval_queue_and_audit.py` mocking execute_async and GovernanceService. Validate the 6 behaviors above.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/test_approval_queue_and_audit.py -x -v</automated>
  </verify>
  <done>
    Approve endpoint executes pending_approval actions and logs to audit. Reject endpoint marks declined without execution. All auto-executed and admin-approved actions produce governance_audit_log rows. 6+ tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Circuit breaker — auto-disable on regression</name>
  <files>
    app/services/self_improvement_engine.py,
    tests/unit/test_approval_queue_and_audit.py
  </files>
  <behavior>
    - Test 7: After two consecutive cycles where avg effectiveness regressed by >5%, auto_execute_enabled is flipped to false
    - Test 8: A single cycle regression does NOT trip the circuit breaker
    - Test 9: Two consecutive cycles where regression is exactly 5% or less do NOT trip the circuit breaker
    - Test 10: After circuit breaker trips, the engine logs a warning and produces a governance_audit_log entry
  </behavior>
  <action>
    1. Add circuit breaker logic to `app/services/self_improvement_engine.py` at the end of `run_improvement_cycle`, AFTER evaluation and execution:

       **`_check_circuit_breaker` method:**
       - Query the two most recent `skill_scores` evaluation snapshots (grouped by evaluated_at, compute average effectiveness per snapshot).
       - If fewer than 2 snapshots exist, return (no action).
       - Compare: if current_avg < previous_avg and the delta is > 0.05 (5%), record this as a regression.
       - Query a simple counter: check if the PREVIOUS cycle also regressed. Implementation: store a `circuit_breaker_consecutive_regressions` key in `self_improvement_settings`. Increment on regression, reset to 0 on non-regression.
       - If consecutive_regressions >= 2:
         - Call `update_self_improvement_settings('auto_execute_enabled', False, 'system:circuit-breaker')`.
         - Log a WARNING: "Circuit breaker tripped: auto_execute disabled after 2 consecutive regressions".
         - Write governance_audit_log entry with action_type='self_improvement_circuit_breaker', details containing the regression deltas.

       **Integration point:** Call `_check_circuit_breaker(scores)` at the end of `run_improvement_cycle`, after evaluation. Pass the current cycle's scores.

    2. Add tests 7-10 to `tests/unit/test_approval_queue_and_audit.py` testing the circuit breaker behavior with mocked DB responses.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/test_approval_queue_and_audit.py -x -v</automated>
  </verify>
  <done>
    Circuit breaker auto-disables auto_execute_enabled after two consecutive >5% regressions. Single regressions and small regressions do not trip. Governance audit logged on trip. 10+ total tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_approval_queue_and_audit.py -x -v` passes all 10+ tests
- `uv run pytest tests/unit/test_scheduled_improvement_cycle.py -x -v` still passes (Plan 01 tests unbroken)
- `uv run ruff check app/routers/self_improvement.py app/services/self_improvement_engine.py` clean
</verification>

<success_criteria>
- Admin can approve pending_approval actions which execute immediately
- Admin can reject pending_approval actions which are marked declined
- Every auto-executed and admin-approved action produces a governance_audit_log row
- Circuit breaker trips after two consecutive >5% regressions and flips auto_execute_enabled to false
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/75-scheduled-improvement-cycle/75-02-SUMMARY.md`
</output>
