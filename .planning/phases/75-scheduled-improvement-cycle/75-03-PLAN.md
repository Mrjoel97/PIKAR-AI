---
phase: 75-scheduled-improvement-cycle
plan: 03
type: execute
wave: 3
depends_on: ["75-01", "75-02"]
files_modified:
  - tests/integration/test_scheduled_improvement_uat.py
autonomous: false
requirements: [SCH-08]

must_haves:
  truths:
    - "Operator can hit the scheduled endpoint via curl and the cycle runs"
    - "The approval queue populates with pending_approval actions for high-risk types"
    - "Audit rows are written for executed actions"
    - "The full loop from endpoint trigger to approval queue to audit log is verified end-to-end"
  artifacts:
    - path: "tests/integration/test_scheduled_improvement_uat.py"
      provides: "Integration test covering the full scheduled cycle flow"
  key_links:
    - from: "tests/integration/test_scheduled_improvement_uat.py"
      to: "app/services/scheduled_endpoints.py"
      via: "TestClient POST to /scheduled/self-improvement-cycle"
      pattern: "self-improvement-cycle"
---

<objective>
UAT gate verifying the full scheduled improvement cycle end-to-end.

Purpose: Confirm the entire flow works as a connected system: scheduled endpoint triggers cycle, risk-tiered actions are created, approval queue populates, and audit log rows are written.
Output: Integration test proving the end-to-end loop, plus manual verification checkpoint.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-scheduled-improvement-cycle/75-01-SUMMARY.md
@.planning/phases/75-scheduled-improvement-cycle/75-02-SUMMARY.md

<interfaces>
<!-- Full API surface built in Plans 01+02 -->

Endpoints:
- POST /scheduled/self-improvement-cycle (X-Scheduler-Secret header)
- GET /self-improvement/actions?status=pending_approval
- POST /self-improvement/actions/{id}/approve
- POST /self-improvement/actions/{id}/reject
- GET /self-improvement/dashboard

Settings table: self_improvement_settings (auto_execute_enabled, auto_execute_risk_tiers)
Audit table: governance_audit_log (user_id, action_type, resource_type, resource_id, details)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test for full scheduled cycle flow</name>
  <files>tests/integration/test_scheduled_improvement_uat.py</files>
  <action>
    Create `tests/integration/test_scheduled_improvement_uat.py` that validates the full SCH-08 flow using mocked Supabase (same execute_async mock pattern as other integration tests in this project).

    **Test structure:**

    1. **test_scheduled_endpoint_unauthorized** — POST /scheduled/self-improvement-cycle without X-Scheduler-Secret returns 401.

    2. **test_scheduled_endpoint_triggers_cycle** — Mock SCHEDULER_SECRET env var, mock execute_async to return plausible interaction_logs and skill_scores data, mock self_improvement_settings to return auto_execute_enabled=True. POST with valid header. Assert:
       - Response 200 with success=True
       - Result contains scores_computed > 0
       - Mock calls show improvement_actions inserts happened

    3. **test_risk_tier_gating_creates_pending_approval** — Set up mocks so identify_improvements produces both a skill_demoted action and a skill_refined action. Assert:
       - skill_demoted action was auto-executed (execute_improvement called)
       - skill_refined action was inserted with status=pending_approval (NOT executed)

    4. **test_approve_action_executes_and_audits** — Mock a pending_approval action in the DB. POST /self-improvement/actions/{id}/approve. Assert:
       - Action status changed to applied
       - governance_audit_log insert was called with action_type containing 'self_improvement'

    5. **test_reject_action_marks_declined** — Mock a pending_approval action. POST /self-improvement/actions/{id}/reject. Assert:
       - Action status changed to declined
       - execute_improvement was NOT called

    Use FastAPI TestClient with the app. Mock external dependencies (execute_async, Gemini calls) to isolate the flow logic. Follow the lazy-import mock pattern established in Phase 62-04 and 73-02 tests.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/integration/test_scheduled_improvement_uat.py -x -v</automated>
  </verify>
  <done>
    5 integration tests pass covering: auth rejection, cycle trigger, risk-tier gating with pending_approval, approve+audit, reject+decline. The full SCH-08 UAT flow is verified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 75 system</name>
  <files>tests/integration/test_scheduled_improvement_uat.py</files>
  <action>
    Present the complete system for human review. All automation is done -- this checkpoint only verifies correctness.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/test_scheduled_improvement_cycle.py tests/unit/test_approval_queue_and_audit.py tests/integration/test_scheduled_improvement_uat.py -v</automated>
  </verify>
  <done>Human approves the complete Phase 75 system.</done>
  <what-built>
    Complete scheduled self-improvement cycle system:
    - POST /scheduled/self-improvement-cycle endpoint (X-Scheduler-Secret gated)
    - Risk-tiered auto_execute: skill_demoted and pattern_extract auto-execute; skill_refined and skill_created queue for approval
    - Admin approval queue: approve executes immediately, reject marks declined
    - Governance audit logging on every execution
    - Circuit breaker: two consecutive >5% regressions disable auto_execute
    - Cloud Scheduler runbook at docs/runbooks/self-improvement-scheduler.md
  </what-built>
  <how-to-verify>
    1. Run full test suite: `uv run pytest tests/unit/test_scheduled_improvement_cycle.py tests/unit/test_approval_queue_and_audit.py tests/integration/test_scheduled_improvement_uat.py -v`
    2. Verify all tests pass (should be 15+ tests total)
    3. Review the Cloud Scheduler runbook: `cat docs/runbooks/self-improvement-scheduler.md`
    4. Verify migration SQL: `cat supabase/migrations/20260412200000_scheduled_improvement_cycle.sql`
    5. Optionally: run `uv run ruff check app/services/self_improvement_engine.py app/routers/self_improvement.py app/services/self_improvement_settings.py app/services/scheduled_endpoints.py` for lint clean
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_scheduled_improvement_uat.py tests/unit/test_scheduled_improvement_cycle.py tests/unit/test_approval_queue_and_audit.py -v` — all pass
- Human verifies the system makes sense as a cohesive whole
</verification>

<success_criteria>
- SCH-08 UAT gate satisfied: endpoint triggered, approval queue populated, audit rows written
- All 15+ tests pass across all 3 test files
- Human approves the complete system
</success_criteria>

<output>
After completion, create `.planning/phases/75-scheduled-improvement-cycle/75-03-SUMMARY.md`
</output>
