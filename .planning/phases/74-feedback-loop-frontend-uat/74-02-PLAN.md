---
phase: 74-feedback-loop-frontend-uat
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - tests/unit/test_feedback_loop_e2e.py
autonomous: false
requirements: [FBL-07]

must_haves:
  truths:
    - "User sends a chat message, clicks thumbs-down, and interaction_logs.user_feedback='negative' is written"
    - "Running evaluate_skills after thumbs-down produces a positive_rate that differs from the default 0.5 baseline"
    - "The full closed loop from chat to feedback to skill evaluation reflects real user signal"
  artifacts:
    - path: "tests/unit/test_feedback_loop_e2e.py"
      provides: "Integration test proving feedback data flows through to evaluate_skills"
      contains: "positive_rate"
  key_links:
    - from: "frontend MessageFeedback component"
      to: "POST /self-improvement/interactions/{id}/feedback"
      via: "fetchWithAuth on thumbs-down click"
      pattern: "fetchWithAuth.*interactions.*feedback"
    - from: "POST /self-improvement/interactions/{id}/feedback"
      to: "interaction_logs.user_feedback column"
      via: "InteractionLogger.record_feedback"
      pattern: "record_feedback"
    - from: "interaction_logs.user_feedback"
      to: "evaluate_skills positive_rate"
      via: "SelfImprovementEngine._compute_agent_metrics"
      pattern: "positive_rate"
---

<objective>
Verify the full feedback loop end-to-end: chat message -> SSE interaction_id -> thumbs-down click -> DB write -> evaluate_skills reflects real signal.

Purpose: Closes FBL-07 — the UAT gate proving the closed feedback loop actually works from user action to skill evaluation. This is the final validation before the scheduled improvement cycle (Phase 75) can trust feedback data.
Output: Verified end-to-end feedback loop with human confirmation.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-feedback-loop-frontend-uat/74-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated integration test for feedback loop data path</name>
  <files>tests/unit/test_feedback_loop_e2e.py</files>
  <action>
    Create a Python integration test that verifies the backend data path from feedback POST to evaluate_skills output:

    1. **Test: feedback_changes_positive_rate** — Use the `SelfImprovementEngine` and `InteractionLogger` directly:
       a. Call `interaction_logger.log_interaction(agent_id="test_agent", user_query="test query", agent_response_summary="test response", session_id="test-session", response_time_ms=100, task_completed=True)` — capture returned UUID
       b. Call `interaction_logger.record_feedback(interaction_id=uuid, feedback="negative")` — verify no exception
       c. Call `engine.evaluate_skills(days=1)` — find the score for "test_agent"
       d. Assert `positive_rate != 0.5` (the default when no feedback exists). With one negative rating and zero positive, it should be `0.0`.

    2. **Test: multiple_feedback_signals** — Log 3 interactions for the same agent. Give 2 negative feedback, 1 positive. Call evaluate_skills. Assert `positive_rate` is approximately `0.333` (1/3).

    Both tests must mock the Supabase client to avoid needing a live database. Use the pattern established in the codebase: patch `app.services.supabase.get_service_client` and `app.services.supabase_async.execute_async` to return controlled data.

    IMPORTANT: Study how `_compute_agent_metrics` in `app/services/self_improvement_engine.py` (around line 720-755) computes `positive_rate` — it queries `interaction_logs` rows where `user_feedback` is not null, counts positive vs total feedback. The test mocks must return data that matches this query pattern.

    Use `pytest` with `@pytest.mark.asyncio` decorator. Import `SelfImprovementEngine` lazily inside the test to avoid import-time Supabase initialization.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run pytest tests/unit/test_feedback_loop_e2e.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Automated test proves that feedback POST data flows through to evaluate_skills and produces a non-default positive_rate reflecting the actual signal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT gate — full feedback loop verification</name>
  <files></files>
  <action>
    Human verifies the full feedback loop end-to-end by running the local stack and interacting with the chat UI. All automation is complete — this checkpoint only confirms it works visually and functionally.
  </action>
  <what-built>
    Full feedback loop from chat UI to skill evaluation:
    1. SSE stream captures interaction_id on every agent response
    2. Thumbs-up/down buttons appear on agent messages (not user messages)
    3. Clicking a thumb shows optimistic filled state immediately
    4. POST fires to /self-improvement/interactions/{id}/feedback
    5. Automated test proves feedback data reaches evaluate_skills
  </what-built>
  <how-to-verify>
    **Prerequisites:** Backend running (`make local-backend`), frontend running (`cd frontend && npm run dev`).

    **Step 1 — Visual Check:**
    1. Open the chat interface at http://localhost:3000 (any persona)
    2. Send any message (e.g., "What's my financial health?")
    3. Wait for the agent to respond fully
    4. Confirm: thumbs-up and thumbs-down icons appear below the agent's message bubble
    5. Confirm: NO thumbs icons appear on your (user) message
    6. Confirm: NO thumbs icons appear on the welcome message

    **Step 2 — Optimistic UI:**
    1. Click the thumbs-down button
    2. Confirm: the button immediately fills/changes color (rose/red) without delay
    3. Confirm: the thumbs-up button remains unfilled

    **Step 3 — API Verification:**
    1. Open browser DevTools > Network tab
    2. Click thumbs-down on an agent message
    3. Confirm: a POST request goes to `/self-improvement/interactions/{uuid}/feedback`
    4. Confirm: the request body is `{"rating":"negative"}`
    5. Confirm: the response is 200 OK

    **Step 4 — Database Verification (optional, if Supabase running):**
    1. Query `interaction_logs` table for the most recent row
    2. Confirm: `user_feedback = 'negative'`

    If all steps pass, type "approved". If any step fails, describe the issue.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human verification required"</automated>
  </verify>
  <done>Human confirms the full feedback loop works: thumbs buttons visible on agent messages, optimistic UI on click, API fires correctly, and the data reaches the database.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/unit/test_feedback_loop_e2e.py -x -v` — automated feedback-to-evaluation test passes
2. Human verifies visual UI, optimistic state, API call, and database write
3. The full loop: chat -> interaction_id -> thumbs-down -> DB write -> evaluate_skills -> non-default positive_rate is proven
</verification>

<success_criteria>
- Automated test proves feedback data changes positive_rate from default 0.5 to actual signal value
- Human confirms thumbs buttons appear on agent messages only
- Human confirms optimistic UI on click
- Human confirms API call fires correctly
- FBL-07 UAT gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/74-feedback-loop-frontend-uat/74-02-SUMMARY.md`
</output>
