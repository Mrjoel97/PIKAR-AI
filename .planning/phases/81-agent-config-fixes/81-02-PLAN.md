---
phase: 81-agent-config-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agents/sales/agent.py
  - app/agents/operations/agent.py
  - app/agents/compliance/agent.py
  - app/agents/customer_support/agent.py
  - app/agents/reporting/agent.py
  - app/agents/research/instructions.py
  - app/agents/research/agent.py
autonomous: true
requirements: [AGT-04]

must_haves:
  truths:
    - "Sales agent instruction string includes error/escalation block"
    - "Operations agent instruction string includes error/escalation block"
    - "Compliance agent instruction string includes error/escalation block"
    - "Customer Support agent instruction string includes error/escalation block"
    - "Reporting agent instruction string includes skills registry, self-improvement, and error/escalation blocks"
    - "Research agent instruction string includes skills registry, self-improvement, and error/escalation blocks"
  artifacts:
    - path: "app/agents/sales/agent.py"
      provides: "Sales agent with escalation instructions"
      contains: "get_error_and_escalation_instructions"
    - path: "app/agents/operations/agent.py"
      provides: "Operations agent with escalation instructions"
      contains: "get_error_and_escalation_instructions"
    - path: "app/agents/compliance/agent.py"
      provides: "Compliance agent with escalation instructions"
      contains: "get_error_and_escalation_instructions"
    - path: "app/agents/customer_support/agent.py"
      provides: "Customer Support agent with escalation instructions"
      contains: "get_error_and_escalation_instructions"
    - path: "app/agents/reporting/agent.py"
      provides: "Reporting agent with all three shared instruction blocks"
      contains: "SKILLS_REGISTRY_INSTRUCTIONS"
    - path: "app/agents/research/instructions.py"
      provides: "Research agent instructions with all three shared blocks"
      contains: "SKILLS_REGISTRY_INSTRUCTIONS"
  key_links:
    - from: "app/agents/sales/agent.py"
      to: "app/agents/shared_instructions.py"
      via: "import get_error_and_escalation_instructions"
      pattern: "get_error_and_escalation_instructions"
    - from: "app/agents/reporting/agent.py"
      to: "app/agents/shared_instructions.py"
      via: "import SKILLS_REGISTRY_INSTRUCTIONS, SELF_IMPROVEMENT_INSTRUCTIONS, get_error_and_escalation_instructions"
      pattern: "SKILLS_REGISTRY_INSTRUCTIONS"
    - from: "app/agents/research/instructions.py"
      to: "app/agents/shared_instructions.py"
      via: "import shared instruction blocks"
      pattern: "SKILLS_REGISTRY_INSTRUCTIONS"
---

<objective>
Add missing shared instruction blocks (error/escalation, skills registry, self-improvement) to six agents that lack them, aligning all agents with the pattern established by Financial, Strategic, and Content agents.

Purpose: Agents without error/escalation instructions may silently fail or fail to escalate correctly. Agents without skills registry/self-improvement instructions cannot discover or iterate on skills. This creates inconsistent behavior across the agent fleet.

Output: Six agent files updated with consistent instruction blocks.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From app/agents/shared_instructions.py — the shared blocks to add -->

```python
# Constants (append to instruction string):
SKILLS_REGISTRY_INSTRUCTIONS: str   # Skills discovery, usage, creation guidance
SELF_IMPROVEMENT_INSTRUCTIONS: str  # Gap reporting, performance checking

# Function (call with agent name and domain rules, append result to instruction string):
def get_error_and_escalation_instructions(agent_name: str, domain_rules: str = "") -> str
```

<!-- Exemplar pattern from app/agents/financial/agent.py (lines 201-213): -->
```python
FINANCIAL_AGENT_INSTRUCTION = (
    """..."""
    + get_widget_instruction_for_agent(...)
    + SKILLS_REGISTRY_INSTRUCTIONS
    + WEB_RESEARCH_INSTRUCTIONS          # or WEB_SEARCH_ONLY_INSTRUCTIONS
    + CONVERSATION_MEMORY_INSTRUCTIONS
    + SELF_IMPROVEMENT_INSTRUCTIONS
    + get_error_and_escalation_instructions(
        "Financial Analysis Agent",
        """- domain-specific escalation rules here...""",
    )
)
```

<!-- Current state of each agent's instruction blocks: -->
<!--
  Sales:            HAS skills_registry, HAS self_improvement, MISSING escalation
  Operations:       HAS skills_registry, HAS self_improvement, MISSING escalation
  Compliance:       HAS skills_registry, HAS self_improvement, MISSING escalation
  Customer Support: HAS skills_registry, HAS self_improvement, MISSING escalation
  Reporting:        MISSING all three (skills_registry, self_improvement, escalation)
  Research:         MISSING all three (skills_registry, self_improvement, escalation)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error/escalation instructions to Sales, Operations, Compliance, and Customer Support agents</name>
  <files>app/agents/sales/agent.py, app/agents/operations/agent.py, app/agents/compliance/agent.py, app/agents/customer_support/agent.py</files>
  <action>
These four agents already have SKILLS_REGISTRY_INSTRUCTIONS and SELF_IMPROVEMENT_INSTRUCTIONS. They only need `get_error_and_escalation_instructions()` appended.

**app/agents/sales/agent.py:**
1. Add `get_error_and_escalation_instructions` to the existing import from `app.agents.shared_instructions` (line 27). It is already imported — wait, check: it is NOT in the current import list. Add it.
2. Append to the instruction string concatenation (after `SELF_IMPROVEMENT_INSTRUCTIONS` on line 176):
```python
    + get_error_and_escalation_instructions(
        "Sales Intelligence Agent",
        """- Escalate to legal/compliance agent for contract terms, NDA review, or regulatory questions
- Escalate to financial agent for pricing strategy, discount authorization beyond standard ranges, or revenue recognition
- Never commit to contract terms or pricing without explicit user approval
- For deals exceeding the user's stated authority threshold, recommend involving a senior decision-maker""",
    )
```

**app/agents/operations/agent.py:**
1. Add `get_error_and_escalation_instructions` to the existing import from `app.agents.shared_instructions` (line 32-35).
2. Append to the instruction string concatenation (after `SELF_IMPROVEMENT_INSTRUCTIONS` on line 166):
```python
    + get_error_and_escalation_instructions(
        "Operations Optimization Agent",
        """- Escalate to compliance agent for regulatory or audit-related process changes
- Escalate to financial agent for budget approvals or cost optimization decisions exceeding operational authority
- Never deploy infrastructure changes or modify production configurations without explicit user confirmation
- For vendor contracts or SaaS commitments, recommend involving procurement or finance""",
    )
```

**app/agents/compliance/agent.py:**
1. Add `get_error_and_escalation_instructions` to the existing import from `app.agents.shared_instructions` (line 37-39).
2. Append to the instruction string concatenation (after `SELF_IMPROVEMENT_INSTRUCTIONS` on line 163):
```python
    + get_error_and_escalation_instructions(
        "Compliance & Risk Agent",
        """- Escalate to external legal counsel for novel regulatory interpretations or high-stakes litigation risk
- Escalate to financial agent for financial impact quantification of compliance violations
- Never provide definitive legal advice — always caveat that recommendations should be reviewed by qualified legal counsel
- For cross-jurisdictional matters, recommend engaging local legal expertise""",
    )
```

**app/agents/customer_support/agent.py:**
1. Add `get_error_and_escalation_instructions` to the existing import from `app.agents.shared_instructions` (line 28-30).
2. Append to the instruction string concatenation (after `SELF_IMPROVEMENT_INSTRUCTIONS` on line 89):
```python
    + get_error_and_escalation_instructions(
        "Customer Success Manager",
        """- Escalate to compliance agent for data privacy requests (GDPR deletion, CCPA access)
- Escalate to financial agent for refund approvals exceeding standard policy limits
- Never promise specific resolution timelines or compensation without user approval
- For legal threats or regulatory complaints, immediately escalate to compliance agent""",
    )
```
  </action>
  <verify>
    <automated>cd C:/Users/expert/Documents/PKA/Pikar-Ai && python -c "
from app.agents.sales.agent import SALES_AGENT_INSTRUCTION
from app.agents.operations.agent import OPERATIONS_AGENT_INSTRUCTION
from app.agents.compliance.agent import COMPLIANCE_AGENT_INSTRUCTION
from app.agents.customer_support.agent import CUSTOMER_SUPPORT_AGENT_INSTRUCTION
for name, instr in [('Sales', SALES_AGENT_INSTRUCTION), ('Operations', OPERATIONS_AGENT_INSTRUCTION), ('Compliance', COMPLIANCE_AGENT_INSTRUCTION), ('CustomerSupport', CUSTOMER_SUPPORT_AGENT_INSTRUCTION)]:
    assert 'ERROR HANDLING & ESCALATION' in instr, f'{name} missing escalation block'
    assert 'SKILLS REGISTRY ACCESS' in instr, f'{name} missing skills registry'
    assert 'Self-Improvement System' in instr, f'{name} missing self-improvement'
    print(f'{name}: all 3 blocks present')
print('PASS')
"</automated>
  </verify>
  <done>Sales, Operations, Compliance, and Customer Support agents all include the error/escalation instruction block alongside the pre-existing skills registry and self-improvement blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Add all three shared instruction blocks to Reporting and Research agents</name>
  <files>app/agents/reporting/agent.py, app/agents/research/instructions.py, app/agents/research/agent.py</files>
  <action>
These two agents are missing all three blocks: SKILLS_REGISTRY_INSTRUCTIONS, SELF_IMPROVEMENT_INSTRUCTIONS, and get_error_and_escalation_instructions().

**app/agents/reporting/agent.py:**
1. Add imports. Currently imports only `CONVERSATION_MEMORY_INSTRUCTIONS` from `app.agents.shared_instructions` (line 25). Update to:
```python
from app.agents.shared_instructions import (
    CONVERSATION_MEMORY_INSTRUCTIONS,
    SELF_IMPROVEMENT_INSTRUCTIONS,
    SKILLS_REGISTRY_INSTRUCTIONS,
    get_error_and_escalation_instructions,
)
```
2. Update `DATA_REPORTING_AGENT_INSTRUCTION` string concatenation (currently ends with `+ CONVERSATION_MEMORY_INSTRUCTIONS` on line 167). Change to:
```python
    + CONVERSATION_MEMORY_INSTRUCTIONS
    + SKILLS_REGISTRY_INSTRUCTIONS
    + SELF_IMPROVEMENT_INSTRUCTIONS
    + get_error_and_escalation_instructions(
        "Data Reporting Agent",
        """- Escalate to financial agent for financial report interpretation or accounting treatment questions
- Escalate to operations agent for workflow or process-related data that requires operational context
- Never modify source spreadsheet data without explicit user confirmation
- For reports containing sensitive financial or HR data, remind the user about access controls before sharing""",
    )
)
```
3. The Reporting agent currently uses `list_available_skills` and `use_skill` from `app.agents.enhanced_tools` (line 22). These are the legacy skill tools — they will continue to work alongside the new instruction blocks which reference the same underlying skill system. No change needed to tools.

**app/agents/research/instructions.py:**
1. Add imports at the top of the file (after the docstring and `from __future__ import annotations`):
```python
from app.agents.shared_instructions import (
    SELF_IMPROVEMENT_INSTRUCTIONS,
    SKILLS_REGISTRY_INSTRUCTIONS,
    get_error_and_escalation_instructions,
)
```
2. Update `RESEARCH_AGENT_INSTRUCTION` to append the three blocks. Currently it ends as a plain triple-quoted string (line 102). Change to a concatenated expression:
```python
RESEARCH_AGENT_INSTRUCTION = (
    """...[existing instruction text unchanged]..."""
    + SKILLS_REGISTRY_INSTRUCTIONS
    + SELF_IMPROVEMENT_INSTRUCTIONS
    + get_error_and_escalation_instructions(
        "Research Intelligence Agent",
        """- Escalate to compliance agent for research involving regulated industries or privacy-sensitive data
- Escalate to the requesting agent when research confidence is below 50% — do not present low-confidence findings as conclusions
- Never fabricate sources or citations — if data is unavailable, report the gap explicitly
- For research requiring paid data sources or API access, inform the user of cost implications before proceeding""",
    )
)
```
3. The Research agent does not currently have skill tools or self-improve tools in its tool list (`app/agents/research/agent.py`). The instruction blocks reference `report_skill_gap()`, `check_my_performance()`, etc. These will be available if/when skill tools are added to the research agent in a future phase. For now, the instructions prepare the agent to use them. No changes to `app/agents/research/agent.py` tool list are needed — only the instruction text changes via `instructions.py`.
  </action>
  <verify>
    <automated>cd C:/Users/expert/Documents/PKA/Pikar-Ai && python -c "
from app.agents.reporting.agent import DATA_REPORTING_AGENT_INSTRUCTION
from app.agents.research.instructions import RESEARCH_AGENT_INSTRUCTION
for name, instr in [('Reporting', DATA_REPORTING_AGENT_INSTRUCTION), ('Research', RESEARCH_AGENT_INSTRUCTION)]:
    assert 'ERROR HANDLING & ESCALATION' in instr, f'{name} missing escalation block'
    assert 'SKILLS REGISTRY ACCESS' in instr, f'{name} missing skills registry'
    assert 'Self-Improvement System' in instr, f'{name} missing self-improvement'
    print(f'{name}: all 3 blocks present')
print('PASS')
"</automated>
  </verify>
  <done>Reporting and Research agents include all three shared instruction blocks (skills registry, self-improvement, error/escalation). All agents in the fleet now have consistent instruction alignment.</done>
</task>

</tasks>

<verification>
Run a comprehensive check across all six agents:

```bash
# Verify escalation block present in all 6 agents
cd C:/Users/expert/Documents/PKA/Pikar-Ai
grep -l "get_error_and_escalation_instructions" \
  app/agents/sales/agent.py \
  app/agents/operations/agent.py \
  app/agents/compliance/agent.py \
  app/agents/customer_support/agent.py \
  app/agents/reporting/agent.py \
  app/agents/research/instructions.py | wc -l
# Expect: 6

# Verify skills registry in Reporting and Research (others already had it)
grep -l "SKILLS_REGISTRY_INSTRUCTIONS" \
  app/agents/reporting/agent.py \
  app/agents/research/instructions.py | wc -l
# Expect: 2

# Verify self-improvement in Reporting and Research
grep -l "SELF_IMPROVEMENT_INSTRUCTIONS" \
  app/agents/reporting/agent.py \
  app/agents/research/instructions.py | wc -l
# Expect: 2

# Full test suite
make test
```
</verification>

<success_criteria>
1. Sales, Operations, Compliance, and Customer Support agent instruction strings include the error/escalation block — verifiable by string search for "ERROR HANDLING & ESCALATION"
2. Reporting and Research agent instruction strings include all three blocks: skills registry ("SKILLS REGISTRY ACCESS"), self-improvement ("Self-Improvement System"), and error/escalation ("ERROR HANDLING & ESCALATION")
3. All existing tests pass (make test)
</success_criteria>

<output>
After completion, create `.planning/phases/81-agent-config-fixes/81-02-SUMMARY.md`
</output>
