---
phase: 48-notification-event-type-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/notification_rule_service.py
autonomous: true
gap_closure: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - INTEL-04

must_haves:
  truths:
    - "A monitoring.alert event dispatched by MonitoringJobService matches notification rules and delivers to Slack/Teams"
    - "The test-notification button dispatches an agent.message event that matches notification rules and delivers to the connected provider (200 response, not 502)"
  artifacts:
    - path: "app/services/notification_rule_service.py"
      provides: "SUPPORTED_EVENTS list with monitoring.alert and agent.message entries"
      contains: "monitoring.alert"
  key_links:
    - from: "app/services/monitoring_job_service.py"
      to: "app/services/notification_rule_service.py"
      via: "dispatch_notification('monitoring.alert') → rule query matches SUPPORTED_EVENTS entry"
      pattern: "monitoring\\.alert"
    - from: "app/routers/integrations.py"
      to: "app/services/notification_rule_service.py"
      via: "dispatch_notification('agent.message') → rule query matches SUPPORTED_EVENTS entry"
      pattern: "agent\\.message"
---

<objective>
Add 2 missing event types to NotificationRuleService.SUPPORTED_EVENTS so that monitoring alerts reach Slack/Teams and the test-notification button verifies connectivity.

Purpose: Close the 2 integration wiring gaps found in the v6.0 milestone audit. Both gaps share a single root cause — the SUPPORTED_EVENTS list in notification_rule_service.py is missing entries for "monitoring.alert" and "agent.message", causing dispatch_notification to find no matching rules and silently return empty results.

Output: Updated notification_rule_service.py with both event types registered.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md
@app/services/notification_rule_service.py
</context>

<interfaces>
<!-- The only code contract that matters: the SUPPORTED_EVENTS list shape -->

From app/services/notification_rule_service.py (line 41-49):
```python
SUPPORTED_EVENTS: list[dict[str, str]] = [
    {"type": "approval.pending", "label": "Approval Required"},
    {"type": "task.created", "label": "Task Created"},
    {"type": "task.completed", "label": "Task Completed"},
    {"type": "workflow.completed", "label": "Workflow Completed"},
    {"type": "workflow.failed", "label": "Workflow Failed"},
    {"type": "campaign.milestone", "label": "Campaign Milestone"},
    {"type": "report.ready", "label": "Report Ready"},
]
```

Callers that depend on these entries existing:
- `app/services/monitoring_job_service.py` line 395: `dispatch_notification(event_type="monitoring.alert", ...)`
- `app/routers/integrations.py` line 1380: `dispatch_notification(event_type="agent.message", ...)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add monitoring.alert and agent.message to SUPPORTED_EVENTS</name>
  <files>app/services/notification_rule_service.py</files>
  <action>
Open `app/services/notification_rule_service.py` and add 2 entries to the `SUPPORTED_EVENTS` class variable (line 41-49):

1. `{"type": "monitoring.alert", "label": "Monitoring Alert"}` — enables Phase 46 MonitoringJobService alerts to route through notification rules to Slack/Teams channels
2. `{"type": "agent.message", "label": "Agent Message"}` — enables the Phase 45 test-notification endpoint (`POST /integrations/{provider}/test-notification`) to verify provider connectivity without returning 502

Insert both entries at the end of the list, before the closing `]`. Follow the exact `{"type": "...", "label": "..."}` dict format used by existing entries.

Do NOT change any other code in this file. The fix is purely additive — 2 new dicts appended to an existing list.
  </action>
  <verify>
    <automated>cd C:/Users/expert/documents/pka/pikar-ai && uv run python -c "from app.services.notification_rule_service import NotificationRuleService; evts = {e['type'] for e in NotificationRuleService.SUPPORTED_EVENTS}; assert 'monitoring.alert' in evts, 'missing monitoring.alert'; assert 'agent.message' in evts, 'missing agent.message'; print(f'OK: {len(NotificationRuleService.SUPPORTED_EVENTS)} events, both new types present')"</automated>
  </verify>
  <done>SUPPORTED_EVENTS contains exactly 9 entries (7 original + monitoring.alert + agent.message). The monitoring alert dispatch path and test-notification endpoint can now match rules and deliver to configured channels.</done>
</task>

</tasks>

<verification>
1. Import check: `from app.services.notification_rule_service import NotificationRuleService` succeeds
2. SUPPORTED_EVENTS contains `"monitoring.alert"` and `"agent.message"` entries
3. Existing tests still pass: `uv run pytest tests/unit/services/test_notification_rule_service.py -x`
4. Lint passes: `uv run ruff check app/services/notification_rule_service.py`
</verification>

<success_criteria>
- NotificationRuleService.SUPPORTED_EVENTS includes {"type": "monitoring.alert", "label": "Monitoring Alert"}
- NotificationRuleService.SUPPORTED_EVENTS includes {"type": "agent.message", "label": "Agent Message"}
- All existing tests pass unchanged
- Lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-notification-event-type-wiring/48-01-SUMMARY.md`
</output>
