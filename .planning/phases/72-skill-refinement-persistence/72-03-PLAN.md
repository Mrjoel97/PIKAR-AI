---
phase: 72-skill-refinement-persistence
plan: 03
type: execute
wave: 3
depends_on: [72-01, 72-02]
files_modified: []
autonomous: false
requirements: [SIE-06]

must_haves:
  truths:
    - "After the engine runs _execute_skill_refined for a skill, restarting the FastAPI process and sending a message that invokes that skill returns the refined knowledge, not the original"
  artifacts: []
  key_links:
    - from: "app/skills/skill_hydration.py"
      to: "app/skills/registry.py"
      via: "hydrate_skills_from_db patches skills_registry on startup"
      pattern: "skill\\.knowledge ="
---

<objective>
UAT gate: verify the complete persistence loop -- refine a skill, restart the process, confirm the refined knowledge is served.

Purpose: This is the end-to-end proof that SIE-01 through SIE-05 work together. Without this gate, we cannot be confident that cold-start hydration actually surfaces refined knowledge.

Output: Human-verified confirmation that the persistence loop works.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-skill-refinement-persistence/72-01-PLAN.md
@.planning/phases/72-skill-refinement-persistence/72-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT -- Verify skill refinement persistence survives restart</name>
  <files></files>
  <action>
Present the user with two verification options:

**Option A -- Full live verification (requires local Supabase):**

1. Start local Supabase: `supabase start`
2. Apply the migration: `supabase db push --local`
3. Start backend: `make local-backend`
4. Trigger a refinement cycle: `curl -X POST http://localhost:8000/self-improvement/run-cycle -H "Content-Type: application/json" -d '{"auto_execute": true, "days": 30}'`
5. Check that a skill_versions row was created: `curl http://localhost:8000/self-improvement/skills/{skill_name}/history` (use a skill name from the cycle output)
6. Stop the backend (Ctrl+C)
7. Restart: `make local-backend`
8. Verify the refined knowledge is served by checking the skill's knowledge via the registry or sending a chat message that invokes that skill

**Option B -- Unit test verification (no Supabase required):**

1. Run all Phase 72 tests: `uv run pytest tests/unit/test_skill_version_persistence.py tests/unit/test_skill_hydration.py -x -v`
2. All tests pass confirming:
   - Write-through inserts into skill_versions
   - Revert reads and restores from skill_versions
   - Hydration patches registry from active versions
   - History endpoint returns version chain
3. Verify migration SQL is syntactically valid: `cat supabase/migrations/20260412100000_skill_versions.sql` and confirm it has CREATE TABLE IF NOT EXISTS, unique partial index, and RLS policies

**Expected outcomes:**
- All unit tests pass
- Migration SQL is complete and idempotent
- (If Option A) History endpoint returns version chain after refinement
- (If Option A) After restart, skill knowledge matches the refined version, not the built-in original
  </action>
  <verify>Human confirms "approved" after verifying persistence loop</verify>
  <done>SIE-06 UAT gate passed: human confirmed the full persistence loop works end-to-end or approved based on unit test coverage</done>
</task>

</tasks>

<verification>
SIE-06 UAT gate passed: human confirmed the full persistence loop works end-to-end.
</verification>

<success_criteria>
- Human confirms refined knowledge survives a process restart (or approves based on unit test coverage)
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/72-skill-refinement-persistence/72-03-SUMMARY.md`
</output>
