---
phase: 50-billing-payments
plan: 04
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-02
  - 50-03
files_modified:
  - frontend/src/components/layout/PremiumShell.tsx
  - frontend/src/app/api/stripe/portal/__tests__/route.test.ts
  - tests/e2e/test_stripe_checkout_flow.py
autonomous: false
requirements:
  - BILL-05

must_haves:
  truths:
    - "A user who completes a Stripe test-mode checkout sees their tier change in the SubscriptionBadge without refreshing the page"
    - "The SubscriptionBadge is visible in the shell header on all authenticated pages for all personas"
    - "A user with an active subscription can click 'Manage Subscription' on /dashboard/billing, land on the Stripe Customer Portal, cancel the plan, and see the badge reflect the cancelation when they return"
    - "The /api/stripe/portal route is covered by a unit test that asserts: unauthenticated requests are rejected, authenticated requests with no subscription are rejected, authenticated requests with a stripe_customer_id call stripe.billingPortal.sessions.create and return the URL"
    - "A Stripe CLI-triggered checkout.session.completed event creates the row; a subsequent customer.subscription.deleted event clears it — both without manual refresh"
    - "Stripe CLI test run produces passing log entries in stripe_webhook_events (status=processed)"
  artifacts:
    - path: "frontend/src/components/layout/PremiumShell.tsx"
      provides: "PremiumShell with SubscriptionBadge rendered in the header bar (for authenticated users only)"
      contains: "SubscriptionBadge"
    - path: "frontend/src/app/api/stripe/portal/__tests__/route.test.ts"
      provides: "Vitest unit test for the Stripe Customer Portal route covering auth gate, missing-customer-id error, and happy path (BILL-05 test coverage)"
    - path: "tests/e2e/test_stripe_checkout_flow.py"
      provides: "Pytest e2e helper that drives a scripted Stripe CLI webhook trigger sequence and asserts subscriptions + stripe_webhook_events table state (RUN LOCALLY WITH STRIPE CLI — marked @pytest.mark.stripe_cli, skipped in CI)"
  key_links:
    - from: "PremiumShell header"
      to: "SubscriptionBadge from Plan 50-02"
      via: "import { SubscriptionBadge } from '@/components/billing/SubscriptionBadge'"
      pattern: "SubscriptionBadge"
    - from: "User clicks 'Manage Subscription' button"
      to: "Stripe Customer Portal"
      via: "useSubscription().openPortal() → /api/stripe/portal → stripe.billingPortal.sessions.create → redirect"
      pattern: "openPortal"
    - from: "frontend/src/app/api/stripe/portal/__tests__/route.test.ts"
      to: "frontend/src/app/api/stripe/portal/route.ts"
      via: "vitest unit test importing the POST handler with mocked Supabase + Stripe SDK"
      pattern: "stripe.*portal"
---

<objective>
Place the SubscriptionBadge in the authenticated shell header so every page shows real-time subscription status, add a vitest unit test for the previously-untested /api/stripe/portal route, and drive an end-to-end Stripe CLI-based UAT that exercises checkout → badge update → portal cancel → badge update. BILL-05 (portal verification + test coverage). This is the gate-keeper plan that proves the entire billing flow works end-to-end. BILL-01 (event-ordering race) is now closed by Plan 50-01's source-of-truth refactor and is regression-tested via the BILL-01 vitest case in 50-01; this plan re-validates it as part of UAT Step C.

Purpose: Plans 50-01/02/03 ship the pieces in isolation. This plan wires the badge into the shell, adds the missing portal route test coverage that no upstream plan provided, and runs a human-in-the-loop verification against Stripe test mode to confirm the full user journey works before Phase 50 closes. A checkpoint is required because Stripe CLI (`stripe listen`, `stripe trigger`) is a local dev tool that the executor cannot run autonomously — the user must have the Stripe CLI installed, be logged in with `stripe login`, and have the dev stack running.

Output: Badge placed in shell, vitest portal route test, scripted e2e test file for repeatable local UAT, human checkpoint confirming the flow.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/50-billing-payments/50-01-SUMMARY.md
@.planning/phases/50-billing-payments/50-02-SUMMARY.md
@.planning/phases/50-billing-payments/50-03-SUMMARY.md

@frontend/src/app/dashboard/billing/page.tsx
@frontend/src/contexts/SubscriptionContext.tsx
@frontend/src/app/api/stripe/portal/route.ts

<interfaces>
<!-- SubscriptionBadge (shipped by Plan 50-02) -->
```typescript
// frontend/src/components/billing/SubscriptionBadge.tsx
// Zero props — reads useSubscription() from context.
// data-testid="subscription-badge", data-state="active|past_due|trial|canceling|free|canceled|loading"
export function SubscriptionBadge(): JSX.Element;
```

<!-- Existing checkout + portal route contracts (already shipped) -->
// POST /api/stripe/checkout — Body: { priceId: string } — Returns: { url: string } (redirect)
// POST /api/stripe/portal — No body — Returns: { url: string } (redirect)
//   Requires: authenticated user via Supabase session cookie
//   Resolves: subscriptions.stripe_customer_id for the user
//   Returns: 401 if not authenticated, 400 if no stripe_customer_id, 200 + { url } on success

<!-- useSubscription hook (Plan 50-02 enhanced) -->
```typescript
const sub = useSubscription();
// sub.tier, sub.willRenew, sub.hasBillingIssue, sub.periodEnd, sub.checkout(priceId), sub.openPortal(), sub.refresh()
```

<!-- PremiumShell (search for header / nav area) -->
// frontend/src/components/layout/PremiumShell.tsx is the authenticated shell wrapping dashboard pages.
// Must inspect the file structure before editing — header is likely a <header> or nav element near the top.
</interfaces>

<user_setup>
REQUIRED BEFORE EXECUTION (for the UAT checkpoint, NOT for Tasks 1 and 2):
1. Stripe CLI installed locally (`brew install stripe/stripe-cli/stripe` on macOS, or https://github.com/stripe/stripe-cli/releases)
2. `stripe login` completed (opens browser, confirms account)
3. Local dev stack running:
   - `supabase start` (migrations applied)
   - `docker compose up` (backend + redis)
   - `cd frontend && npm run dev` (frontend on :3000)
4. Environment variables set in `frontend/.env.local`:
   - STRIPE_SECRET_KEY (test mode — sk_test_...)
   - STRIPE_WEBHOOK_SECRET (obtained from `stripe listen` output below)
   - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY (test mode — pk_test_...)
   - NEXT_PUBLIC_STRIPE_PRICE_SOLOPRENEUR=price_xxx (real Stripe test-mode price ID for Solopreneur plan)
   - STRIPE_PRICE_TIER_MAP=price_xxx:solopreneur,...
5. In a terminal, run `stripe listen --forward-to http://localhost:3000/api/webhooks/stripe` and copy the whsec_... into STRIPE_WEBHOOK_SECRET. Restart `npm run dev` so it picks up the secret.
6. A test user account exists in Supabase (any dev account works).
</user_setup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place SubscriptionBadge in PremiumShell header</name>
  <files>frontend/src/components/layout/PremiumShell.tsx</files>
  <action>
    Step 1: Read frontend/src/components/layout/PremiumShell.tsx to understand the existing header structure.

    Step 2: Import SubscriptionBadge at the top:
    ```typescript
    import { SubscriptionBadge } from '@/components/billing/SubscriptionBadge';
    ```

    Step 3: Place the badge in the header bar — adjacent to the existing user avatar / menu / notifications area. The exact placement depends on the file structure; look for where NotificationCenter or the user menu is rendered and add SubscriptionBadge next to it.

    Step 4: Ensure the badge is only rendered when ready (PremiumShell is already inside SubscriptionProvider because SubscriptionProvider wraps dashboard/layout.tsx per Plan 50-02 discovery — no provider re-wrap needed).

    DO NOT modify frontend/src/app/dashboard/layout.tsx — the provider is already there. Only PremiumShell.tsx changes.

    DO NOT add any state, fetching, or refresh logic in PremiumShell — the badge reads context directly.

    Lint: `cd frontend && npm run lint` — clean.

    Commit: feat(50-04): render SubscriptionBadge in PremiumShell header
  </action>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai/frontend" && grep -c "SubscriptionBadge" src/components/layout/PremiumShell.tsx && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PremiumShell.tsx imports SubscriptionBadge
    - Badge is rendered in the header JSX (grep count ≥ 2: import + usage)
    - No lint errors
    - No provider changes (SubscriptionProvider remains in dashboard/layout.tsx)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest unit test for /api/stripe/portal route (BILL-05 test coverage gap)</name>
  <files>
    frontend/src/app/api/stripe/portal/__tests__/route.test.ts
  </files>
  <behavior>
    The Stripe Customer Portal route at frontend/src/app/api/stripe/portal/route.ts already exists from a prior phase but has ZERO test coverage. This task closes that gap so BILL-05 has both implementation AND automated regression tests (the existing UAT in Task 4 only runs locally with Stripe CLI; CI needs unit-level coverage too).

    RED tests (must fail until the test file is filled in — existing route handler should make them pass once mocks are wired correctly):

    - Test 1 — "401 when no Supabase session": POST /api/stripe/portal with no auth cookie. Mock supabase.auth.getUser() to return { data: { user: null }, error: ... }. Assert response.status === 401, no Stripe SDK call made.

    - Test 2 — "400 when user has no stripe_customer_id": Mock supabase.auth.getUser() to return a valid user, but supabase.from('subscriptions').select(...).eq('user_id',...).single() returns { data: { stripe_customer_id: null }, error: null }. Assert response.status === 400, response body contains an error message mentioning the missing customer (e.g. "no customer", "not subscribed", or whatever the existing route emits — read the route file first to match its actual error string), no Stripe SDK call made.

    - Test 3 — "200 + portal URL on happy path": Mock supabase.auth.getUser() to return a valid user, mock the subscriptions select to return { stripe_customer_id: 'cus_test123' }. Mock stripe.billingPortal.sessions.create to resolve with { url: 'https://billing.stripe.com/session/test' }. Assert response.status === 200, response.json().url === 'https://billing.stripe.com/session/test', and that stripe.billingPortal.sessions.create was called with { customer: 'cus_test123', return_url: <some app URL> }.

    - Test 4 — "500 when Stripe SDK throws": Same setup as Test 3 but stripe.billingPortal.sessions.create rejects with a Stripe error. Assert response.status === 500 and response body has an error message; ensure the error message does NOT leak the Stripe error stack to the client (only a generic message).
  </behavior>
  <action>
    Step 1: Read frontend/src/app/api/stripe/portal/route.ts to understand:
    - The exact import paths (Supabase server client, Stripe SDK)
    - The exact error messages and status codes the existing handler emits
    - Whether it reads the user via `createServerClient` cookies or another pattern
    - The return_url it constructs (app origin? hardcoded? from env var?)

    Match the test expectations to the ACTUAL behaviour of the route — do NOT speculate. If the route returns 403 instead of 401 for missing auth, write the test for 403. If it returns a generic 500 instead of 400 for missing customer, adjust accordingly. The goal is REGRESSION COVERAGE of the existing implementation, not redesigning it.

    Step 2 (RED): Create frontend/src/app/api/stripe/portal/__tests__/route.test.ts using Vitest. Mock '@supabase/ssr' (or whichever Supabase server client the route uses) and 'stripe' via vi.mock(). Build the 4 test cases per <behavior>, importing the POST handler from '../route' and calling it with a fabricated NextRequest.

    Run: `cd frontend && npx vitest run src/app/api/stripe/portal/__tests__/route.test.ts` — initially expect 0 tests (file doesn't exist). After file creation, expect all 4 to PASS immediately because the route already exists and the tests describe its actual behaviour. If any test fails, the failure indicates either a bug in the route OR a mismatch between the test's mock and the route's actual call shape. Reconcile by reading the route source again — do NOT modify the route source in this task (out of scope; flag as a follow-up if a real bug is found).

    Step 3: Run `cd frontend && npm run lint` — clean.

    DO NOT modify frontend/src/app/api/stripe/portal/route.ts in this task. If the test discovers a real bug, document it in the SUMMARY and create a follow-up — Phase 50's portal route shipped previously and is in production scope; behavioural changes need their own plan.

    Commit: test(50-04): add unit test coverage for /api/stripe/portal route (BILL-05)
  </action>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai/frontend" && npx vitest run src/app/api/stripe/portal/__tests__/route.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>
    - Test file exists with 4 test cases (auth gate, missing customer, happy path, Stripe error)
    - All 4 tests pass against the existing /api/stripe/portal/route.ts
    - No modification to route.ts
    - npm run lint clean
    - If any real bug discovered, documented in SUMMARY as a follow-up (not silently fixed)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create scripted Stripe CLI e2e helper (skipped in CI)</name>
  <files>tests/e2e/test_stripe_checkout_flow.py</files>
  <action>
    Create tests/e2e/test_stripe_checkout_flow.py as a pytest-marker-gated helper that encodes the UAT script below. Mark with `@pytest.mark.stripe_cli` and add a skip condition `if not os.environ.get("STRIPE_CLI_ENABLED"): pytest.skip(...)`. This is an operator aid, not a CI test.

    The test should:
    1. Verify Stripe CLI is on PATH (shutil.which('stripe')) — skip if not present with a useful message.
    2. Use subprocess to run `stripe trigger checkout.session.completed --add checkout_session:metadata.supabase_user_id=${TEST_USER_ID}` and assert exit code 0.
    3. Poll the Supabase REST endpoint (or direct psql) to verify a row appears in `subscriptions` with is_active=true for TEST_USER_ID within 5 seconds (because customer.subscription.created — delivered alongside checkout.session.completed — is now the source of truth per Plan 50-01's BILL-01 fix).
    4. Verify a row appears in `stripe_webhook_events` with status='processed'.
    5. Run `stripe trigger customer.subscription.deleted --add subscription:metadata.supabase_user_id=${TEST_USER_ID}` and poll for the subscription row to show is_active=false.
    6. Verify a second row in stripe_webhook_events with status='processed'.

    The test should be RE-RUNNABLE — wrap in a setup/teardown that deletes test rows from both tables before each run. DO NOT drop tables.

    Provide clear instructions at the top of the file as a docstring:
    ```python
    """Stripe CLI end-to-end UAT helper — Plan 50-04.

    PREREQUISITES:
      - Stripe CLI installed and `stripe login` completed
      - Local stack running (supabase + frontend + backend)
      - STRIPE_WEBHOOK_SECRET matches `stripe listen` output
      - TEST_USER_ID env var set to a real Supabase user UUID
      - STRIPE_CLI_ENABLED=1 env var to opt in

    RUN WITH:
      STRIPE_CLI_ENABLED=1 TEST_USER_ID=<uuid> uv run pytest tests/e2e/test_stripe_checkout_flow.py -v -s

    This test is SKIPPED in CI by default — it requires Stripe CLI and live local services.
    """
    ```

    Place supporting queries in helper functions: _poll_subscription(user_id, expected_active: bool, timeout: int=10), _clear_test_rows(user_id), _assert_webhook_event(event_id_pattern).

    Lint: `uv run ruff check tests/e2e/test_stripe_checkout_flow.py && uv run ruff format tests/e2e/test_stripe_checkout_flow.py` — clean. Do NOT run the test in this task — it requires user setup.

    Commit: test(50-04): add Stripe CLI e2e helper for local billing UAT
  </action>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai" && uv run ruff check tests/e2e/test_stripe_checkout_flow.py && uv run pytest tests/e2e/test_stripe_checkout_flow.py --collect-only 2>&1 | tail -15</automated>
  </verify>
  <done>
    - File exists with proper pytest skip marker
    - Ruff + ruff format clean
    - `pytest --collect-only` discovers the test (proves importable)
    - Test is skipped without STRIPE_CLI_ENABLED (safe for CI)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — end-to-end billing flow with Stripe CLI</name>
  <files>N/A — human verification of Tasks 1, 2, 3 output plus all Phase 50 artifacts already committed</files>
  <action>
    AUTOMATION FIRST — the executor MUST complete ALL of the following BEFORE pausing for human verification:

    1. Run `uv run pytest tests/unit/admin/test_billing_api.py tests/unit/services/test_billing_metrics_service.py -x` and confirm all pass (regression check that Plans 50-01 and 50-03 backend work is green).
    2. Run `cd frontend && npx vitest run src/contexts/__tests__/SubscriptionContext.test.tsx src/components/billing/__tests__/SubscriptionBadge.test.tsx src/app/api/webhooks/stripe/__tests__/route.test.ts src/app/api/stripe/portal/__tests__/route.test.ts` and confirm all pass (regression check that Plans 50-01, 50-02, AND the new portal test from Task 2 are green).
    3. Run `cd frontend && npm run lint` — no errors.
    4. Run `uv run ruff check app/services/billing_metrics_service.py app/routers/admin/billing.py` — clean.
    5. Print a summary table to the terminal showing: Plan → Requirements → Test results (pass/fail counts).
    6. Verify the dev stack can be started by printing the EXACT commands the user needs to run (do NOT start background processes — just emit the commands and wait).

    Only AFTER steps 1-6 pass, present the human UAT script to the user via the <how-to-verify> block below and wait for resume-signal.

    If ANY automated step (1-4) fails, do NOT present the human UAT — instead, report which test failed and stop. The user will re-spawn the executor to fix the upstream plan.
  </action>
  <how-to-verify>
    PREREQUISITES (do this ONCE before clicking through the flow):
    1. Install Stripe CLI if not already: https://stripe.com/docs/stripe-cli#install
    2. `stripe login` — opens browser, confirm pairing
    3. Create 4 test-mode products + prices in Stripe Dashboard (if not already) matching PLAN_CONFIG in frontend/src/app/dashboard/billing/page.tsx:
       - Solopreneur $99/mo → price_xxx
       - Startup $297/mo → price_yyy
       - SME $597/mo → price_zzz
       - Enterprise (custom — can skip)
    4. Put the price IDs in `frontend/.env.local`:
       ```
       NEXT_PUBLIC_STRIPE_PRICE_SOLOPRENEUR=price_xxx
       NEXT_PUBLIC_STRIPE_PRICE_STARTUP=price_yyy
       NEXT_PUBLIC_STRIPE_PRICE_SME=price_zzz
       STRIPE_PRICE_TIER_MAP=price_xxx:solopreneur,price_yyy:startup,price_zzz:sme
       ```
    5. In one terminal: `stripe listen --forward-to http://localhost:3000/api/webhooks/stripe` — copy the `whsec_...` output and put it in `frontend/.env.local` as `STRIPE_WEBHOOK_SECRET=whsec_...`. Leave this terminal running — it streams events in real time.
    6. Start the stack:
       - Terminal 2: `supabase start` (if not already running)
       - Terminal 3: `docker compose up` (backend + redis)
       - Terminal 4: `cd frontend && npm run dev`

    MANUAL UAT STEPS — you MUST execute Steps A → B → C → D → E → F in order. Step D (portal cancel) REQUIRES that Step B (checkout) has been completed first, because the portal can only be opened for a user who has an active subscription. Skipping Step B will cause Step D to fail with "no customer" — that is expected, NOT a bug. Report each step pass/fail.

    **Step A: Empty state**
    1. Sign in at http://localhost:3000
    2. Navigate to any authenticated page
    3. Observe the SubscriptionBadge in the shell header shows "Free" (gray)

    **Step B: Checkout flow (PREREQUISITE for Steps C, D, E, F)**
    1. Navigate to http://localhost:3000/dashboard/billing
    2. Click "Subscribe" (or equivalent) on the Solopreneur plan
    3. You are redirected to a Stripe Checkout page (checkout.stripe.com)
    4. Use test card `4242 4242 4242 4242`, any future expiry, any CVC, any ZIP
    5. Complete the checkout
    6. You are redirected back to /dashboard/billing?checkout=success
    7. The SubscriptionBadge in the shell header updates to "Active · Solopreneur" (emerald) within 5 seconds — WITHOUT manually refreshing the page
    8. In the `stripe listen` terminal, you see 2xx responses for:
       - checkout.session.completed (handler is now demoted — only writes stripe_customer_id)
       - customer.subscription.created (handler now writes the full state — sole source of truth per Plan 50-01)
    9. Run `psql postgresql://postgres:postgres@localhost:54322/postgres -c "SELECT user_id, tier, is_active, last_event_type FROM subscriptions;"` — your row shows tier='solopreneur', is_active=true, last_event_type='customer.subscription.created' (NOT 'checkout.session.completed' — that handler no longer touches tier/state)
    10. Run `psql ... -c "SELECT event_id, type, status FROM stripe_webhook_events ORDER BY received_at DESC LIMIT 5;"` — you see status='processed' rows for both event types

    **Step C: Idempotency + BILL-01 event-ordering check (depends on Step B)**
    1. Re-send the checkout.session.completed event from the Stripe Dashboard (Developers → Webhooks → click endpoint → click any recent checkout.session.completed event → "Resend")
    2. In the stripe listen terminal, you see a 200 response
    3. `psql ... -c "SELECT event_id, status FROM stripe_webhook_events WHERE event_id = 'evt_xxx';"` — still only ONE row for that event_id (idempotency confirmed)
    4. CRITICAL BILL-01 CHECK: `psql ... -c "SELECT tier, is_active, will_renew, last_event_type FROM subscriptions WHERE user_id='<your_id>';"` — tier is STILL 'solopreneur', is_active STILL true, will_renew STILL true, last_event_type STILL 'customer.subscription.created' (the late checkout.session.completed re-delivery did NOT regress the row — it only touched stripe_customer_id which was already set)

    **Step D: Portal + cancel flow — BILL-05 (REQUIRES Step B completed)**
    1. Back on /dashboard/billing, click "Manage Subscription" (or equivalent)
    2. You are redirected to billing.stripe.com (if you skipped Step B, this fails with "no customer" — that is correct behaviour, go back and complete Step B first)
    3. Click "Cancel subscription" → Confirm
    4. Return to the app (the portal has a back link)
    5. The SubscriptionBadge updates to "Canceling" (amber) WITHOUT a page reload — because cancel_at_period_end=true is reflected via webhook → Supabase Realtime → badge
    6. In stripe listen: you see customer.subscription.updated with 200
    7. CRITICAL BILL-01 RE-CHECK: `psql ... -c "SELECT will_renew FROM subscriptions WHERE user_id='<your_id>';"` returns false. Now resend ANY older checkout.session.completed event from the Stripe Dashboard for this customer. Re-query — will_renew MUST STILL be false (the demoted checkout.session.completed handler cannot regress will_renew).

    **Step E: Admin billing dashboard (BILL-04 — depends on Step B)**
    1. Open http://localhost:3000/admin/billing as an admin user
    2. MRR card shows $99 (from your 1 active solopreneur)
    3. Active Subscriptions card shows 1
    4. Churn Rate shows 0% (no canceled yet — but "canceling" is not "canceled" until period_end)
    5. Plan Distribution chart shows 1 solopreneur
    6. Open `/admin/billing` in devtools Network tab, observe `/admin/billing/summary` response — confirm `mrr: 99`, `data_source: "db_only"` (unless Stripe integration proxy is configured)

    **Step F: Real-time badge regression (depends on Step B)**
    1. Open /dashboard in one browser tab. Leave it on the page.
    2. In a second tab, go to Stripe Dashboard → Customers → your test customer → click "Update subscription" → change plan to Startup.
    3. Return to the first tab WITHOUT refreshing.
    4. The SubscriptionBadge updates from "Active · Solopreneur" to "Active · Startup" within 5 seconds — proves real-time update path is working.

    REPORT EACH CHECKBOX. If any fail, describe the failure mode (no badge update? webhook 500? missing row?). The executor will diagnose and propose fixes before marking the plan done.
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/expert/documents/pka/pikar-ai" && uv run pytest tests/unit/admin/test_billing_api.py tests/unit/services/test_billing_metrics_service.py -x 2>&1 | tail -15 && cd frontend && npx vitest run src/contexts/__tests__/SubscriptionContext.test.tsx src/components/billing/__tests__/SubscriptionBadge.test.tsx src/app/api/webhooks/stripe/__tests__/route.test.ts src/app/api/stripe/portal/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All pre-checkpoint automated regression tests pass (Plans 50-01/02/03 + the new 50-04 portal test all green)
    - Human has completed UAT steps A-F in order and typed "approved"
    - Step C BILL-01 regression check passed (re-delivered checkout.session.completed did NOT regress state)
    - Any reported issues have been diagnosed and closed
    - Phase 50 ROADMAP success criteria 1-5 all demonstrated working in test mode
  </done>
  <resume-signal>Type "approved" if all 6 flows (A-F) pass. Type "issues: &lt;description&gt;" if any step fails so the executor can diagnose.</resume-signal>
</task>

</tasks>

<verification>
1. Task 1: PremiumShell imports + renders SubscriptionBadge
2. Task 2: Vitest portal route test exists and passes 4/4 against the existing /api/stripe/portal/route.ts
3. Task 3: Pytest discovers but skips tests/e2e/test_stripe_checkout_flow.py without STRIPE_CLI_ENABLED
4. Task 4 (checkpoint): Human confirms all 6 UAT flows pass IN ORDER (A → B → C → D → E → F), with explicit BILL-01 regression check in Step C and Step D
5. Post-checkpoint: `uv run pytest tests/unit/admin/test_billing_api.py tests/unit/services/test_billing_metrics_service.py && cd frontend && npx vitest run` — all Phase 50 tests pass
6. Manual: `psql ... -c "SELECT count(*) FROM stripe_webhook_events WHERE status='processed';"` returns ≥ 2 after the UAT run (proves webhooks delivered and deduped)
</verification>

<success_criteria>
- SubscriptionBadge visible in shell header on all authenticated pages
- /api/stripe/portal route has unit test coverage (4 vitest cases) — closes BILL-05 test gap
- End-to-end checkout → badge update flow works (validates Plans 50-01 + 50-02)
- Stripe Customer Portal cancel flow works (BILL-05)
- Webhook idempotency confirmed via Stripe Dashboard "Resend" (validates Plan 50-01)
- BILL-01 event-ordering race confirmed CLOSED via Step C and Step D regression checks (validates Plan 50-01's source-of-truth refactor)
- Admin billing dashboard shows real MRR from DB (validates Plan 50-03)
- Real-time badge updates without page reload (validates Plan 50-02)
- All 5 success criteria from Phase 50 ROADMAP met
</success_criteria>

<output>
After completion, create `.planning/phases/50-billing-payments/50-04-SUMMARY.md` with:
- UAT outcome: which flows passed, any issues found and how resolved
- Stripe test card reference (4242...) and the test user UUID used
- Screenshots or console log excerpts confirming webhook processing (especially the BILL-01 Step C regression check showing will_renew preserved across late checkout.session.completed re-delivery)
- Any env var documentation updates (add STRIPE_PRICE_* to .env.example if not already)
- AUDITED_ROUTES note: Document that the Python AuditLogMiddleware runs on the FastAPI backend, but the Stripe webhook + checkout + portal routes live in the Next.js frontend. The only FastAPI billing route (/admin/billing) is already excluded via /admin in _EXCLUDED_PREFIXES. No audit-route changes are needed for Phase 50; future verifiers should not re-ask this question.
- Deferred items: production Stripe key rotation runbook, prod webhook endpoint creation — these are NOT part of Phase 50 (deployment-time concerns)
</output>
