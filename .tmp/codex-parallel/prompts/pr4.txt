Implement PR4 only: modularize app/fast_api_app.py without changing endpoint behavior.

Create modules under app/server/ for:
- runtime/bootstrap
- error handlers
- router registration
- feedback endpoint
- health endpoints
- admin endpoints
- chat SSE endpoint

Edit app/fast_api_app.py to become a thin composition layer that still exports app and compatibility symbols used by tests.

Preserve:
- endpoint URLs
- request/response behavior
- import path app.fast_api_app.app

Constraints:
- Prefer re-exporting moved helpers to minimize test churn.
- Do not combine unrelated feature changes.

Before finishing:
- Summarize new modules and what moved into each.
