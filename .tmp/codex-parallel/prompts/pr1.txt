Implement PR1 only: remove event-loop blocking Supabase calls in app/workflows/engine.py.

Required:
- Use app/services/supabase_async.py execute_async() for Supabase query_builder.execute() inside async methods.
- Add a small internal helper in WorkflowEngine for consistent execute_async usage (timeout/op_name) if useful.
- Convert _get_workflow_readiness() to async and update callers.
- Convert best-effort audit write methods to use execute_async.
- Preserve behavior, payloads, and error semantics.

Touch tests only as needed to fix mocks/patches:
- tests/unit/test_workflow_engine_readiness_gate.py
- tests/unit/test_workflow_engine_validation.py (probably no changes)
- tests/unit/test_workflow_template_tool_resolution.py (probably no changes)
- tests/integration/test_workflow_template_lifecycle_endpoints.py (only if mocks break)

Constraints:
- Do not refactor unrelated workflow logic.
- Keep API responses stable.

Before finishing:
- Confirm no direct .execute() calls remain inside async def methods in app/workflows/engine.py.
- Summarize all replacements with op_name labels used.
