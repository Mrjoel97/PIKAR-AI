Implement PR3 only: expand GitHub CI quality gates.

Required:
- Preserve existing workflow template validation jobs in .github/workflows/ci.yml.
- Add backend lint job (ruff).
- Add backend unit tests job.
- Add selected integration tests job (CI-safe subset, mocked/no external secrets).
- Add frontend lint and frontend tests job.
- Use dependency caching where practical.

Files:
- .github/workflows/ci.yml
- Makefile
- frontend/package.json (only if CI-specific scripts are needed)
- pyproject.toml (only if pytest marker/config changes are needed)

Constraints:
- Keep CI readable and maintainable.
- Prefer using Makefile targets if they help avoid duplication.

Before finishing:
- Summarize added jobs and exact commands each job runs.
